# SPDX-License-Identifier: AGPL-3.0-only
# Copyright (C) 2026 RS-Key contributors
#
# THE CLAUSE EVERY PARENT-LEVEL VERDICT ACTUALLY REPORTS. `SEC-FIDO-006C` is the
# third conjunct of `SEC-FIDO-006` (`clause_of = "SEC-FIDO-006"`), and it is the
# one whose gate reads BACKWARDS: `EF_BACKUP_SEALED` is a one-byte marker whose
# ABSENCE is the permissive state, so a torn wipe that drops it over a seed it
# did not destroy re-opens the one-time seed-export window rather than closing
# anything. Its sibling bundles are `SEC-FIDO-006A` and `SEC-FIDO-006B`.
#
# `SEC-FIDO-006C` `ResetKeepsTheBackupSeal`: cfgs=2 mut=1 co=1 kani=1 fuzz=1
# rust=1 test=1, measured by `python3 scripts/assurance_gate.py` at EXIT=0 while
# this was written. It reaches all four rungs of the ladder — model, bounded
# proof, fuzz, board — and the board rung has no recorded run.
#
# Four findings the building of this produced, and only the last is about the
# firmware's own behaviour:
#
#  * `co = 0` FOR THIS CLAUSE IS REFUTED, AND IT WAS REFUTED TWICE OVER.
#    `FORMAL_VERIFICATION_ROADMAP.md` records the clause rows at `co = 0`; the
#    derivation returns `['BugBackupSealedNotAGate']`. Re-measured here against
#    BOTH the committed `scripts/assurance_gate.py` and the uncommitted one
#    another agent is editing in this same tree, and the answer is the same
#    under each — see `x_co_refuted_measured`.
#  * THE COMPANION SWITCH IS NECESSARY, AND THE MODEL SAYS SO IN ITS OWN TEXT.
#    Every configuration arming `BugBackupSealedNotAGate` also arms
#    `BugSeedDoesNotLead`. The reason is derivable without a run and is derived
#    in `x_companion_mechanism` — so the isolation this clause's own
#    configuration buys is of the CLAUSE, never of the defect.
#  * THE BASELINE WAS ALREADY RED WHEN THIS BUNDLE ARRIVED. `python3
#    scripts/bundle_gate.py` exits 1 on all eight existing bundles' `gate_ledger`
#    line, because another agent's uncommitted `assurance/token_refinement.toml`
#    moved a derived count under them. Recorded in `x_baseline_red`, because a
#    new bundle landing into a red gate must not be read as the thing that
#    reddened it.
#  * AND THE ONE BEHAVIOURAL TEST FOR THIS CLAUSE IS A COIN FLIP THAT LOOKS
#    DETERMINISTIC. `a_seed_the_medium_kept_stops_the_wipe_before_the_gates` is
#    green on the shipped tree BY CONSTRUCTION — measured here over sixty fresh
#    processes — and a detector only about 42% of the time on the mutant it is
#    read as catching. `c_coin_flip_control` records what was measured and what
#    was inherited.

# ---- 1. Property, subject and owners -----------------------------------------

[property]
id = "SEC-FIDO-006C"
invariant = "ResetKeepsTheBackupSeal"
# `assurance/properties.toml:153`, verbatim.
statement = "A torn reset never re-opens the one-time seed-export window over a seed it did not destroy: EF_BACKUP_SEALED is the gate that reads backwards."
# THREE subject classes. `hardware` is deliberately absent and so is
# `integration`: no board run of this clause exists, and claiming either would
# make the evidence gate demand a stepping this bundle has no result to name.
subjects = ["requirement", "abstract model", "Rust source"]
subjects_absent = ["FFI", "binary", "hardware", "integration"]
requirement = [
  "docs/threat-model.md#TM-SEED-BACKUP — the clause this property is registered against, and unlike its two siblings' anchor it states the obligation outright. Resolved through assurance/threat_clauses.toml:377, whose `where` is `## Seed backup (the deliberate exception)` at docs/threat-model.md:451; the id is a registry name and not a rendered anchor, which that file's own header says in as many words. The sentence is: after an explicit finalize, export is refused until a full reset regenerates a new seed",
  "the SAME clause, second surface: on the trusted-display flavor the on-device recovery-phrase reveal `runs only inside the same one-time window (and seal closes it)`. So one marker gates two things, and the property is about both",
  "CTAP 2.3 §6.6 authenticatorReset states the END state and its authorization and nothing about a PREFIX of it. Seed backup is a VENDOR surface and is outside CTAP entirely, so for this clause the specification contributes the reset's touch and window and no part of the obligation — which is why the threat-model clause above is the whole requirement rather than a gloss on one",
]
threat_clause = "docs/threat-model.md#TM-SEED-BACKUP"
# The tag the registry's `rust` column counts, and it is ONE line of ONE
# function shared with the two sibling clauses.
owner_tagged = "crates/rsk-fido/src/reset.rs:247 — `Refines RSKeySecurityState!ResetKeepsTheBackupSeal — SEC-FIDO-006C`, on `is_fido_gate_record` at crates/rsk-fido/src/reset.rs:248, whose sibling tags at :245 and :246 carry SEC-FIDO-006A and SEC-FIDO-006B. All three clauses derive `rust = 1` from this one file because `assurance_gate.grep_word` returns FILE names"
# What the tagged predicate is and is not a list of, in its own words.
owner_tag_reading = "crates/rsk-fido/src/reset.rs:237-243 says the list is FIVE and the clauses are three, and that `EF_DEVICE_PIN` and `EF_MINPINLEN` are in the phase and in no clause. Read the tag as `this one of the five`. What makes THIS member different from the other four is at crates/rsk-fido/src/reset.rs:215-220: it is never re-provisioned, its absence is the permissive state, and what it gates is the master seed itself"
# The ordering that IS the property, which carries no tag of its own.
owner_of_the_order = "crates/rsk-fido/src/reset.rs:104-121 — `ctx.state.reset()`, then the fixed two-fid `FIDO_SEED_FIDS` loop, then the secret sweep, then the gate sweep. The comment at crates/rsk-fido/src/reset.rs:118-119 states this clause outright: the secret sweep covers the seed fids too, so a refused seed removal stops the wipe THERE, before the gate phase could drop `EF_BACKUP_SEALED` over a seed still live"
# The gate's TWO consumers, which is the fact that makes its absence dangerous.
owners_that_read_the_marker = [
  "crates/rsk-fido/src/vendor.rs:847 — `backup_export` refuses when `try_backup_sealed` answers true; this is the host `BACKUP_EXPORT` window",
  "crates/rsk-display/src/backup.rs:47 — `can_reveal` conjoins `!st.sealed`; this is the on-device recovery-phrase reveal, and it exists only on a display build",
  "crates/rsk-fido/src/vendor.rs:1035 — `backup_sealed` is `try_backup_sealed(fs).unwrap_or(true)`, so a probe the medium could not answer reads as SEALED. The runtime reader is fail-closed BECAUSE the absent arm is the one that hands out the master seed; the reset is the only path that legitimately makes the record absent, which is exactly what this clause constrains",
]
owners_that_write_the_marker = [
  "crates/rsk-fido/src/vendor.rs:948 — `backup_finalize`, the host `BACKUP_FINALIZE`, behind the PIN gate and a named touch",
  "crates/rsk-fido/src/vendor.rs:1024 — `mark_backup_sealed`, the on-device Settings seal, behind the device PIN and a deliberate hold",
]
# The verification-only Rust twin of the clause.
owner_of_the_alpha = "crates/rsk-fido/src/reset_assurance.rs:122-128 — `reset_keeps_the_backup_seal`, and its conjunction with the two siblings under the parent's name at crates/rsk-fido/src/reset_assurance.rs:130-134"
# What co-refutation patches, and the two tests that stand behind its `killed`.
owner_co_refuted = "formal/comutants.toml:188 — `BugBackupSealedNotAGate`, one hunk over crates/rsk-fido/src/reset.rs, dropping `| EF_BACKUP_SEALED` from the match arm at crates/rsk-fido/src/reset.rs:251"
owners_of_the_kills = [
  "crates/rsk-fido/src/reset_assurance_tests.rs:130 a_backup_seal_swept_with_the_secrets_is_a_reopened_export_window — the deterministic one, and the only test in the tree that states THIS clause behaviourally without a map in the path",
  "crates/rsk-fido/src/reset_assurance_tests.rs:46 reset_property_controls_go_red_on_each_early_gate_mutant — the per-clause negative control; its third arm asserts `!reset_keeps_the_backup_seal`",
  "crates/rsk-fido/src/reset_tests.rs:638 the_gate_set_defers_every_record_whose_absence_is_permissive — a TRANSCRIBER of the match arm the patch edits, and recorded as one",
  "crates/rsk-fido/src/reset_assurance_tests.rs:76 reset_projection_finishes_only_after_every_ordered_phase — the second transcriber",
  "crates/rsk-fido/src/reset_tests.rs:1456 a_seed_the_medium_kept_stops_the_wipe_before_the_gates — the behavioural one, and a coin flip; see c_coin_flip_control",
]

# ---- 2. Commit, build and features -------------------------------------------

[build]
commit = "b185fc3596a3107375fc3f35a597caaacb575b7f"
tree_state = "the runs this bundle records are `formal/runs.toml`'s safety tier as it stood at `b185fc3`, and `formal/runs.toml` now records a LATER tier at `9cafee7`; this bundle's numbers and its logs are the `b185fc3` ones and are internally coherent — read the results table for the current tier's. THE `EMPTY (re-measured for this bundle, zero bytes)` CLAIM THAT STOOD HERE WAS FALSE FOR THE `formal/` HALF, and was already false when this bundle was committed: `git diff b185fc3..HEAD -- formal/*.tla formal/*.cfg formal/*.sh` is 6 files and 153 insertions, and so is the same pathspec at `b185fc3..3e8ab2c`. A re-measurement that reports zero bytes over a diff that is not zero bytes is the finding here, and it is this bundle's own class: the claim was written, not derived. What is true: `git diff b185fc3..HEAD -- crates/rsk-fido/` IS empty, so the Kani and Rust verdicts stand as written; the single hunk in `formal/RSKeySecurityState.tla` is ONE `\\*` comment line (a `ctaphid.rs` citation re-anchored), so `run_count_gate.uncommented` over that module is IDENTICAL across `b185fc3..HEAD`, measured; `formal/Shipped.cfg` and `formal/SoloClause_ResetKeepsTheBackupSeal.cfg` are byte-identical; and the other five changed paths are loaded by no configuration this bundle cites. `formal/floors.txt` HAS moved in uncommented content — the `Historical_*` glob became two exact rows — and it is a verdict registry, not a model input. So every verdict below still describes the model and the code at the recorded commit. THE WORKING TREE IS NOT CLEAN AND IS BEING EDITED BY OTHER AGENTS WHILE THIS IS WRITTEN: `git status --porcelain` shows thirty-five modified paths, among them `scripts/assurance_gate.py`, `assurance/token_refinement.toml`, `formal/comutants.toml`, `formal/gen-configs.sh`, `formal/floors.txt` and `formal/Historical_E76.cfg`. Four of those touch this vertical and each was opened: the `comutants.toml` hunk edits an OATH row's anchors and note and no reset row; the `gen-configs.sh`, `floors.txt` and `Historical_E76.cfg` hunks together re-purpose `Historical_E76.cfg` as a counterfactual and arm `BugSeedDoesNotLead` in it, which changes no configuration this bundle cites and no derived column of this property — `comutate.solo_invariants` for that switch still returns one invariant, and `Historical_` is not a solo-shaped prefix. `formal/RSKeySecurityState.tla`, `formal/Shipped.cfg` and `formal/SoloClause_ResetKeepsTheBackupSeal.cfg` are all clean. The five transcribed `result.gate_*` lines were RE-DERIVED at integration and agree — and that is held rather than promised: `bundle_gate.gate_transcriptions` derives all five from the gates themselves on every run"
matrix_column = "firmware"
cargo_features = "the default workspace feature set, with no feature flag added or removed. The one Kani harness of this clause is the exception and builds under `--features kani-soft` with `--cfg kani`. `fips-profile` and `display` are the two flags that change what this clause is ABOUT rather than how it is checked, and each has its own assumption row"
board_knobs = "BOARD unset, LED_KIND unset, the default 4 MB layout. None of the three is on this clause's path — the marker is one flash record and the wipe order is the whole mechanism — which is why the two equivalent columns below are equivalent"
host_triple = "aarch64-apple-darwin"
firmware_triple = "thumbv8m.main-none-eabihf"
machine = "18 cores, macOS 27.0 aarch64 — the figure TLC's own banner records in each of the five artifacts below, so the log and this line cannot disagree"
equivalent_columns = ["firmware-pico", "waveshare-one"]

# ---- 3. Method and scope/bounds ----------------------------------------------

# EIGHT ways. The model rung is split in three because the parent's verdict
# cannot name a conjunct and this clause is the conjunct it always names anyway;
# the code rung is split in two because the marker's PHASE and the marker's
# CONSUMERS are different questions with different artifacts.

[[method]]
obligation = "the clause over the whole reachable space of the shipped constants, as one of the twelve names that configuration checks"
method = "model-check"
artifact = "Shipped.cfg::ResetKeepsTheBackupSeal formal/RSKeySecurityState.tla::ResetKeepsTheBackupSeal"
bound_rps = 2
stops_rps = "a torn prefix that first goes wrong over a third relying party — and for THIS clause that costs less than for its siblings, because its antecedent names no credential at all: `snap.seed /\\ snap.sealed` is true with `store.cred` empty, which the recorded counterexample confirms"
bound_channels = 2
stops_channels = "a torn prefix that first goes wrong over a third concurrent channel"
bound_max_retries = 8
stops_max_retries = "nothing by shrinking, and eight IS the shipped ladder at crates/rsk-fido/src/consts.rs:368. This clause names no retry count"
bound_mismatch_limit = 3
stops_mismatch_limit = "nothing by shrinking, for the same reason as the ladder it pairs with"
bound_max_clock = 1
stops_max_clock = "any prefix that needs two units of elapsed time; with the window at zero one Tick is also what closes the window, so the two bounds bite together here"
bound_reset_window = 0
stops_reset_window = "the whole of the shipped power-up window but its first instant. A reset may START only at clock 0, so a reset begun late inside the shipped RESET_WINDOW_MS — 10 000 ms at crates/rsk-fido/src/consts.rs:404 — is a prefix this configuration never tears. The touch is not what is skipped: crates/rsk-fido/src/reset.rs:43-51 runs the ceremony unconditionally after the window check"
bound_invariant_names = 12
stops_invariant_names = "any obligation outside the twelve. TypeOK and eleven invariants is what this configuration checks, and this clause is one of the three added to that block after the existing bundles' runs were recorded — a sibling bundle describing the block as nine names is describing a different file"
shipped_relation = "MaxRetries and MismatchLimit ARE the shipped MAX_PIN_RETRIES and PIN_MISMATCH_LIMIT at crates/rsk-fido/src/consts.rs:368 and :368; RPs and Channels are two against an unbounded device. The clause held at every reachable state of this run, and that is a stronger reading than it looks: TLC evaluates an invariant at every state, so GREEN here is a verdict about `(Idle /\\ snap.seen) => ((snap.seed /\\ snap.sealed) => gate.backupSealed)` over the whole set and not about a sampled prefix. Measured against `sed -n '/^INVARIANTS/,/^SYMMETRY/p' formal/Shipped.cfg`, which lists TypeOK and eleven names"
cfg = "none"
features = "none"

[[method]]
obligation = "non-degeneracy: the clause alone, with its own defect armed, so a RED cannot belong to a sibling conjunct"
method = "model-check"
artifact = "SoloClause_ResetKeepsTheBackupSeal.cfg"
bound_invariants = 2
stops_invariants = "anything about the rest of the invariant set; TypeOK and this one clause is the whole of what the row checks"
bound_armed_switches = 2
stops_armed_switches = "isolation of the DEFECT. Two switches are armed — `BugBackupSealedNotAGate` and its companion `BugSeedDoesNotLead` — so what this configuration isolates is the CLAUSE, and the falsifying behaviour needs both. The mechanism is derived in `x_companion_mechanism` rather than asserted"
bound_depth = 8
stops_depth = "nothing by capping — eight is a MEASURED trace length and not a bound. What it says is why the parent's verdict cannot name a conjunct: this clause falls two states shallower than either sibling, so a search checking all three reports this one every time"
bound_distinct_states = 5359
stops_distinct_states = "any behaviour outside the reachable set this configuration explored. It is three orders of magnitude smaller than either sibling clause's run, which is the same fact as the depth: the shallow clause is the cheap one to falsify and the one that masks the others"
shipped_relation = "the configuration checks TypeOK and this clause and nothing else — `formal/gen-configs.sh:358` emits exactly that inside the loop over `CLAUSE_INV` at `formal/gen-configs.sh:121`, and `clause_mutant` at `formal/gen-configs.sh:348` is what pairs this clause with `BugBackupSealedNotAGate`. Its constants are Shipped.cfg's, verbatim, except the two armed switches. `formal/floors.txt:SoloClause_*.cfg` gives the family a wildcard RED row with no invariant column, so the runner does not compare the reported name for these; the name in `formal/runs.toml:138` is the one TLC printed"
cfg = "none"
features = "none"

[[method]]
obligation = "the localisation claim: that every parent-level reset verdict in this tree reports THROUGH this clause, which is the reason the clause split exists at all"
method = "model-check"
artifact = "Solo_BugBackupSealedNotAGate.cfg Solo_BugResetGatesFirst.cfg Solo_BugSeedDoesNotLead.cfg"
bound_configurations = 3
stops_configurations = "a fourth reset-family switch. `BugStateResetAfterWipe` is the one not run here, and it is the parent bundle's row — its falsifying state carries the same three fields, so including it would add a fourth instance and not a fourth kind"
bound_clause_reported = 1
stops_clause_reported = "which conjunct a reader would have to open the trace to learn. The parent invariant is what the verdict column prints; that the trace is this clause is recovered by reading the final state, and no column in this tree carries it"
bound_depths = "8, 8 and 13"
stops_depths = "nothing — the three are MEASURED trace lengths. Read against the two sibling clauses' 16 and 18, they are the whole localisation argument"
bound_switches = 2
stops_switches = "the pair as a unit. Two DISTINCT defects reach this clause at the parent level — the marker's own and the phase-order one — and the third configuration is the companion, present to show it does NOT reach this clause"
shipped_relation = "`formal/RSKeySecurityState.tla:1826-1830` claims all four reset-family mutants report the umbrella invariant on its third clause. Verified here by opening the final state of each artifact rather than by citing the comment: `Solo_BugBackupSealedNotAGate.cfg` and `Solo_BugResetGatesFirst.cfg` both end at `ResetAborts` with `snap.surv` empty, `snap.pin` FALSE, `snap.seed` TRUE, `snap.sealed` TRUE and `gate.backupSealed` FALSE — so the two sibling clauses are VACUOUS there and only this one is falsified, on a switch whose only model owner the sibling clauses are. The third artifact is the control in the other direction: `Solo_BugSeedDoesNotLead.cfg` falls on `NoUnmanageableCredential` at a state with `snap.sealed` FALSE, so the companion does not reach this clause and is not what its verdict is about"
cfg = "none"
features = "none"

[[method]]
obligation = "tier C: the clause holds of the projected reset state after one concrete step out of a symbolic pre-state"
method = "bounded proof"
artifact = "crates/rsk-fido/src/reset_refinement_kani.rs::reset_keeps_the_backup_seal crates/rsk-fido/src/reset_assurance.rs::reset_keeps_the_backup_seal"
bound_steps = 1
stops_steps = "every reachability question. Closure under one symbolic step out of a symbolic pre-state is an induction and carries to sequences; what it cannot say is that any particular interleaving is REACHED, so a step whose precondition no real run satisfies is proved about all the same"
bound_actions = 13
stops_actions = "any reset step the thirteen do not name. `apply_symbolic_step` at crates/rsk-fido/src/reset_refinement_kani.rs:56-94 selects one by `action % 13` and applies it once; a transition the shipped reset can make with no arm here is outside the closure argument. The arm that matters for this clause is `reset.delete(EF_BACKUP_SEALED)` at crates/rsk-fido/src/reset_refinement_kani.rs:77"
bound_symbolic_booleans = 14
stops_symbolic_booleans = "every field of `FidoState` the projection does not carry. Fourteen booleans — six persistent, six snapshot, two volatile — are the WHOLE projection, so what is closed under a step is the projection and not the state"
bound_progress_values = 5
stops_progress_values = "nothing by omission: five is the whole `ResetProgress` enum at crates/rsk-fido/src/reset_assurance.rs:62-68 and the pre-state draws every variant, so this axis is not a shrink"
bound_covers = 1
stops_covers = "every witness but the one. The single `kani::cover!` at crates/rsk-fido/src/reset_refinement_kani.rs:173-178 witnesses `snapshot.seen /\\ snapshot.owner_seed /\\ owner_seed_reachable /\\ persistent.backup_sealed`, and says nothing about any other valuation being reachable"
shipped_relation = "AND THIS ASSERTION IS IMPLIED BY THE PARENT HARNESS ON THE TREE AS IT STANDS, which this bundle says rather than presenting an independent proof: `reset_never_weakens_surviving_state_across_reboot` assumes the same `well_formed(pre)`, applies the same one step, and asserts `well_formed(post)` — whose last conjunct at crates/rsk-fido/src/reset_assurance.rs:267 IS the conjunction. What this harness adds is its cover, which is not implied, and a mutation signal if the conjunction ever loses this conjunct. It is also the reason the property reads BOUNDED at all: `status` is rebuilt from the derivation and `kani > 0` is what selects it. The α is WIDER than the TLA+ clause in one place and that widening belongs to this clause alone — see `c_alpha_is_not_the_model`"
cfg = "kani"
features = "kani-soft"

[[method]]
obligation = "the production side: which site decides the marker's phase, which sites read it, and what the derived column can see of any of that"
method = "review"
artifact = "crates/rsk-fido/src/reset.rs::is_fido_gate_record crates/rsk-fido/src/reset.rs::reset crates/rsk-fido/src/reset.rs::sweep crates/rsk-fido/src/vendor.rs::try_backup_sealed crates/rsk-fido/src/vendor.rs::backup_export"
bound_tagged_sites = 1
stops_tagged_sites = "every owner outside the one tagged site. The clause's whole `rust` column is one `Refines` line on `is_fido_gate_record`, so the ordering block that the property actually lives in — crates/rsk-fido/src/reset.rs:104-121 — carries no tag of its own and is invisible to the derivation"
bound_gate_records = 5
stops_gate_records = "the four gate records this clause does not name. The phase holds five and this clause is about one, so a torn prefix that drops `EF_DEVICE_PIN` or `EF_MINPINLEN` early is outside this argument entirely"
bound_consumers = 2
stops_consumers = "a third reader of the marker. Two exist — the host export at crates/rsk-fido/src/vendor.rs:847 and the on-device reveal at crates/rsk-display/src/backup.rs:47 — and the second is compiled in only on a display build, so on the default image this clause guards one surface and on a display image it guards two. The model has one boolean and no consumer at all"
bound_writers = 2
stops_writers = "any path that could set the marker other than the two finalize surfaces. Both write the same one byte, so no reader can tell which wrote it, and neither can the model"
bound_crates = 1
stops_crates = "the display crate, which is where the second consumer lives. Everything else in this argument is in `rsk-fido`, which is what makes `rust = 1` a count of one file rather than a spread"
shipped_relation = "the marker's phase is decided at exactly one site — the `matches!` arm at crates/rsk-fido/src/reset.rs:251 — and consumed at two, in two different crates, one of them behind a feature. The runtime consumer is FAIL-CLOSED where this clause is fail-open: `backup_sealed` at crates/rsk-fido/src/vendor.rs:1035 answers `true` for a probe the medium could not complete, precisely because the absent arm hands out the master seed. So the only way the window re-opens on a live seed is a reset that removed the record, which is the behaviour this clause constrains and the reason it has no runtime backstop"
cfg = "none — every site above is unconditional production code except crates/rsk-display/src/backup.rs, which is named in prose and not resolved here because it is behind the display feature"
features = "none"

[[method]]
obligation = "the code twin: the model switch as the real defect in production Rust, and what the tree's own suite is actually detecting when it kills it"
method = "mutation"
artifact = "formal/comutants.toml::BugBackupSealedNotAGate scripts/comutate.py::armed_subject crates/rsk-fido/src/reset_assurance_tests.rs::a_backup_seal_swept_with_the_secrets_is_a_reopened_export_window"
bound_twins = 1
stops_twins = "any defect of this shape nobody registered. One comutant credits this clause, against the parent's three, so a variant that re-opens the window some other way has no code-level verdict here"
bound_patch_hunks = 1
stops_patch_hunks = "the CLASS the hunk belongs to. One hunk drops one alternative from one match arm; a defect that needed two edits, or an edit anywhere but that arm, is not expressed"
bound_patched_files = 1
stops_patched_files = "a defect that has to span two files — which the two-consumer shape above makes plausible and no comutant covers"
bound_slice_crates = 1
stops_slice_crates = "any test outside `cargo test -p rsk-fido`, and any schedule but the weekly one — no row of `scripts/check.sh` re-drives this"
bound_deterministic_killers = 1
stops_deterministic_killers = "the two transcribers and the coin flip, which is the whole point of the count. Three tests in the slice go red on this patch and only one of them is about the defect; the other three killers are recorded in the mutation register rather than counted here"
shipped_relation = "`formal/comutants.toml:188` carries `status = \"patch\"` and `expect = \"killed\"`, which that file's own vocabulary at formal/comutants.toml:19-21 defines as a RECORDED verdict and not a plan. It is re-driven by the weekly `comutate run` and by no row of `scripts/check.sh`. THE ROW ALSO CARRIES ITS OWN CORRECTION, and it is the longest note in that file: it was `status = \"unreachable\"` on a verdict MEASURED IN THE MODEL AND WRITTEN ABOUT THE CODE. What the code answers is recorded in the register below, with which part of it this bundle re-measured and which it inherited"
cfg = "none"
features = "none — the slice the row names is `cargo test -p rsk-fido` on the default feature set"

[[method]]
obligation = "the fuzz rung: what the byte-granular probe's oracle asserts about this clause, read rather than run"
method = "review"
artifact = "fuzz/fuzz_targets/power_cut.rs::reset_property_holds fuzz/fuzz_targets/power_cut.rs::reset_probe"
bound_oracle_conjuncts = 2
stops_oracle_conjuncts = "any verdict finer than the two. The oracle at fuzz/fuzz_targets/power_cut.rs:194-204 is one boolean expression whose first conjunct folds the two sibling clauses and whose second is this one, so a failure names the function and the reader has to know which conjunct is which"
bound_separable_asserts = 1
stops_separable_asserts = "the separation the two tiers above have. One assertion covers all three clauses, so a passing run cannot distinguish a clause that was exercised from one the fixture made vacuously true"
bound_snapshot_valuations = 1
stops_snapshot_valuations = "the other snapshot valuations. `reset_probe` writes `EF_CRED`, `EF_PIN`, `EF_ALWAYS_UV` and `EF_BACKUP_SEALED` before the reset at fuzz/fuzz_targets/power_cut.rs:229-232, so `snap.sealed` is TRUE by construction and the symbolic draw the Kani harness makes over it never happens here"
bound_cut_budget_bytes = 1048575
stops_cut_budget_bytes = "a power cut later in the write stream than the budget allows. The cut point is `u32::from_be_bytes([0, data[1] & 0x0f, data[2], data[3]])` at fuzz/fuzz_targets/power_cut.rs:236-241, so a tear further in than that is not a prefix this target can produce"
shipped_relation = "the probe drives the REAL `rsk_fido::reset::reset` over a cuttable mock NOR, then mounts a fresh store, runs boot-time seed provisioning and boots a second time. It is the strongest rung below the board, and it is the ONLY rung of this clause's ladder where the RAM copy of the seed is drawn: `data[4] & 1` loads `state.keydev_dec` at fuzz/fuzz_targets/power_cut.rs:244-246, which is what makes `owner_reachable` in the oracle mean more than the flash record. No log of any run is in the tree and none is in this bundle"
cfg = "none"
features = "none — a nightly `cargo fuzz` build of the fuzz workspace, no feature flag"

[[method]]
obligation = "the board rung, and what its column is worth: this clause OWNS an assertion on hardware, and no run of it has ever been recorded"
method = "accepted risk"
artifact = "tests/29_reset_power_cut.py::cut_during_reset"
bound_recorded_runs = 0
stops_recorded_runs = "everything the board rung would say. The `test` column counts a FILE that names the invariant; no run is recorded anywhere, so nothing here is evidence that a real supply cut on real silicon preserves this clause. `PLAT-FLASH-001` owns the missing result and is `pending`, and its own board record reads `outcome = \"planned\"`"
bound_clause_asserts = 1
stops_clause_asserts = "nothing this clause needs — and it is the one place in the whole ladder where THIS clause is better off than its siblings. `tests/29_reset_power_cut.py:537` asserts `current != owner_seed` under its own name; the sibling clauses share one folded assertion at `tests/29_reset_power_cut.py:526-527` that says only that the operation was refused, without saying which gate refused it"
bound_flavours = 1
stops_flavours = "the display flavour's second surface. The script drives the host `BACKUP_STATE` and `read_seed` path only, so the on-device recovery-phrase reveal — the marker's other consumer — has no board assertion at any rung"
shipped_relation = "the script needs a throwaway board, the no-touch image, and either an operator's hand or a relay, and it can exit inconclusive when the reset finishes before power goes. Its clause-C arm is conditional: it reads `BACKUP_STATE`, and only when the device reports NOT sealed does it try to read the seed back and assert it changed. So a board run in which the marker survived the cut passes this arm without exercising it, which is the correct shape for the property and worth saying out loud beside a column that reads one"
cfg = "none"
features = "none"

# ---- 4. Tool, version, invocation and environment ----------------------------

# SIX tools. cargo-kani is the one on this clause's path that the flake does not
# pin, which is what AS-SEAL-3 is the row about.

[[tool]]
name = "tlaplus"
version = "1.7.4"
provenance = "flake.lock; tla2tools.jar sha256 936a262061c914694dfd… — the prefix formal/run-tlc.sh:16 records, which is the half PLAT-TOOL-003 does not have for cargo-kani"
invocation = "java -XX:+UseParallelGC -Xmx4g -cp $TLA2TOOLS_JAR tlc2.TLC -nowarning -workers 2 -config <cfg> RSKeySecurityState"
environment = "nix develop; WORKERS=2, HEAP=4g — the banner of each artifact below reports that as 3641MB heap and 64MB offheap. Not the liveness tier's larger heap: no configuration in this bundle declares a temporal property"

[[tool]]
name = "openjdk"
version = "1.8.0_492 (Zulu 8.94.0.17-CA-macos-aarch64)"
provenance = "flake.lock"
invocation = "see tlaplus; the banner line of every artifact below records this build"
environment = "nix develop"

[[tool]]
name = "cargo-kani"
version = "0.67.0"
provenance = "NOT in flake.lock — not packaged in nixpkgs. Installed with `cargo install --locked kani-verifier --version $KANI_VERSION`, and KANI_VERSION is a workflow variable. PLAT-TOOL-003 is the registry row that says so"
invocation = "scripts/kani.sh state, which selects `-p rsk-fido --features kani-soft`; this clause's harness is in that tier's roster"
environment = 'RUSTFLAGS=--cfg sha2_backend="soft" --cfg poly1305_force_soft --cfg sha1_force_soft'

[[tool]]
name = "rustc"
version = "1.96.0 (ac68faa20 2026-05-25)"
provenance = "flake.lock"
invocation = "cargo test -p rsk-fido --target aarch64-apple-darwin — the slice the co-refutation row names, and the one this bundle drove for its own control measurement"
environment = "nix develop"

[[tool]]
name = "cargo"
version = "1.96.0 (30a34c682 2026-05-25)"
provenance = "flake.lock"
invocation = "see rustc; `cargo kani` re-invokes cargo per package, which is why the Kani row's feature is written `kani-soft` and not `rsk-fido/kani-soft`"
environment = "nix develop"

[[tool]]
name = "python"
version = "3.13.13"
provenance = "flake.lock"
invocation = "python3 scripts/assurance_gate.py for the vector, python3 scripts/bundle_gate.py for the five transcribed lines, and scripts/comutate.py's own readers for the co-refutation derivation"
environment = "nix develop"

# ---- 5. Principal result -----------------------------------------------------

[result]
# TIER B. Every row is `formal/run-tlc.sh <cfg>` from the safety tier recorded at
# `b185fc3`, WORKERS=2, HEAP=4g, and every number is read out of the artifact in
# the same row rather than out of another bundle's prose.
b_shipped = "Shipped.cfg GREEN — 986 836 197 states, 77 563 872 distinct, depth 58, 1984 s; formal/runs.toml:64, with TLC's own `Finished in 33min 02s` in the artifact. ResetKeepsTheBackupSeal is one of the twelve names that configuration checks, and TLC evaluates an invariant at every reachable state, so the clause held at every one of the 77 563 872"
b_solo_clause = "SoloClause_ResetKeepsTheBackupSeal.cfg RED: ResetKeepsTheBackupSeal — 23 504 states, 5 359 distinct, depth 8, 1 s; formal/runs.toml:138. The counterexample is eight states and no credential is needed at any of them: PressDown, BackupFinalize, ResetStart, TouchConfirm, ResetConfirmed, ResetSweepSecrets, ResetAborts. The marker goes at state 7, in the SECRETS phase, and the run ends Idle with store.seed TRUE, snap.seen TRUE, snap.seed TRUE, snap.sealed TRUE and gate.backupSealed FALSE"
b_solo_clause_reading = "and the route is what separates this clause from its two siblings. Theirs fall through ResetSweepGates with a surviving credential; this one falls through ResetSweepSecrets with `snap.surv` empty, because its antecedent names the seed and not a credential. That is also why it needs depth 8 where they need 16 and 18, and why it is the conjunct a search reaches first"
b_parent_reports = "Solo_BugBackupSealedNotAGate.cfg RED: ResetNeverWeakensSurvivingState — 23 258 states, 5 312 distinct, depth 8, 1 s (formal/runs.toml:103) and Solo_BugResetGatesFirst.cfg RED: ResetNeverWeakensSurvivingState — 23 327 states, 5 331 distinct, depth 8, 1 s (formal/runs.toml:119). TWO DIFFERENT SWITCHES, ONE REPORTED NAME, AND BOTH TRACES ARE THIS CLAUSE: each ends at ResetAborts with snap.surv empty, snap.pin FALSE, snap.seed TRUE, snap.sealed TRUE and gate.backupSealed FALSE, so the two sibling clauses are vacuous in both falsifying states. Read out of the two artifacts in this bundle, not out of the comment that claims it"
b_companion_control = "Solo_BugSeedDoesNotLead.cfg RED: NoUnmanageableCredential — 1 875 228 states, 219 707 distinct, depth 13, 5 s; formal/runs.toml:120. This is the control in the other direction: the companion switch armed alone falls on a DIFFERENT invariant at a state whose snap.sealed is FALSE, so it does not reach this clause and the co-refutation column does not credit it here"
b_mut_row = "Mut_BugBackupSealedNotAGate.cfg RED: ResetNeverWeakensSurvivingState — 25 711 states, 5 834 distinct, depth 8, 1 s; formal/runs.toml:72. Same defect against the six-name mutant invariant set rather than against the parent alone, and the same clause-C falsifying shape. No artifact of it is carried here — the two solo rows above are the ones the localisation argument needs"

# THE COMPANION, derived from the model text rather than asserted.
x_companion_mechanism = "every configuration arming BugBackupSealedNotAGate also arms BugSeedDoesNotLead — SoloClause_ResetKeepsTheBackupSeal.cfg, Solo_BugBackupSealedNotAGate.cfg and Mut_BugBackupSealedNotAGate.cfg, checked by grep over their CONSTANTS blocks — and `companion_bug` at formal/gen-configs.sh:34 is where that pairing is written. WHY IT IS NECESSARY IS DERIVABLE WITHOUT A RUN, and it is derived here instead of quoted: the demoted marker's own disjunct in ResetSweepSecrets is guarded by `SeedLeadsTheWipe => ~store.seed`, so with the shipped seed-lead the marker cannot drop until the flash seed is already gone. The only action that clears store.seed is the seed-delete disjunct in the same phase, and it assigns `snap' = KeepSurv(snap, ram)` — and KeepSurv, at formal/RSKeySecurityState.tla:310-311, sets snap.seed FALSE whenever `ram` is FALSE. `ram` is FALSE from ResetConfirmed onward on every configuration here, because `ram' = IF BugStateResetAfterWipe THEN ram ELSE FALSE` and that switch is off in all three. So on the shipped seed-lead the clause's antecedent is retired by the very step that would let the marker go, and the defect is unfalsifiable alone. THE COMPANION IS THE PRICE OF REACHABILITY AND NOT A SECOND DEFECT"
x_co_refuted_measured = "`co = 1`, and the roadmap's `co = 0` for this clause is REFUTED. Measured by calling `assurance_gate.co_refuted(ROOT)` directly: it returns `['BugBackupSealedNotAGate']` for this invariant. The credit is resolved through `comutate.armed_subject` at scripts/comutate.py:310-334, which accepts a configuration with two armed switches when the second is the first's companion — and its docstring names THIS configuration as the case it was written for, saying that refusing the pair left the clause reading zero on the very commit that drove its code twin. MEASURED TWICE, BECAUSE THE FILE IS BEING EDITED: once against the committed `scripts/assurance_gate.py` and once against the working copy another agent is changing, by loading each module separately over the same tree. The whole derived row is byte-identical under the two — the in-flight edit routes the `mut` column through the same companion rule the `co` column already used, and this clause's number does not move"
x_baseline_red = "and the bundle gate was ALREADY red when this bundle was written, on every one of the eight existing bundles and for a reason none of them owns. `python3 scripts/bundle_gate.py` exits 1 with sixteen findings, two per bundle, all on `result.gate_ledger`: each transcribes a volatile count the token-refinement derivation no longer produces, because another agent's uncommitted `assurance/token_refinement.toml` moved it. This bundle transcribes the derivation as it stands at the moment of writing, which means the same edit landing or being abandoned moves this line too. Recorded so that a reader of a red gate does not attribute it to the arriving bundles. CLOSED on integration: the eight were re-derived from a live `bundle_gate.gate_corpus()` in the same pass that landed these three, and the sixteen findings are gone"

# TIER C.
c_kani = "crates/rsk-fido/src/reset_refinement_kani.rs:162-179 `reset_keeps_the_backup_seal` — `kani::assert(reset.reset_keeps_the_backup_seal(&volatile), \"ResetKeepsTheBackupSeal\")` after one symbolic step, with a cover for its own antecedent. It is the sole reason this row's status reads BOUNDED rather than MODELLED-ONLY, because `status` is rebuilt from the derivation and keys on a harness whose name contains the snake form of the invariant"
c_kani_subsumption = "and its ASSERTION is implied by the parent harness on the tree as it stands: same `well_formed(pre)`, same one step, and the parent asserts `well_formed(post)` whose last conjunct at crates/rsk-fido/src/reset_assurance.rs:267 is the conjunction. What is not implied is the cover, and what the split buys under mutation is in the register below. This bundle states the subsumption rather than presenting a fourth independent proof"
c_alpha_is_not_the_model = "AND THE TWO TIERS DO NOT STATE THE SAME CLAUSE, in a way that belongs to this conjunct alone. The TLA+ clause at formal/RSKeySecurityState.tla:1848-1849 is `(Idle /\\ snap.seen) => ((snap.seed /\\ snap.sealed) => gate.backupSealed)` — no reachability conjunct in the antecedent, because `KeepSurv` retires `snap.seed` when the last copy dies. The Rust α at crates/rsk-fido/src/reset_assurance.rs:122-128 adds `!self.owner_seed_reachable(volatile)` as a fourth disjunct, and `owner_seed_reachable` at :96-98 is a three-way disjunction over `owner_seed`, `owner_locked_seed` and the RAM copy. So the α's antecedent is STRICTLY NARROWER and the α is the weaker statement of the two. Sound in the direction it is used — the model's GREEN carries to the α's — and it means the harness is not a second check of the same sentence. The sibling clauses have the reachability conjunct on BOTH sides, so this asymmetry is this clause's own"
c_coin_flip_control = "THE ONE BEHAVIOURAL TEST FOR THIS CLAUSE IS GREEN ON THE SHIPPED TREE BY CONSTRUCTION, AND THAT IS NOT A GOOD PROPERTY. `a_seed_the_medium_kept_stops_the_wipe_before_the_gates` at crates/rsk-fido/src/reset_tests.rs:1456 asserts the marker is still live after a refused seed removal. MEASURED HERE: sixty fresh processes of the compiled `rsk-fido` test binary, that test and the deterministic one together, sixty green and zero red. The reason it cannot go red on the shipped tree is structural and was read out of the code rather than inferred from the runs — `EF_BACKUP_SEALED` is a gate record, so the secret sweep's predicate at crates/rsk-fido/src/reset.rs:120 excludes it and it can never enter the batch that aborts. On the MUTANT the same test is a coin flip, and the mechanism is end-to-end: `Fs::for_each_key` at crates/rsk-fs/src/fs.rs:393 is a pass-through to the backend, the fixture's backend delegates to a `RamStorage` whose map is a `std::collections::HashMap` at crates/rsk-fs/src/storage.rs:78, and `for_each_key` iterates `self.map.keys()` at crates/rsk-fs/src/storage.rs:105 — so the sweep collects a batch in a fresh permutation per process, and whether the demoted marker is collected before the refused seed fid decides the verdict. THE FRACTION IS INHERITED AND NOT RE-MEASURED: formal/comutants.toml:199 records 233 of 400 fixtures keeping the marker and 167 losing it, which is the ~42% detection rate; this bundle drove neither the mutant nor the four hundred"
c_deterministic_replacement = "the test that IS the witness is `a_backup_seal_swept_with_the_secrets_is_a_reopened_export_window` at crates/rsk-fido/src/reset_assurance_tests.rs:130. It drives the projection, whose `delete` at crates/rsk-fido/src/reset_assurance.rs:160-182 takes its phase from the PRODUCTION `reset_phase` classifier rather than a second hand-written list, and asserts `reset_keeps_the_backup_seal` over a seed still reachable — no map anywhere in the path. Its recorded directions, from formal/comutants.toml:199 and its own doc comment at crates/rsk-fido/src/reset_assurance_tests.rs:110-128: shipped GREEN, this mutant RED five of five, the EF_MINPINLEN control GREEN three of three. Those three figures are the tree's own measurement and are cited as such; what this bundle measured is the shipped-green half, sixty times"

# THE PRODUCTION SIDE.
r_owner = "one tagged site, one deciding site, two consumers, two writers. The tag the column counts is at crates/rsk-fido/src/reset.rs:247; the arm that actually decides the phase is at crates/rsk-fido/src/reset.rs:251; and the property itself lives in the ordering at crates/rsk-fido/src/reset.rs:104-121, which carries no tag at all"
r_gate_reads_backwards = "the marker is one byte — `[1] once the seed has been backed up`, crates/rsk-fido/src/consts.rs:299 — and it is the only gate record in the phase whose ABSENCE is the permissive state for an irreversible disclosure. crates/rsk-fido/src/reset.rs:215-220 puts it in the same class as OATH's access code and records that it sat in the seed's phase until the audit run-36 class sweep moved it. `EF_PAUTHTOKEN` is the record that failed the same rule from the other side and was moved OUT of the gate set, which is the pair worth reading together"
r_fail_closed_reader = "and the runtime reader is fail-closed where a reset is fail-open. `backup_sealed` at crates/rsk-fido/src/vendor.rs:1035 is `try_backup_sealed(fs).unwrap_or(true)`, so a probe the medium could not answer reads as SEALED. That is the same class as the faulted-read defect this tree already fixed elsewhere, and it means no runtime check can substitute for this clause: the reset is the one path that makes the record legitimately absent, so a wipe that drops it early produces exactly the state the fail-closed reader is designed to be unable to distinguish from a device that was never sealed"
r_no_negative_control_for_the_conjunction = "and there is a hole one layer up that this clause inherits. Every use of the parent conjunction in crates/rsk-fido/src/reset_assurance_tests.rs is a positive assertion, and weakening a conjunction can only make a positive assertion easier — so a parent that silently dropped THIS conjunct would be invisible to `cargo test -p rsk-fido`. The three per-clause controls at crates/rsk-fido/src/reset_assurance_tests.rs:46-63 call each clause method directly and never the parent, so they would not see it either. The rung that would is this clause's own Kani harness, because `well_formed` is its precondition"

# THE FUZZ AND BOARD RUNGS.
f_fuzz = "`fuzz = 1` counts fuzz/fuzz_targets/power_cut.rs, whose module doc at :29-31 names this clause among the three. The oracle folds them into one boolean and this clause is its second conjunct, separable by reading and not by the assertion. No log of any run is in the tree and none is in this bundle"
t_hil = "`test = 1` counts tests/29_reset_power_cut.py, and this clause is better served there than either sibling: it has an assertion of its own at `tests/29_reset_power_cut.py:537`, under its own words, where the two siblings share a folded one at `tests/29_reset_power_cut.py:526-527`. No board run is recorded anywhere. PLAT-FLASH-001's discharge is a PASS of exactly that script, its status is `pending`, and `hardware` is zero on the evidence vector"
x_ladder_reading = "so the four rungs are not four checks of one sentence. The model states the clause with a snapshot antecedent and no reachability conjunct; the α adds one and is weaker; the fuzz oracle folds it with its siblings and draws the RAM copy neither of the upper rungs draws; the board script asserts it conditionally and has never run. The ladder is real and every rung is narrower than it looks from the column"

# The gates, each with the number it derives, transcribed from a live run.
gate_ghost = "ghost-gate: ok — 21 action(s) record NoAuthorizationBypass over 24 route(s), consulting 11 guard(s) — unchanged by this bundle, and transcribed because the roster rule demands every gate line whether or not this property moved it. The ghost records a different invariant; no action in this module writes this clause into `viol`, which is correct, because it is a state predicate and not a step one"
gate_ledger = "token-refinement-gate: GREEN keys=2 api=11 volatile=11/10 persistent=14/5 outcomes=7/6 walk=4 softlock=12 window=2 testonly=1 — the ledger owns the pinUvAuthToken axes and no owner of this clause is in it. DERIVED OVER A WORKING TREE CARRYING ANOTHER AGENT'S UNCOMMITTED `assurance/token_refinement.toml`, which is why the volatile pair here differs from the one every existing bundle transcribes and why the gate was red on all of them; RE-DERIVED ON INTEGRATION and unchanged, and the eight were moved up to this reading"
gate_assumption = "assumption-gate: 5 standing assumption(s) AlwaysUvShipped TRUE=5 FALSE=91 ForceChangeModelled TRUE=3 FALSE=93 PowerOnClearsScratch2 TRUE=11 FALSE=4 RekeyOrderModelled TRUE=2 FALSE=13 WidePerms TRUE=3 FALSE=93 — derived over a working tree carrying other agents' uncommitted configurations, so these denominators move with the roster and must be re-derived; `RekeyOrderModelled` is the entry that moved them since. None of the five constants appears in this clause's antecedent or consequent: unlike its sibling, this conjunct is falsifiable on both arms of every one of them, and the two that live in RSKeyBootHardening are assigned by neither configuration that checks this clause"
gate_matrix = "matrix-gate: ok — 40 P0-family properties × 31 build configurations = 1240 cells (37 covered, 143 equivalent, 0 conditional, 106 out-of-scope, 954 gap) — UNCHANGED by this bundle: a model check, a bounded proof and two reviews read no board preset and no flash geometry, so nothing here settles a cell. Two build columns would change what this clause is ABOUT rather than how it is checked, and each has an assumption row instead of a cell"
gate_registry = "assurance-gate: SEC-FIDO-006C ResetKeepsTheBackupSeal BOUNDED cfgs=2 mut=1 co=1 kani=1 fuzz=1 rust=1 test=1 — `co=1` is the column the roadmap records as a zero, and it is the credit resolved through the companion rule; `rust=1` counts a FILE holding one tag; `test=1` counts a FILE that names the invariant and not a board run"

# ---- 6. Raw artifact or log --------------------------------------------------

# FIVE logs, copied from the recorded safety tier and re-hashed after copying.
# `formal/out/` is gitignored and the next run overwrites it, so these are the
# only durable copies of the runs this bundle is about.

[[artifact]]
run = "formal/run-tlc.sh Shipped.cfg — the clause over the shipped constants, GREEN, and the run it held at every reachable state of"
path = "assurance/bundle/logs/SEC-FIDO-006C/tlc-Shipped.log"
bytes = 7332
sha256 = "9adf2af45b85995b91491cca599ead504f65e9f7e38cee3c85d17b088c7ffa37"

[[artifact]]
run = "formal/run-tlc.sh SoloClause_ResetKeepsTheBackupSeal.cfg — this clause alone, RED, and the log IS the eight-state counterexample"
path = "assurance/bundle/logs/SEC-FIDO-006C/tlc-SoloClause_ResetKeepsTheBackupSeal.log"
bytes = 9279
sha256 = "f8047fc39ab30fab15b8fd48338f61a25d56d3224fa4f54f63aaa37866b78d35"

[[artifact]]
run = "formal/run-tlc.sh Solo_BugBackupSealedNotAGate.cfg — the same defect against the PARENT invariant, whose reported name is the umbrella and whose trace is this clause"
path = "assurance/bundle/logs/SEC-FIDO-006C/tlc-Solo_BugBackupSealedNotAGate.log"
bytes = 9279
sha256 = "09bf94ff2396111acf8415fab76a7b98f920fc22674c77e4069b8221ce411ff7"

[[artifact]]
run = "formal/run-tlc.sh Solo_BugResetGatesFirst.cfg — the OTHER switch, the sole model owner of the two sibling clauses, reporting through this one as well"
path = "assurance/bundle/logs/SEC-FIDO-006C/tlc-Solo_BugResetGatesFirst.log"
bytes = 9284
sha256 = "82f6b3ffa5f1553bab8116163a32b3c7400b9dfb7de373df6941b0544923247c"

[[artifact]]
run = "formal/run-tlc.sh Solo_BugSeedDoesNotLead.cfg — the companion armed alone, falling on a different invariant at a state where this clause is not violated; the control that says the companion is not the subject"
path = "assurance/bundle/logs/SEC-FIDO-006C/tlc-Solo_BugSeedDoesNotLead.log"
bytes = 14430
sha256 = "519cc0f9fdbc2ccf2084ed75a0af2c6d4f53a77be2b7096dd3f034d7ca4e8823"

# ---- 7. Assumptions and TCB / trust dependencies -----------------------------

# TWELVE rows. Seven are registered against entries that already exist and each
# names the field an integrator must extend; five need a new `PLAT-SEAL-*` entry,
# and every one of those five is `discharge_owner = "contributor"` — a
# maintainer-owned row would owe a board record for a question no board answers,
# so this bundle leaves the board obligation where it found it. Its size is not
# spelled here: it read `twelve` until `PLAT-TIMER-003` made it thirteen on
# 2026-09-05 and nothing went red. The exact edits are in the platform
# delta accompanying this bundle; `python3 scripts/platform_gate.py` is RED on
# the twelve `slice:AS-SEAL-*` candidates until they are merged.

[[assumption]]
id = "AS-SEAL-1"
statement = "A torn flash write leaves the old record or a detectably bad one, never a plausible wrong one — so a reset prefix is a sequence of completed record transitions, and a half-written EF_BACKUP_SEALED is not a state any tier here has to consider."
kind = "platform"
discharger = "a recorded PASS of tests/29_reset_power_cut.py on a throwaway board with a real supply cut, which is exactly the discharge PLAT-FLASH-001 already names and has never been given"
expressible = "no — a fact about silicon, not a model constant; the model has no partial write at all, it has an ordering over completed ones"
registered = "yes — assurance/platform.toml PLAT-FLASH-001, pending, which already `supports` this property; its `covers` carries `slice:AS-SEAL-1` beside the sibling's `slice:AS-RESET-1` now. Extending `covers` on an existing maintainer row adds no board obligation"

[[assumption]]
id = "AS-SEAL-2"
statement = "TLC is sound for the finite configurations it checks, and the verdict recorded for a configuration is the verdict that run produced."
kind = "tool TCB"
discharger = "a pinned checker and a recorded artifact. tlaplus 1.7.4 is pinned by flake.lock, formal/run-tlc.sh:16 records the jar's sha256 prefix, and the five logs in this bundle carry TLC's own banner and closing sentences beside the runner's numbers. What none of that discharges is the soundness of the checker"
expressible = "no — a fact about a tool, not about the model"
registered = "yes — assurance/platform.toml PLAT-TOOL-004, pending, which already supports this property; its `covers` carries `slice:AS-SEAL-2` beside the sibling slice ids now"

[[assumption]]
id = "AS-SEAL-3"
statement = "Kani/CBMC is sound for the arithmetic this clause's harness bounds, and the solver that ran is the pinned one — which for cargo-kani means a version number in a workflow file and not a hash in flake.lock."
kind = "tool TCB"
discharger = "a pinned toolchain and a recorded tool hash, and the second does not exist — for cargo-kani or for the solver bundle it fetches. The first does: KANI_VERSION 0.67.0, held by scripts/kani_gate.py. cargo-kani is the only tool on this clause's path outside the hermetic shell, and this clause's BOUNDED status rests entirely on one harness that tool ran"
expressible = "no — a fact about a tool, not about the model"
registered = "yes — assurance/platform.toml PLAT-TOOL-003, pending, whose `covers` carries `slice:AS-SEAL-3` now; its `supports` named the parent property and no clause row, and names this property now"

[[assumption]]
id = "AS-SEAL-4"
statement = "The relational pre-state is a GHOST in both tiers — `snap` in the model, `ResetSnapshot` in the Rust α — and no shipped image holds it, so nothing in the firmware can observe or enforce this clause at runtime."
kind = "model fidelity"
discharger = "nothing to run, and that is the content: the write ORDER is the whole implementation. What supports it runs in the other direction and is machine-checked — scripts/check.sh's `assurance-trace image identity` row builds the default image twice, poisoning reset_assurance.rs and reset_refinement_kani.rs before the second, and requires the two loadable images byte-identical. A device that had swept the marker early would answer every command exactly as one that had not"
expressible = "no — the absence of a runtime check is not something a configuration can assign"
registered = "yes — assurance/platform.toml PLAT-MODEL-014, accepted-risk, which already supports this property and carries `slice:AS-RESET-2`, and `slice:AS-SEAL-4` beside it now. Its own `failure_direction` already names the one guard that applies here: the projection's `delete` takes its phase from the production classifier rather than a second list"

[[assumption]]
id = "AS-SEAL-5"
statement = "One `store.seed` boolean stands for two flash records in the model, and this clause's α does NOT make the same collapse — `owner_seed_reachable` disjoins the plain record, the soft lock's wrapped copy and the RAM copy, where the model has one field."
kind = "model fidelity"
discharger = "reading the two apart, and for this clause the gap is wider than for its siblings. crates/rsk-fido/src/reset_assurance.rs:96-98 disjoins three sources; formal/RSKeySecurityState.tla has `store.seed` and `ram`. So the soft-locked device — the wrapped copy present, the plain record gone, which is exactly the state a partly-torn seed loop leaves — is a state the α can distinguish and no configuration can. The clause is about the seed, so this is the collapse that costs it most"
expressible = "no — the model has one field where the code has two, and expressing the alternative needs a second VARIABLE"
registered = "yes — assurance/platform.toml PLAT-MODEL-004, pending, which carries the same collapse for two other properties and covers `slice:AS-RAM-2` and `slice:AS-RESET-12`, and `slice:AS-SEAL-5` beside them now, with this property in `supports`"

[[assumption]]
id = "AS-SEAL-6"
statement = "The Kani induction domain is deliberately WIDER than the firmware: `well_formed` constrains `volatile.owner_seed` only while a reset is in progress, and the harness draws it as a free symbolic boolean beside an equally free persistent seed."
kind = "model fidelity"
discharger = "reading the bridge, and the direction is the whole of it. For the ASSERTION the widening is sound — a proof over a superset of the reachable states holds of the subset. For the `kani::cover!` it is not: this clause's cover conjoins `owner_seed_reachable`, which the widened domain can satisfy from a RAM copy the device could not be holding, so a reachable-antecedent report is not the same claim as a reachable state. Narrowing the domain changes what all four harnesses prove"
expressible = "no — a scope statement about an existing proof, not a constant"
registered = "yes — assurance/platform.toml PLAT-MODEL-007, pending, whose `discharge` already names the four harnesses of this family and whose `covers` carries `slice:AS-RAM-8` and `slice:AS-RESET-8`, and `slice:AS-SEAL-6` beside them now, with this property in `supports`"

[[assumption]]
id = "AS-SEAL-7"
statement = "The gate phase holds FIVE records and this clause names one of them; `EF_DEVICE_PIN` and `EF_MINPINLEN` are swept in the same phase and appear in no clause, so a torn reset that drops either early is unobserved by every configuration, the harness and the twin here."
kind = "model abstraction"
discharger = "the reset argument covers them and no invariant states it: phase 2 cannot begin until phase 1 has provably emptied the store, and the seed leads phase 1. Both halves are already written down — crates/rsk-fido/src/reset.rs:237-243 says the list is five and the clauses are three. What would discharge it is a fourth clause and a fourth `snap` field, which is a model change and not a measurement"
expressible = "no — a missing clause, not a boolean; `ForceChangeModelled` toggles whether a RECORD exists, not whether a clause names it"
registered = "yes — assurance/platform.toml PLAT-MODEL-011, pending, which already supports this property and whose statement's last sentence IS this argument for the reset family; its `covers` carries `slice:AS-F004-3` and `slice:AS-SEAL-7` now"

[[assumption]]
id = "AS-SEAL-8"
statement = "EF_BACKUP_SEALED is ONE BYTE on flash and the model has ONE BOOLEAN for it, so a marker that is present but not readable, or present with a value nobody wrote, is a state no tier of this ladder can express."
kind = "model abstraction"
discharger = "not by a run: it needs either a second model variable or the argument that the byte's VALUE is never read. The second is nearly available and is not quite the same claim — every consumer tests presence rather than content (`try_has_data` at crates/rsk-fido/src/vendor.rs:1045, `has_data` on the display side), and both writers write the same `[1]`. What is left over is the read that FAILS, which crates/rsk-fido/src/vendor.rs:1035 answers `true` for and which the model has no state for at all. AS-SEAL-1 is the row that assumes that state away at the medium; this row is the one that says the model could not describe it if it happened"
expressible = "no — a record with three states where the model has two needs a second variable, not a constant"
registered = "yes — assurance/platform.toml PLAT-SEAL-001, a new entry written by this bundle, `class = \"model-abstraction\"`, `discharge_owner = \"contributor\"`, `status = \"pending\"`"

[[assumption]]
id = "AS-SEAL-9"
statement = "This clause's model verdict and its co-refutation credit both come from a configuration arming TWO switches, so what its own configuration isolates is the CLAUSE and never the defect."
kind = "tool fidelity"
discharger = "a run of the defect armed alone, which no configuration in the tree performs and which the model text says would be GREEN. The mechanism is derived in `result.x_companion_mechanism` from `SeedLeadsTheWipe` and `KeepSurv`, so the pairing is justified rather than assumed — but the justification is an argument about the model and not a recorded verdict, and the derivation that credits `co = 1` accepts the pair on a NAME lookup in formal/gen-configs.sh's `companion_bug`, not on that argument. What would discharge it is a recorded GREEN of the switch alone, which would also make the argument checkable"
expressible = "yes — every switch is a model constant, and the alone-armed configuration is one file"
registered = "yes — assurance/platform.toml PLAT-SEAL-002, a new entry written by this bundle, `class = \"tool-fidelity\"`, `discharge_owner = \"contributor\"`, `status = \"pending\"`"

[[assumption]]
id = "AS-SEAL-10"
statement = "The TLA+ clause and the Rust α do not state the same sentence: the α adds `owner_seed_reachable` to the antecedent, making it strictly weaker, and this asymmetry belongs to this conjunct alone."
kind = "model fidelity"
discharger = "either the conjunct added to the model's clause or the disjunct removed from the α — and the direction says which is safe to leave. The model's clause is the STRONGER of the two, so its GREEN carries to the α and the harness proves less than the configurations do. That is sound for citing them together and unsound for reading the harness as an independent confirmation, which is what a `kani` column of one invites. The sibling clauses carry `SeedReachable` on both sides and have no such gap"
expressible = "yes — it is a conjunct of a definition, so a variant model states the other reading"
registered = "yes — assurance/platform.toml PLAT-SEAL-003, a new entry written by this bundle, `class = \"model-abstraction\"`, `discharge_owner = \"contributor\"`, `status = \"pending\"`"

[[assumption]]
id = "AS-SEAL-11"
statement = "The shipped image is built without `--features fips-profile`. On that build `backup_export` returns NotAllowed before the marker is ever read, so one of this clause's two consumers is gone and the clause guards a surface that column does not have."
kind = "build configuration"
discharger = "the build itself, and the reading is the content rather than a run: crates/rsk-fido/src/vendor.rs:842-845 refuses export under `cfg!(feature = \"fips-profile\")` ahead of the seal check, and the on-device reveal is disabled on that profile too. So on that column a torn reset that drops the marker re-opens nothing over USB — which makes the property WEAKER there, not stronger, and means a GREEN measured on the default column is not evidence about it. No configuration and no harness runs that column"
expressible = "no — the model has one gate boolean and no notion of a consumer at all, so the arm cannot be assigned"
registered = "yes — assurance/platform.toml PLAT-SEAL-004, a new entry written by this bundle, `class = \"build-configuration\"`, `discharge_owner = \"contributor\"`, `status = \"pending\"`"

[[assumption]]
id = "AS-SEAL-12"
statement = "The marker's SECOND consumer — the on-device recovery-phrase reveal — exists only on a display build, so on that column this clause guards two disclosure surfaces and every rung of the ladder checks it as if it guarded one."
kind = "build configuration"
discharger = "the display columns of the matrix, which are not covered for this property. crates/rsk-display/src/backup.rs:47 conjoins `!st.sealed` into `can_reveal`; the model has one `gate.backupSealed` and no consumer; the fuzz oracle tests one record's presence; and the board script drives the host path only. The reveal is the surface where the host is not in the trust path at all, which is exactly the case seed backup exists for, so the uncovered column is not the marginal one"
expressible = "no — a second consumer is an action the model does not have, not a value a constant takes"
registered = "yes — assurance/platform.toml PLAT-SEAL-005, a new entry written by this bundle, `class = \"build-configuration\"`, `discharge_owner = \"contributor\"`, `status = \"pending\"`"

# ---- 8. Mutation verdicts ----------------------------------------------------

# TEN rows and TEN verdicts, none of them `inverse`. THE SPLIT MATTERS AND IS
# STATED RATHER THAN LEFT TO BE COUNTED: six are level B and mutate the MODEL,
# four are level `code` and are about production Rust or the suite that guards
# it, and NOT ONE mutates a gate script. The verdict floor is eight, and a
# bundle can clear it on gate-script mutations that say nothing about the
# firmware; this one does not, and a reader can check that by reading the
# `level` column.

[[mutation]]
level = "B"
mutant = "BugBackupSealedNotAGate with its companion — EF_BACKUP_SEALED moved out of the gate set and back into the secret phase, against this clause alone"
invocation = "formal/run-tlc.sh SoloClause_ResetKeepsTheBackupSeal.cfg"
expected = "RED: ResetKeepsTheBackupSeal"
verdict = "RED: ResetKeepsTheBackupSeal, 23 504 states, 5 359 distinct, depth 8, 1 s"
fell = "ResetKeepsTheBackupSeal, at state 8 — an Idle state reached by ResetAborts with store.seed TRUE, snap.seen TRUE, snap.seed TRUE, snap.sealed TRUE and gate.backupSealed FALSE. The marker went one state earlier, at ResetSweepSecrets"
direction = "modelled"
reading = "the antecedent is TRUE and the consequent FALSE, and the failing state is the run-36 defect exactly: the one-time export window re-opened over a seed the wipe did not manage to destroy. The INVERSE defect — a marker that outlives the seed it gates — makes the consequent TRUE and cannot falsify this implication at all; it would have to be caught by some other invariant, and none of the twelve Shipped.cfg checks is that one. Read out of the artifact in this bundle: the `Error:` line names this clause, and the final state is the one printed above."

[[mutation]]
level = "B"
mutant = "the same switch pair against the PARENT invariant only, with no clause named anywhere in the configuration"
invocation = "formal/run-tlc.sh Solo_BugBackupSealedNotAGate.cfg"
expected = "RED: ResetNeverWeakensSurvivingState, and a trace that cannot say which conjunct"
verdict = "RED: ResetNeverWeakensSurvivingState, 23 258 states, 5 312 distinct, depth 8, 1 s"
fell = "ResetNeverWeakensSurvivingState — the only invariant besides TypeOK this configuration checks; the trace is the same seven-action behaviour and the same final state as the row above"
direction = "modelled"
reading = "the verdict names the umbrella and the trace is this conjunct, which is the whole reason a clause row exists. snap.surv is empty and snap.pin is FALSE in the falsifying state, so both sibling conjuncts are VACUOUSLY TRUE there and only this one is broken. A verdict column at invariant granularity shows none of that, and the column is what every derived page reads."

[[mutation]]
level = "B"
mutant = "BugResetGatesFirst — the two sweeps swapped, which is the sole model owner of the two SIBLING clauses and not of this one — armed alone against the parent"
invocation = "formal/run-tlc.sh Solo_BugResetGatesFirst.cfg"
expected = "RED: ResetNeverWeakensSurvivingState, and, if the localisation claim is true, a trace that is this clause and not either sibling"
verdict = "RED: ResetNeverWeakensSurvivingState, 23 327 states, 5 331 distinct, depth 8, 1 s"
fell = "ResetNeverWeakensSurvivingState, at an Idle state after ResetSweepGates and ResetAborts with snap.surv empty, snap.pin FALSE, snap.seed TRUE, snap.sealed TRUE and gate.backupSealed FALSE — this clause, on a switch that owns the other two"
direction = "modelled"
reading = "A SECOND DEFECT, A DIFFERENT ROUTE, AND THE SAME CONJUNCT REPORTED. This switch reaches the failing state through ResetSweepGates where the row above reaches it through ResetSweepSecrets, and the search still arrives here first because this conjunct falls at depth 8 where its siblings need 16 and 18. That is the measurement behind the clause split, and it is read here out of two artifacts rather than taken from the comment that asserts it."

[[mutation]]
level = "B"
mutant = "BugSeedDoesNotLead armed alone — the companion, run as a control to see whether it reaches this clause on its own"
invocation = "formal/run-tlc.sh Solo_BugSeedDoesNotLead.cfg"
expected = "RED, and on a DIFFERENT invariant if the companion is a reachability aid rather than a second defect against this clause"
verdict = "RED: NoUnmanageableCredential, 1 875 228 states, 219 707 distinct, depth 13, 5 s"
fell = "NoUnmanageableCredential, at a thirteen-state behaviour ending Idle with store.cred = {r1}, store.rpent empty and snap.sealed FALSE"
direction = "modelled"
reading = "THE CONTROL PASSES AND IT IS WHAT MAKES THE PAIR HONEST. snap.sealed is FALSE in this falsifying state, so this clause is not violated there and could not be: the behaviour never runs BackupFinalize. So the companion breaks a credential-management invariant on its own and reaches this clause only in combination — which is precisely the shape `companion_bug` is for, and it is why the co-refutation column credits `BugBackupSealedNotAGate` here and not the companion."

[[mutation]]
level = "B"
mutant = "the same switch pair against the six-name mutant invariant set rather than against the parent alone"
invocation = "formal/run-tlc.sh Mut_BugBackupSealedNotAGate.cfg"
expected = "RED: ResetNeverWeakensSurvivingState, and no OTHER invariant of the six reported first"
verdict = "RED: ResetNeverWeakensSurvivingState, 25 711 states, 5 834 distinct, depth 8, 1 s"
fell = "ResetNeverWeakensSurvivingState, on the same final state shape as its solo sibling and at the same depth"
direction = "modelled"
reading = "the row exists to say that widening the checked set does not move the reported name: with six invariants live the defect still surfaces as the reset conjunction and still through this conjunct. It also bounds the other direction — none of the five neighbours is broken by moving this marker between phases, so the defect is local to the property this bundle is about. No artifact of this run is carried; its numbers are formal/runs.toml:72."

[[mutation]]
level = "B"
mutant = "BugBackupSealedNotAGate armed ALONE, without its companion — the configuration the tree does not contain"
invocation = "argued from the model text; no configuration in formal/ arms this switch without BugSeedDoesNotLead, and no TLC was run for this bundle"
expected = "GREEN, if the shipped seed-lead really does make the defect unreachable"
verdict = "NOT DRIVEN, and labelled as such. The argument is the one in result.x_companion_mechanism and it is a derivation rather than a guess: the demoted marker's disjunct is guarded by `SeedLeadsTheWipe => ~store.seed`, the only clearer of store.seed assigns `snap' = KeepSurv(snap, ram)`, KeepSurv sets snap.seed FALSE when ram is FALSE, and ram is FALSE from ResetConfirmed onward on every configuration here"
fell = "nothing would fall, on that argument: the antecedent `snap.seed /\\ snap.sealed` is retired by the very step that permits the marker to drop, so the implication holds vacuously"
direction = "modelled"
reading = "recorded as an ARGUED row and labelled as one, because the alternative is to inherit the claim from `comutate.armed_subject`'s docstring. What it buys is that the companion pairing is justified from the model rather than from a name lookup — and what it does not buy is a verdict, which is why AS-SEAL-9 exists and stays open. The direction is the same as every other row here: the argued outcome describes the defect being unreachable, not its inverse being invisible."

[[mutation]]
level = "code"
mutant = "the same defect in production Rust: `| EF_BACKUP_SEALED` dropped from the match arm at crates/rsk-fido/src/reset.rs:251"
invocation = "python3 scripts/comutate.py run BugBackupSealedNotAGate, in the throwaway worktree that file's own header describes"
expected = "killed by `cargo test -p rsk-fido`"
verdict = "killed — `expect = \"killed\"` at formal/comutants.toml:188-199 is a RECORDED verdict per that file's status vocabulary at formal/comutants.toml:19-21, re-driven by the weekly `comutate run` and by no row of scripts/check.sh. NOT re-driven by this bundle: the patch touches a file three agents are working in, and a transient edit to it was judged worse than an inherited verdict clearly labelled"
fell = "three tests: the_gate_set_defers_every_record_whose_absence_is_permissive at crates/rsk-fido/src/reset_tests.rs:638, reset_projection_finishes_only_after_every_ordered_phase at crates/rsk-fido/src/reset_assurance_tests.rs:76, and a_backup_seal_swept_with_the_secrets_is_a_reopened_export_window at crates/rsk-fido/src/reset_assurance_tests.rs:130"
direction = "modelled"
reading = "the direction is the same at both levels — a marker removed while the seed it gates is still reachable — and the row's OWN note is the reason it cannot be read as three independent kills. Two of the three transcribe the very match arm the patch edits, so on their own they answer whether somebody edited the list. The third is the one that states the property, and the next two rows are what separates them."

[[mutation]]
level = "code"
mutant = "the CONTROL for the two transcribers: EF_MINPINLEN demoted out of the same match arm instead — a record crates/rsk-fido/src/reset.rs:237-243 says out loud is in the phase and in NO clause"
invocation = "python3 scripts/comutate.py run, with the control patch in place of the registered one"
expected = "if the two transcribers were about the defect they would stay GREEN, because this edit breaks nothing this clause is about"
verdict = "GREEN three of three, per formal/comutants.toml:199 — meaning the slice was killed identically by the control. INHERITED, not re-measured here, and the figure is the tree's own"
fell = "the same two transcribers fell on the control as on the real defect, which is the finding: they cannot tell this defect from an edit that changes no clause"
direction = "modelled"
reading = "A KILL FOR THE WRONG REASON, MEASURED AND RECORDED BY THE TREE ITSELF. The control is what turns two green-looking kills into evidence about a list rather than about a property, and it is the reason this bundle's `bound_deterministic_killers` is one and not three. Read in the other direction it is also reassuring: the control does not fail the deterministic test, so that test is not a third transcriber."

[[mutation]]
level = "code"
mutant = "no mutant — the shipped tree, run sixty times, to find out what the ONE behavioural test for this clause is actually detecting"
invocation = "the compiled rsk-fido test binary, sixty fresh processes, `--exact a_seed_the_medium_kept_stops_the_wipe_before_the_gates a_backup_seal_swept_with_the_secrets_is_a_reopened_export_window`"
expected = "if the behavioural test's green were evidence about the defect it would be sensitive to something; if it is structural it will be sixty green"
verdict = "sixty passes, zero failures — measured for this bundle, not inherited"
fell = "nothing, and the reason it CANNOT fall on the shipped tree was read out of the code: EF_BACKUP_SEALED is a gate record there, so the secret sweep's predicate at crates/rsk-fido/src/reset.rs:120 excludes it and it never enters the batch that aborts on the refused seed fid"
direction = "modelled"
reading = "THIS IS A NEGATIVE RESULT AND IT IS THE POINT. A test that is green by construction on the tree it guards tells a reader nothing, and this one looks like a behavioural witness. On the mutant it is a coin flip — the tree records 233 of 400 fixtures keeping the marker and 167 losing it, about 42% detection — because the sweep collects its batch in HashMap order and the order is a fresh permutation per process. The mechanism was verified end to end here by reading crates/rsk-fs/src/fs.rs:393, crates/rsk-fs/src/storage.rs:78 and crates/rsk-fs/src/storage.rs:105; the 42% itself is the tree's measurement and is cited, not claimed."

[[mutation]]
level = "code"
mutant = "the parent conjunction losing THIS conjunct — `reset_never_weakens_surviving_state` returning the two sibling clauses only"
invocation = "argued from the call sites rather than driven; every use is enumerated by grepping crates/ for the conjunction and for `well_formed`"
expected = "invisible to `cargo test -p rsk-fido`, and caught only through this clause's Kani precondition"
verdict = "NOT DRIVEN, and the argument is monotone rather than a guess: every use of the conjunction in crates/rsk-fido/src/reset_assurance_tests.rs is a positive assertion, and weakening a conjunction can only make a positive assertion easier"
fell = "nothing would fall in the unit suite. This clause's harness would, because `well_formed` is its PRECONDITION at crates/rsk-fido/src/reset_refinement_kani.rs:165 — a weakened precondition admits pre-states violating the dropped conjunct while the assertion still names it directly"
direction = "modelled"
reading = "recorded as an argued row and labelled as one. It is the answer to the subsumption note in the method group: on the tree as it stands this clause's harness proves nothing the parent's does not, and under THIS mutation it is the only rung that would notice — the parent harness asserts `well_formed(post)`, which weakens with the definition, so it would stay green too. That is what the clause split buys at tier C, and it is the same thing the clause-isolating configuration buys at tier B."

# ---- 9. Timestamp, freshness and revalidation inputs -------------------------

[freshness]
measured = "2026-08-31, Europe/Moscow"
expires_on_prover_bump = "tlaplus 1.7.4 or its jar hash for all five recorded verdicts, and cargo-kani 0.67.0 for the one harness this row's BOUNDED status rests on — the second has no hash to expire against, which AS-SEAL-3 is the row about"
expires_on_toolchain_bump = "rustc/cargo 1.96.0 — the co-refutation slice and the sixty-process control measurement only; no model verdict here reads a compiler"
expires_on_stepping = "nothing here is a hardware result and none is claimed. tests/29_reset_power_cut.py has never been recorded, `hardware` is zero on the evidence vector, and a silicon change revalidates PLAT-FLASH-001 rather than this bundle"
expires_on_owner_change = "crates/rsk-fido/src/reset.rs's ordering block and its gate-record match arm, crates/rsk-fido/src/reset_assurance.rs's clause method and its `owner_seed_reachable`, the two consumers at crates/rsk-fido/src/vendor.rs and crates/rsk-display/src/backup.rs, and formal/RSKeySecurityState.tla's ResetConfirmed, ResetSweepSecrets, ResetSweepGates or KeepSurv"
expires_on_model_change = "the clause at formal/RSKeySecurityState.tla:1848-1849 gaining or losing a conjunct, `KeepSurv` changing which snapshot fields it retires, `SealedIsAGate` or `SealedIsASecret` moving, `companion_bug` losing its BugBackupSealedNotAGate arm, or any configuration's INVARIANTS block gaining or dropping this clause's name — the last of which moves the reading and not the evidence"
expires_on_schema_change = "assurance/properties.toml's field set or its `clause_of` relation, formal/comutants.toml's status vocabulary, formal/floors.txt's verdict column, and `comutate.armed_subject`'s companion rule, which is what makes this row's `co` column a one"

# ---- 10. Measured costs ------------------------------------------------------

# THE RUNNER SECONDS ARE MEASUREMENTS AND THE PEAK IS A CEILING, NOT AN RSS.
# Each file row's `runner_seconds` is formal/run-tlc.sh's own timer as
# formal/runs.toml records it for the safety tier at `b185fc3`, with TLC's own
# closing sentence in the same artifact. `peak_memory_mb` is 3705 on every TLC
# row and it is DERIVED FROM THE ARTIFACT IN THAT ROW rather than transferred
# from another bundle: each of the five logs states `3641MB heap and 64MB
# offheap` in its banner, and 3705 is that ceiling. It BOUNDS the process; it is
# not what the process used, and run-tlc.sh records no RSS.
#
# THE HUMAN COLUMN IS A RECONSTRUCTION AND NO TIMER PRODUCED ANY OF IT. The
# seven rows below total 214 human_minutes of attended time reconstructed after
# the fact, inside a session whose wall clock was not recorded. Every `basis`
# says so in its own row rather than leaving a reader to find this paragraph.
# They are kept because they are the only cost signal the programme has.

[[cost]]
artifact = "assurance/bundle/logs/SEC-FIDO-006C/tlc-Shipped.log"
human_minutes = 8
runner_seconds = 1984
peak_memory_mb = 3705
basis = "runner is run-tlc.sh's own timer as formal/runs.toml:64 records it; TLC's closing sentence in this same log says 33min 02s, the difference being JVM start. PEAK IS A CEILING READ OUT OF THIS LOG'S OWN BANNER — 3641MB heap plus 64MB offheap — and not a measured resident set; run-tlc.sh takes no RSS and this bundle ran no TLC. human is attended time RECONSTRUCTED after the fact, not measured, and carries reading the invariants block against this run"

[[cost]]
artifact = "assurance/bundle/logs/SEC-FIDO-006C/tlc-SoloClause_ResetKeepsTheBackupSeal.log"
human_minutes = 26
runner_seconds = 1
peak_memory_mb = 3705
basis = "runner from formal/runs.toml:138, where TLC's own sentence rounds to under a second. peak is this log's own banner ceiling, in the same sense as the row above, and is loose by orders of magnitude for a run this size — which is what a ceiling costs. human is attended time RECONSTRUCTED after the fact, not measured, and is the largest of the five log rows because reading the eight-state counterexample state by state, and settling that the two sibling conjuncts are vacuous in the final state rather than merely unreported, is where the direction of this RED was established"

[[cost]]
artifact = "assurance/bundle/logs/SEC-FIDO-006C/tlc-Solo_BugBackupSealedNotAGate.log"
human_minutes = 9
runner_seconds = 1
peak_memory_mb = 3705
basis = "runner from formal/runs.toml:103. peak is this log's banner ceiling and not an RSS. human is attended time RECONSTRUCTED after the fact, not measured — it carries comparing this trace against the clause-isolating one and confirming they are the same behaviour under two reported names"

[[cost]]
artifact = "assurance/bundle/logs/SEC-FIDO-006C/tlc-Solo_BugResetGatesFirst.log"
human_minutes = 12
runner_seconds = 1
peak_memory_mb = 3705
basis = "runner from formal/runs.toml:119. peak is this log's banner ceiling and not an RSS. human is attended time RECONSTRUCTED after the fact, not measured, and is high relative to a one-second run because this is the artifact that carries the localisation finding: a second, unrelated defect reaching the same conjunct by a different route"

[[cost]]
artifact = "assurance/bundle/logs/SEC-FIDO-006C/tlc-Solo_BugSeedDoesNotLead.log"
human_minutes = 7
runner_seconds = 5
peak_memory_mb = 3705
basis = "runner from formal/runs.toml:120, with TLC's own `Finished in 04s` in the log. peak is this log's banner ceiling and not an RSS. human is attended time RECONSTRUCTED after the fact, not measured — it is the control row, and what it cost was checking that its falsifying state has snap.sealed FALSE"

[[cost]]
artifact = "the analytical half — the property, method and assumption groups, which produced no run of their own"
human_minutes = 118
runner_seconds = 0
peak_memory_mb = 0
basis = "the human figure is attended time RECONSTRUCTED after the fact and never measured, over the model's reset actions and its KeepSurv/SeedLeadsTheWipe guards, the production classifier and its two consumers in two crates, the Rust projection and its harness, the fuzz oracle, the board script, the threat clause and its registry indirection, and twelve platform proposals. It is the largest single figure here and it is NOT a measurement taken by any process; the two zeros are honest, because nothing ran"

[[cost]]
artifact = "the derivations and the one control this bundle actually drove"
human_minutes = 34
runner_seconds = 122
peak_memory_mb = 3705
basis = "runner is WALL CLOCK SUMMED OVER COMMANDS THAT RAN, and it is a LOWER BOUND because the individual calls were not timed apart: the assurance-gate vector twice, the co-refutation derivation under two versions of scripts/assurance_gate.py, the five gate lines twice, one cargo build of the rsk-fido test target, and sixty invocations of the compiled binary. peak is the same banner ceiling as the TLC rows and does not describe the cargo or python processes, which were not measured at all — a figure that bounds a Java heap says nothing about a linker. human is attended time RECONSTRUCTED after the fact, not measured, over the platform delta and the four findings in the header"
