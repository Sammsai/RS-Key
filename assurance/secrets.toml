# SPDX-License-Identifier: AGPL-3.0-only
# Copyright (C) 2026 RS-Key contributors
#
# Which secrets this device holds in RAM, and which of a request's exits each one
# is wiped on. `docs/threat-model.md`'s Zeroization clause has claimed since it
# was written that key-grade material is wiped "when its use ends: session state
# and PIN/UV tokens on drop, transient key copies at end of scope including error
# paths, and the transport/exchange buffers as soon as a message completes". No
# inventory stood behind that sentence. There was no list of which secrets it is
# about, no statement of which exit each wipe is on, and no record at all of the
# one exit where nothing runs: this firmware links `panic-halt`, so a panic spins
# with no unwinding, no destructor and every secret in the frame resident.
#
# NOT a roster. `scripts/secrets_gate.py` derives which sources carry key-grade
# material, how many wipes each has, how many of those sit below an early exit of
# their own function, which types wipe themselves on drop, whether the source can
# observe a cancel at all, and which scrubs the reboot path calls. What is stored
# here is only what a script cannot decide: what the secret IS, which exits its
# wipe is on, and why the ones it is not on are allowed to be.
#
# SCOPE is the device image — `crates/` and `firmware/`. `tools/` is out because
# the exits below are the device's: a halt that runs no destructor, a reboot that
# clears SRAM, a dispatch a cancel cannot unwind. A host process has none of
# those, and a row for one would answer questions that are not about it. The
# exclusion is stated rather than silent: `out_of_scope_sites` is derived too, so
# a new host secret shows up as a number that moved.
#
# EXITS. Four are answered per row; the fifth is derived once for all of them.
#
#   success   the request completed and the answer went out
#   error     the request returned an error, including every `?` on the way
#   cancel    the entry was declined or a CTAPHID_CANCEL arrived. Only the
#             sources that take a PIN entry or read a cancel frame observe one —
#             the worker's dispatch is synchronous, and a cancel sets a flag
#             rather than unwinding an applet — so `n/a` here is DERIVED, and a
#             source that starts taking a PIN entry stops being allowed to say it
#   reboot    the secure-reboot / BOOTSEL drop, where `worker::reboot` runs its
#             scrubs and then the platform clears SRAM
#   panic     NOT a field. It is one fact about every row at once and is derived
#             from the panic strategy; `RES-PANIC-HALT` below is what it owes
#
# ANSWERS.
#
#   explicit   a `.zeroize()` in this source runs on that exit. On `error` and
#              `cancel` this is only allowed where NO wipe sits below an early
#              exit of its own function, which is counted per row
#   on-drop    a type defined here wipes itself when it drops, so the exit is
#              covered by construction. Same restriction on the two early exits
#   partial    at least one of the source's wipes sits BELOW an early exit of its
#              own function, so that exit is not covered. The count is in
#              `skippable`, and the row owes `RES-ERROR-EARLY-EXIT`
#   wiper      `worker::reboot` calls a scrub that reaches this secret. Which
#              scrubs those are is derived from its body into `reboot_wipers`;
#              the row NAMES the one it means in `wiper` and repeats it in the
#              reason, so deleting that scrub reddens this row by name
#   platform   nothing in this source runs on the reboot; what covers it is the
#              SRAM clear, registered as `PLAT-MEM-001` and DISCHARGED on a
#              board — the platform's clear, never a wipe of ours, and only for
#              the silicon and boot configuration it names — `RES-SRAM-CLEAR`
#   not-wiped  the secret is resident on that exit. A finding, or a residual
#   n/a        the exit cannot occur for this secret
#
# `sites`, `skippable`, `wraps` and `bearers` are DERIVED and held against the
# tree in both directions. Nothing here is read for truth: a wipe deleted from a source
# moves its count and reddens its row, and a source that starts holding a secret
# arrives with no row at all. A hand-written count in a register is the defect
# this programme keeps finding — `assurance/crates.toml`'s header records two
# roadmap drafts enumerating crates from memory and missing four.
#
# What no rule here can say. First, and bluntly: this is a roster of WIPES, not
# of secrets. A source earns a row by wiping, wrapping, or defining a self-wiping
# type, so a source that unseals a key and never touches it again has no row and
# owes no answer — the shape a secret inventory would most like to catch is the
# one the derivation is blind to. Second, whether a wipe is REACHED: `skippable`
# is syntax, not control flow — `clientpin`'s `perform_builtin_uv` returns on
# four entry outcomes without touching `pin`, and the caller that owns that
# buffer wipes it on all four — so the number bounds the question rather than
# answering it. Third, whether the compiler kept the write: `RES-COMPILER-ELIDES`
# carries that half, which is a disassembly question and not a source-reading one.

# The threat clause the whole register sources to. It served no registered
# property when this file was written and it still serves none — the threat
# gate's tally counts `assurance/properties.toml` rows, and these are not
# properties. What has changed is that the clause is no longer behind nothing:
# every row below is an answer to one of its sentences, and the gate holds that
# the clause is really declared and is really a defence.
clause = "docs/threat-model.md#TM-ZEROIZATION"

# DERIVED from `worker::reboot`'s body, in call order. A wipe dropped from the
# reboot path is then a finding rather than an edit — which is not hypothetical:
# audit run-34 #23 is the last time one was missing from it.
reboot_wipers = [
    "ctap.scrub_secrets",
    "ccid.scrub",
    "rng.borrow_mut().scrub",
    "otp_kbd::scrub",
    "core1::scrub",
]

# DERIVED over `tools/`, so the scope note above cannot quietly stop being true.
out_of_scope_sites = 11

# ---------------------------------------------------------------------------
# Residuals
# ---------------------------------------------------------------------------

[[residual]]
id = "RES-PANIC-HALT"
applies = "register"
paths = ["panic"]
what = """
Every secret live in a frame at the moment of a panic: PIN buffers, unsealed \
private keys, the device seed, the DRBG state, and both fused keys if the panic \
lands inside the one operation that reads them."""
why = """
`firmware/Cargo.toml` links `panic-halt` and `firmware/src` defines no \
`#[panic_handler]` of its own, so a panic spins in place: nothing unwinds, no \
`Drop` runs, and no explicit wipe below the panic point runs either. Every \
`on-drop` and `explicit` answer in this file is therefore an answer about the \
four other exits and none of them is about this one. Halting is the right \
behaviour — a `no_std` unwinder is not on the table, and a handler that walked \
RAM looking for secrets would be a larger attack surface than the one it \
closes — but the threat model stated the wipes without stating this exception, \
and an unstated exception is the shape this programme was set up to find. What \
bounds the exposure is that a panic is not a host-reachable state: the fuzz \
targets exist to keep it that way, and `docs/threat-model.md` already treats a \
panic-halt as a denial of service rather than a disclosure. What it does not \
bound is a panic reached with a key unsealed, where the secret stays in SRAM \
until the next power cycle."""
revalidate = """
A `#[panic_handler]` appearing under `firmware/src`, or `panic-halt` leaving \
`firmware/Cargo.toml` — the gate derives both and goes red on either, because a \
handler that could wipe makes every row's exits worth re-reading. Also: any \
change that makes a panic reachable from unauthenticated input, which would \
move this from a residual to a finding."""

[[residual]]
id = "RES-COMPILER-ELIDES"
applies = "register"
paths = ["success", "error", "cancel"]
what = """
Whatever the optimiser deleted. Every `explicit` and `on-drop` answer here is \
read out of the SOURCE; none of it is read out of the ELF."""
why = """
`zeroize` writes through `write_volatile` and fences after it, which is what \
makes the crate's wipes survive optimisation, so the ordinary case is sound by \
construction. What is not covered is the copies the compiler made and this \
register never names: a spilled register, an inlined `memcpy` staging buffer, a \
return slot. Those are unnameable in source, and the only instrument that could \
see them is a disassembly of the shipped image."""
revalidate = """
A toolchain bump, an opt-level or LTO change in `firmware/Cargo.toml`'s release \
profile, or a switch to a non-volatile wipe. `scripts/elf_gate.py` is where an \
image-level check would live if one is written; nothing reads the ELF for this \
today."""

[[residual]]
id = "RES-ERROR-EARLY-EXIT"
applies = "rows"
paths = ["error", "cancel"]
what = """
The wipes that sit below an early exit of their own function — 302 of the 447 \
in scope, spread over 43 of the 57 sources. On a `?` that fires above one, that \
wipe does not run."""
why = """
The threat model says transient key copies are wiped at end of scope "including \
error paths", and that sentence was never held against anything. It is not \
refuted by the count either: the number is SYNTAX, and three shapes make it \
overcount. The `?` can be in a branch the wipe is not under; the buffer can be \
owned by a caller that wipes it whatever this function returns \
(`clientpin`'s `perform_builtin_uv` returns on four entry outcomes without \
touching `pin`, and `builtin_uv` above it wipes on all four); and a value \
carried in a `Zeroizing` or a self-wiping type is wiped by the early return \
itself. What the count does establish is that the claim is not free — it holds \
per statement, and no artifact in this tree has yet checked it per statement."""
revalidate = """
The count moving in either direction is held by the gate per row. Closing this \
means a per-statement analysis — reachability of each wipe from each early exit \
— which is stage 9D's open half and is not attempted here."""

[[residual]]
id = "RES-SRAM-CLEAR"
applies = "rows"
paths = ["reboot"]
what = """
Every secret still in a stack frame, a static, or a heap allocation when the \
device reboots or drops to BOOTSEL, on all the rows whose reboot answer is \
`platform` rather than a named scrub."""
why = """
`worker::reboot` wipes the live transports, the FIDO state, the DRBG, the OTP \
keyboard buffers and core1's mailbox, and then nothing in software touches the \
stacks — which carry the deepest secrets, RSA primes and EC scalars. What is \
supposed to cover them is the platform clearing main SRAM across the drop, and \
that is registered as `PLAT-MEM-001`, whose status is now **discharged**: the \
2026-09-03 run of `tests/54_sram_residue.py` is on the record with the sha256 of \
the image it ran on, and it is read against an `expected` committed before the \
board was powered. So the reboot answer for 48 of the 57 rows rests on a \
measurement rather than on an open assumption — and on a PLATFORM measurement, \
which is the whole of what these rows may claim from it: the silicon cleared \
that memory, `worker::reboot` did not, and a dump that is zero everywhere cannot \
tell those two apart. The `control` half is what was taken; the `residue` half, \
which is the one that would measure a scrub, is unsatisfiable on a board that \
clears SRAM and is owed on a configuration that does not."""
revalidate = """
`PLAT-MEM-001` moving again — the discharging run has happened and turned this \
from an open assumption into a platform measurement, and a refuting one would \
turn 47 rows from `platform` into `not-wiped`. Also an RP2350 stepping other than A4, \
secure boot turned on, any change to the boot configuration, or a reboot route \
other than `reset_to_usb_boot` / `SCB::sys_reset`: each invalidates the run that \
discharged it. This is the register that names which rows depend on it."""

[[residual]]
id = "RES-LIBRARY-TEMPORARIES"
applies = "rows"
paths = ["success", "error", "cancel"]
what = """
The copies inside dependencies that no wipe in this tree can reach: `Copy` \
temporaries in the RustCrypto curve arithmetic, digest internals, and the heap \
limbs `num-bigint-dig` allocates for itself."""
why = """
The threat model accepts these three by name and this register does not \
re-litigate them; what it adds is WHICH rows they are about, which was not \
recorded anywhere. `num-bigint-dig` has no scrubbing `Drop` of its own — every \
value `rsk-rsa` owns rides in a `Zeroizing`, and a temporary the library \
allocates internally does not — and it is the only allocating dependency the \
device links, so it is also the only place an allocator can hand a freed secret \
to the next caller. Short-lived, library-internal, not wipeable without forking \
the crates."""
revalidate = """
A new dependency doing key-grade arithmetic, a RustCrypto or `num-bigint-dig` \
major bump, or any of the three gaining a zeroizing `Drop` upstream — the last \
would let a row upgrade its answer instead of citing this."""

[[residual]]
id = "RES-CORE1-SIEVE"
applies = "rows"
paths = ["reboot"]
what = """
`CORE1_SIEVE`, the static small-prime sieve core1 owns, when core1 is wedged. \
It carries the candidate stream of a live RSA keygen."""
why = """
The two sieves are single-core-exclusive by construction — each `&mut` lives on \
exactly one core — so `core1::scrub` cannot clear core1's from core0 without \
aliasing a live reference across cores. It asks core1 to wind down and waits, \
bounded; a core1 that never answers is faulted, which is what latches \
`DEGRADED`, and its sieve stays resident. Audit run-34 #23 is where this was \
found and the bounded wait is the fix that came out of it. The residue is a \
sieve state, not a prime: the mailbox's found primes and the job seed ARE \
cleared from core0, which is what `scrub_found` and `scrub_job` are for."""
revalidate = """
Any change to the sieve ownership rule in `firmware/src/core1.rs`, to \
`SCRUB_WAIT_ITERS`, or to what latches `DEGRADED`. Also `RES-SRAM-CLEAR`'s own \
trigger: the SRAM clear is what covers this residue today, so a stepping that \
stops clearing SRAM turns it from a window into a persistent one."""

# ---------------------------------------------------------------------------
# The secrets
# ---------------------------------------------------------------------------

[[secret]]
file = "crates/rsk-crypto/src/drbg.rs"
secret = "the HMAC-DRBG key and V — the keystream behind every nonce, blinding factor and generated key on the device"
bearers = ["HmacDrbg"]
sites = 4
skippable = 0
wraps = 0
success = "explicit"
error = "on-drop"
cancel = "n/a"
reboot = "wiper"
wiper = "rng.borrow_mut().scrub"
residual = []
why = "`scrub` and the `Drop` impl wipe the same two fields, so a DRBG that goes out of scope on any non-panic path takes its state with it. The reboot path reaches it through `rng.borrow_mut().scrub`, which only destroys the current session's keystream — the next boot reseeds from the TRNG."

[[secret]]
file = "crates/rsk-crypto/src/kdf.rs"
secret = "the PIN-derived verifier and session keys, and the device-key AEAD material"
bearers = []
sites = 8
skippable = 2
wraps = 3
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR", "RES-LIBRARY-TEMPORARIES"]
why = "The five derivation methods are infallible — each hashes into a local and wipes it on the one tail it has — and the two counted wipes are in the AEAD wrappers, below the `?` that rejects a bad tag. The `Zeroizing` mentions here wrap ONE value, the fused-key read, and not the derivations: an earlier draft of this row credited them with covering the early returns and that was wrong. The digest internals under every one of these are the threat model's own accepted residual."

[[secret]]
file = "crates/rsk-crypto/src/pinproto.rs"
secret = "the CTAP2 PIN/UV-auth ECDH shared secret"
bearers = []
sites = 1
skippable = 1
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR", "RES-LIBRARY-TEMPORARIES"]
why = "One wipe, on the agreed point after the KDF has consumed it, and it is below the `?` that rejects a malformed platform key. The curve arithmetic that produced the point is RustCrypto's and its temporaries are unreachable from here."

[[secret]]
file = "crates/rsk-device/src/ccid.rs"
secret = "the CCID response buffer, which holds a deciphered session key or an unwrapped PIN after a dispatch"
bearers = []
sites = 1
skippable = 0
wraps = 0
success = "explicit"
error = "explicit"
cancel = "n/a"
reboot = "wiper"
wiper = "ccid.scrub"
residual = []
why = "One buffer, one wipe, and the worker calls it after every hand-off rather than at an exit inside the dispatch — so success and error are the same statement. The reboot path calls the same `ccid.scrub`."

[[secret]]
file = "crates/rsk-device/src/ctap.rs"
secret = "the CTAP response buffer, which holds a pinUvAuthToken or an hmac-secret output after a dispatch"
bearers = []
sites = 2
skippable = 0
wraps = 0
success = "explicit"
error = "explicit"
cancel = "n/a"
reboot = "wiper"
wiper = "ctap.scrub_secrets"
residual = []
why = "Same shape as the CCID buffer beside it: the wipe is the worker's post-dispatch call, not an exit inside the handler. The reboot path uses the wider `ctap.scrub_secrets`, which also resets the cross-message FIDO state."

[[secret]]
file = "crates/rsk-display/src/backup.rs"
secret = "the SLIP-39 recovery phrase and share words while they are on the trusted display"
bearers = []
sites = 6
skippable = 6
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR"]
why = "The reveal screens hold the recovery words while the user copies them down, which is the longest a plaintext seed is on the display. Every wipe sits below an early exit of its own flow, because the screens are a chain of fallible store reads with the words already decoded."

[[secret]]
file = "crates/rsk-display/src/gates.rs"
secret = "the local PIN as the unlock and first-run gates collect it"
bearers = []
sites = 1
skippable = 1
wraps = 0
success = "explicit"
error = "partial"
cancel = "partial"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR"]
why = "One buffer, wiped after the gate decides — and the wipe is below the early return the refusal path takes, so an entry that is rejected before the decision leaves it. Cancel leaves through the same return an error does: the pad's outcome is a value here, not an unwind."

[[secret]]
file = "crates/rsk-display/src/pin.rs"
secret = "the PIN buffers of the on-screen pad: entry, confirmation, and the PIV reference being set or unblocked"
bearers = []
sites = 18
skippable = 11
wraps = 0
success = "explicit"
error = "partial"
cancel = "partial"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR"]
why = "Eighteen buffers across entry, confirmation and the PIV set/change/unblock flows, and 11 of the 18 sit below an early exit — the flows are chains of fallible store work with a PIN already typed. A cancelled entry leaves through the same returns an error does."

[[secret]]
file = "crates/rsk-ec/src/key.rs"
secret = "the EC private scalar both card applets hold — P-256/384/521, brainpool, k256, Ed25519 and X25519"
bearers = ["PrivKey"]
sites = 14
skippable = 3
wraps = 15
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR", "RES-LIBRARY-TEMPORARIES"]
why = "`PrivKey`'s `Drop` wipes the scalar of whichever variant it is, so the key itself leaves on every non-panic path; the three counted wipes are the raw import and agreement buffers around it, which are not inside the type. The RustCrypto curve temporaries under the operations are the threat model's accepted residual."

[[secret]]
file = "crates/rsk-fido/src/clientpin.rs"
secret = "the client PIN, its double hash, the pinUvAuthToken, the shared secret and the built-in-UV entry buffer"
bearers = []
sites = 28
skippable = 27
wraps = 0
success = "explicit"
error = "partial"
cancel = "partial"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR"]
why = "The densest file in the register and the one that makes the point: 27 of its 28 wipes are below a `?` in their own function. The count is not a verdict — `builtin_uv` owns the pad buffer and wipes it whatever `perform_builtin_uv` returns, cancel included — but nothing in this tree has checked the other 21 per statement."

[[secret]]
file = "crates/rsk-fido/src/config.rs"
secret = "the device seed in the clear, while authenticatorConfig wraps it under the PIN for the soft lock or unwraps it back"
bearers = []
sites = 4
skippable = 4
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR"]
why = "Every wipe here is below a `?`, because the whole AUT_ENABLE / AUT_DISABLE flow is a chain of fallible store operations around a plaintext seed. This is the row where a skipped wipe would cost the most, and it is the row with the least evidence that none is skipped."

[[secret]]
file = "crates/rsk-fido/src/credential.rs"
secret = "the credential-id sealing key and the plaintext rpId and nickname inside a credential id"
bearers = []
sites = 7
skippable = 6
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR"]
why = "The seal and unseal helpers each derive a key, use it, and wipe it after the AEAD returns, which puts the wipe below the `?` that rejects a bad tag. The rpId plaintext is a privacy secret rather than a key, and it rides the same buffers."

[[secret]]
file = "crates/rsk-fido/src/credmgmt.rs"
secret = "the per-credential keys and unsealed rpId records credential management walks"
bearers = []
sites = 5
skippable = 5
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR"]
why = "An enumeration holds one record at a time and wipes it before taking the next; the wipes are below the `?` that ends the walk on a store error, which is exactly the state where a record is live."

[[secret]]
file = "crates/rsk-fido/src/ec.rs"
secret = "the FIDO credential signing keys: the bare Weierstrass scalars, and the ML-DSA seeds behind the boxed lattice keys"
bearers = ["CredKey"]
sites = 16
skippable = 9
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR", "RES-LIBRARY-TEMPORARIES"]
why = "`CredKey`'s `Drop` covers the four Weierstrass variants, which is the whole reason it exists — `NonZeroScalar` has none of its own — and Ed25519's `SigningKey` and the boxed ML-DSA keys wipe themselves. That is not the whole file: nine wipes are raw seed buffers the ML-DSA constructors take, and they sit below the `?` that rejects a malformed seed, outside anything the `Drop` owns."

[[secret]]
file = "crates/rsk-fido/src/getassertion.rs"
secret = "the resolved credential's private key and the hmac-secret outputs on the assertion path"
bearers = []
sites = 5
skippable = 5
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR"]
why = "All five wipes are below a `?`, which is what an assertion is: a chain of fallible resolutions with a key live from the moment the credential is found. The key itself is a `CredKey` and leaves on its own `Drop`; what is counted here are the surrounding buffers."

[[secret]]
file = "crates/rsk-fido/src/hmacsecret.rs"
secret = "the hmac-secret CredRandom and the two salt outputs derived from it"
bearers = []
sites = 7
skippable = 7
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR"]
why = "One function, seven wipes, every one of them below the `?` chain that decrypts the salts and re-encrypts the outputs under the platform's shared secret. The outputs are the extension's whole product, so a skipped wipe here leaves the value the host asked for in RAM."

[[secret]]
file = "crates/rsk-fido/src/journal.rs"
secret = "the attestation key the audit journal signs its checkpoints with"
bearers = []
sites = 3
skippable = 3
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR"]
why = "The key is loaded, used for one signature and wiped; the load is fallible, so all three are below its `?`. The journal is opt-in at RUNTIME — a flash-persisted flag a host sets, not a build feature — so every build can have this secret live."

[[secret]]
file = "crates/rsk-fido/src/keyderiv.rs"
secret = "the ratchet state of the FIDO key-handle derivation — the seed-derived path and its HMAC tag"
bearers = []
sites = 2
skippable = 0
wraps = 0
success = "explicit"
error = "explicit"
cancel = "n/a"
reboot = "platform"
residual = ["RES-SRAM-CLEAR"]
why = "The two ratchet helpers are infallible: they take bytes, hash, and wipe the intermediate before returning, with no early exit to sit below. This is the shape the rest of the register would prefer and mostly does not have."

[[secret]]
file = "crates/rsk-fido/src/largeblobext.rs"
secret = "the per-credential blob key and the blob plaintext on the CTAP 2.3 largeBlob extension path"
bearers = []
sites = 2
skippable = 2
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR"]
why = "This build seals each blob at rest under the device seed because the platform no longer encrypts it, so the plaintext is briefly the device's own secret rather than the host's. The read path's wipe is below the `?` that rejects a bad tag."

[[secret]]
file = "crates/rsk-fido/src/makecredential.rs"
secret = "the freshly minted credential key and the attestation signature inputs"
bearers = []
sites = 3
skippable = 2
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR"]
why = "The credential key is a `CredKey` and leaves on its own `Drop`; the counted wipes are the seed and attestation buffers around it, two of them below the `?` chain that writes the credential to the store."

[[secret]]
file = "crates/rsk-fido/src/passkeys.rs"
secret = "the unsealed rpId and nickname records the resident-credential enumeration reads"
bearers = []
sites = 6
skippable = 6
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR"]
why = "The enumeration is a callback walk that can stop early, and every wipe is below that stop — so a walk abandoned mid-record leaves the record it was holding. Privacy records rather than keys: a survivor names a relying party, not a credential."

[[secret]]
file = "crates/rsk-fido/src/seed.rs"
secret = "the device master seed, the AES-GCM and CBC keys that unseal it, and every per-purpose key derived from it"
bearers = []
sites = 36
skippable = 29
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR"]
why = "The root of the device's key hierarchy and the largest single group of wipes in the tree, and 29 of the 36 are below a `?`. Two migration paths hold the seed in the clear for the length of a store rewrite, which is the longest any secret here is live."

[[secret]]
file = "crates/rsk-fido/src/state.rs"
secret = "the cross-message FIDO state: the pinUvAuthToken, the ephemeral ECDH scalar and the MSE session key"
bearers = ["FidoState"]
sites = 6
skippable = 0
wraps = 0
success = "explicit"
error = "on-drop"
cancel = "n/a"
reboot = "wiper"
wiper = "ctap.scrub_secrets"
residual = []
why = "`FidoState`'s `Drop` destructures exhaustively on purpose, so a field added later stops compiling until someone decides whether it is a secret — the one thing that would catch a new key missing the scrub, since a dropped value has no test that reads it. The reboot path reaches it through `ctap.scrub_secrets`."

[[secret]]
file = "crates/rsk-fido/src/u2f.rs"
secret = "the U2F attestation key and the per-registration key handle material"
bearers = []
sites = 7
skippable = 7
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR"]
why = "Both U2F commands load the attestation key, sign, and wipe — and the load is fallible, so all seven wipes are below its early return. U2F is disabled on an alwaysUv build, which bounds when this row is live at all."

[[secret]]
file = "crates/rsk-fido/src/vendor.rs"
secret = "the seed-backup payload, the MSE channel key, the ML-KEM leg and the device PIN taken at the pad"
bearers = []
sites = 26
skippable = 25
wraps = 0
success = "explicit"
error = "partial"
cancel = "partial"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR"]
why = "The worst ratio in the register — 25 of 26 — because the whole vendor applet is a fallible ceremony around the exported seed. Its cancel arm is the one place in the tree that wipes the pad buffer on every `PinEntry` outcome by hand, which is what the rest of the register would have to look like to close the error exit."

[[secret]]
file = "crates/rsk-mldsa/src/poly.rs"
secret = "the ML-DSA ring element — secret polynomial coefficients of s1, s2 and the signing nonce"
bearers = ["Poly"]
sites = 0
skippable = 0
wraps = 0
success = "on-drop"
error = "on-drop"
cancel = "n/a"
reboot = "platform"
residual = ["RES-SRAM-CLEAR"]
why = "One of two types in the tree whose wipe is entirely a `ZeroizeOnDrop` derive with no explicit call anywhere — `ExpandedKey` beside it is the other, and those two are what a `ZeroizeOnDrop` grep finds where the tree has eleven self-wiping types. Every polynomial the signer touches is one of these, so the derive covers the lattice arithmetic the way `Zeroizing` covers the bignum arithmetic."

[[secret]]
file = "crates/rsk-mldsa/src/sign.rs"
secret = "the expanded ML-DSA signing key: the matrix A, s1, s2 and the seed they came from"
bearers = ["ExpandedKey"]
sites = 1
skippable = 0
wraps = 2
success = "explicit"
error = "on-drop"
cancel = "n/a"
reboot = "platform"
residual = ["RES-SRAM-CLEAR"]
why = "`ExpandedKey` derives the same wipe `Poly` does, and the one explicit call is the seed buffer `from_seed` takes, wiped on its own tail. The matrix is streamed rather than held, which is what makes the −87 parameter set fit at all."

[[secret]]
file = "crates/rsk-oath/src/lib.rs"
secret = "the OATH credential secrets while the applet enumerates or re-seals them"
bearers = []
sites = 3
skippable = 0
wraps = 0
success = "explicit"
error = "explicit"
cancel = "n/a"
reboot = "platform"
residual = ["RES-SRAM-CLEAR"]
why = "The enumeration and the migration both wipe on the loop body's tail rather than after the fallible step, so no wipe sits below an early exit. The HMAC keys themselves are the seal module's next door."

[[secret]]
file = "crates/rsk-oath/src/seal.rs"
secret = "the OATH per-credential HMAC keys and the AEAD key that seals them at rest"
bearers = []
sites = 8
skippable = 7
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR"]
why = "Derive, seal, wipe — and both the derive and the seal are fallible, so 7 of the 8 wipes are below an early exit. A survivor here is a TOTP secret, which is a long-lived shared secret rather than a session value."

[[secret]]
file = "crates/rsk-openpgp/src/init.rs"
secret = "the DEK and the default PIN verifiers written at first-boot provisioning"
bearers = []
sites = 4
skippable = 4
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR"]
why = "Provisioning is a chain of store writes with the DEK in the clear between them, so every wipe is below a `?`. It runs once per factory reset, which bounds the exposure in time but not in value."

[[secret]]
file = "crates/rsk-openpgp/src/keypairgen.rs"
secret = "the freshly generated OpenPGP private key before it is sealed into the DEK"
bearers = []
sites = 1
skippable = 1
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR"]
why = "One wipe, on the generation tail, below the `?` that seals the key. The key material itself is an `RsaKey` or a `PrivKey` and leaves on its own `Drop`; what is counted is the staging buffer."

[[secret]]
file = "crates/rsk-openpgp/src/keys.rs"
secret = "the DEK, and every OpenPGP private key it unseals — RSA, EC and the AES key"
bearers = []
sites = 21
skippable = 21
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR", "RES-LIBRARY-TEMPORARIES"]
why = "Every one of the 21 wipes is below an early exit, which is what a store-backed key cache looks like: read, unseal, use, wipe, with a `?` on each of the first two. The DEK is the applet's root key, so this is the highest-value row with no wipe the derivation can vouch for."

[[secret]]
file = "crates/rsk-openpgp/src/kdf.rs"
secret = "the DEK in the open between the load that unseals it and the two re-seals that rewrap it under the reference values the KDF-DO declares"
bearers = []
sites = 2
skippable = 2
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR"]
why = "One buffer, wiped on both ways out: below the whole stage/verifier/commit sequence, and again on the `load_dek` refusal above it — `decrypt_with_aad` writes the plaintext before it checks the tag, so a refused open can still have left the DEK there. Both count as skippable because the wipe is syntactically below an exit of its own function; the exits above them hold nothing, since the PW3 gate, the body's validation and the no-keys guard all run before the DEK is opened. No cancel: this command polls no presence."

[[secret]]
file = "crates/rsk-openpgp/src/pin.rs"
secret = "PW1, PW3 and the resetting code, the session copies of all three, and the DEK while a PIN change rewraps it"
bearers = ["Session"]
sites = 22
skippable = 15
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR"]
why = "`Session`'s `Drop` wipes the three session copies, and `reset` wipes the same fields by hand for the case where the session is reused rather than dropped. The other 15 are the PIN and DEK buffers of the change, unblock and rewrap flows, where fallible store work is interleaved with plaintext key material."

[[secret]]
file = "crates/rsk-openpgp/src/pso.rs"
secret = "the AES key the symmetric PSO operation loads out of the DEK"
bearers = []
sites = 1
skippable = 1
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR"]
why = "One key, one wipe, below the `?` that loads it. The asymmetric arms of PSO hand off to the keys module next door and hold nothing of their own."

[[secret]]
file = "crates/rsk-otp/src/hid.rs"
secret = "the OTP keyboard frame buffers: the reassembly frame and the taken request, either of which can hold a slot AES key, private UID or access code"
bearers = []
sites = 3
skippable = 1
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "wiper"
wiper = "otp_kbd::scrub"
residual = ["RES-ERROR-EARLY-EXIT"]
why = "A transport buffer rather than a key store, and the threat model's \"transport/exchange buffers as soon as a message completes\" is about exactly this one. The reboot path reaches it through `otp_kbd::scrub`, which is the wipe audit run-34 added for the reflash case."

[[secret]]
file = "crates/rsk-otp/src/lib.rs"
secret = "the Yubico OTP slot secrets while the applet configures, swaps or re-seals a slot"
bearers = []
sites = 2
skippable = 0
wraps = 0
success = "explicit"
error = "explicit"
cancel = "n/a"
reboot = "platform"
residual = ["RES-SRAM-CLEAR"]
why = "The migration wipes on its loop tail; nothing here sits below an early exit. The slot secrets themselves live in the seal module and the keyboard transport."

[[secret]]
file = "crates/rsk-otp/src/seal.rs"
secret = "the AES key, private UID and access code of a Yubico OTP slot, and the AEAD key sealing them"
bearers = []
sites = 8
skippable = 7
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR"]
why = "Derive, seal, wipe, with the fallible store call between the derive and the wipe — so 7 of the 8 sit below an early exit. A survivor is a slot's AES key or private UID, which the keyboard transport will type on the next button press."

[[secret]]
file = "crates/rsk-piv/src/auth.rs"
secret = "the PIV management key and the ECDH shared secret GENERAL AUTHENTICATE computes"
bearers = []
sites = 7
skippable = 7
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR", "RES-LIBRARY-TEMPORARIES"]
why = "The mutual-auth witness, the challenge and the ECDH secret are all live across fallible store and crypto calls, so every wipe here is below an early exit. The curve arithmetic's temporaries under them are RustCrypto's."

[[secret]]
file = "crates/rsk-piv/src/files.rs"
secret = "the PIV PIN and PUK verifiers while they are written at provisioning or reset"
bearers = []
sites = 3
skippable = 2
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR"]
why = "The verifier is derived, written and wiped. Two of the three wipes are below a `?`; the third is below a `map_err` that binds the store's answer without returning, which is why an earlier count of three was wrong. A survivor is a PIN verifier, not a PIN — a weaker secret, still an offline-guessing target."

[[secret]]
file = "crates/rsk-piv/src/keygen.rs"
secret = "the imported Edwards scalar and the attestation key PIV signs a slot certificate with"
bearers = []
sites = 2
skippable = 2
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR"]
why = "Both wipes sit below an early exit: the import's staging buffer under the parse that can refuse the key, and the attestation buffer under the load of the attestation key. Generated keys go straight into the seal module and are never in the clear here."

[[secret]]
file = "crates/rsk-piv/src/lib.rs"
secret = "the protected management key, the GENERAL AUTHENTICATE challenge, and the key material a MOVE carries between slots"
bearers = []
sites = 12
skippable = 11
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR"]
why = "The applet returns status words rather than `Result`, so its early exits are `return Sw::…` and 11 of the 12 wipes are below one — a count an earlier derivation read as zero because it looked for `return Err`. The MOVE path reads the answer of the delete that drops the SOURCE KEY; the certificate and public-point deletes beside it are best-effort by disposition, so the key never outlives a half-done move."

[[secret]]
file = "crates/rsk-piv/src/seal.rs"
secret = "the PIV slot private keys — EC scalars and RSA CRT parameters — and the AES-256-GCM key that seals them"
bearers = []
sites = 14
skippable = 12
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR", "RES-LIBRARY-TEMPORARIES"]
why = "Twelve of the fourteen are below an early exit, in the load and store helpers where the unsealed plaintext and the derived key are both live across a store call. The RSA halves hand their limbs to `num-bigint-dig`, whose internal temporaries nothing here can reach."

[[secret]]
file = "crates/rsk-rescue/src/keydev.rs"
secret = "the rescue device key — a secp256k1 private scalar — and the AEAD key that seals it"
bearers = []
sites = 14
skippable = 11
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR", "RES-LIBRARY-TEMPORARIES"]
why = "Eleven of the fourteen are below an early exit, in the load-or-generate and migration paths where the scalar is in the clear across a store write. The key attests the device, so a survivor is an impersonation input rather than a user secret."

[[secret]]
file = "crates/rsk-rsa/src/crt.rs"
secret = "the RSA CRT parameters — p, q, dP, dQ, qInv — and the plaintext blob they are parsed out of"
bearers = ["RsaCrt"]
sites = 18
skippable = 13
wraps = 10
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR", "RES-LIBRARY-TEMPORARIES"]
why = "`RsaCrt`'s `Drop` wipes all five parameters, so the parsed key leaves on every non-panic path; the 13 counted wipes are the parse and private-operation scratch, below the `?` that rejects a malformed blob. These are the factors of a live modulus — the highest-value bytes in the tree."

[[secret]]
file = "crates/rsk-rsa/src/key.rs"
secret = "the RSA private key: the two primes, the private exponent and the cached CRT parameters"
bearers = ["CrtParams", "RsaKey"]
sites = 8
skippable = 1
wraps = 23
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR", "RES-LIBRARY-TEMPORARIES"]
why = "Two `Drop` impls, because `num-bigint` has no scrubbing one of its own and the limbs would otherwise be freed intact; `n` and `e` are public and deliberately not wiped. Every value the module owns rides in a `Zeroizing`, which is why only one wipe sits below an early exit — and why the library's own internal temporaries are the residual that is left."

[[secret]]
file = "crates/rsk-rsa/src/keygen.rs"
secret = "the first prime found by a keygen, and the candidate stream around it"
bearers = ["RsaKeygen"]
sites = 7
skippable = 3
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR", "RES-LIBRARY-TEMPORARIES"]
why = "`RsaKeygen`'s `Drop` exists for the abandoned-between-steps case, where the search holds one prime and no key — a state that lasts as long as the host takes to send the next command. Three of the seven wipes are in the candidate offers, below the early return a rejected candidate takes."

[[secret]]
file = "crates/rsk-rsa/src/lib.rs"
secret = "the modexp working buffers and the static small-prime sieve state"
bearers = []
sites = 22
skippable = 0
wraps = 0
success = "explicit"
error = "explicit"
cancel = "n/a"
reboot = "platform"
residual = ["RES-SRAM-CLEAR"]
why = "Twenty-two wipes and none below an early exit: the modexp and primality routines are infallible by construction, so every scratch buffer is wiped on the one tail they have. `SMALL_PRIMES` itself is a public constant table and holds no secret; the sieve's candidate state does, and its wipe is `scrub`."

[[secret]]
file = "crates/rsk-rsa/src/pkcs1v15.rs"
secret = "the decrypted PKCS#1 v1.5 plaintext before the padding check strips it"
bearers = []
sites = 1
skippable = 1
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR"]
why = "One wipe, below the `?` on the private operation. The padding check is constant-time on purpose, so the failing branch reaches the same wipe as the succeeding one — the early exit above it is the decryption, not the check."

[[secret]]
file = "crates/rsk-sdk/src/applet.rs"
secret = "the APDU dispatch buffers, which carry a VERIFY PIN or an imported private key on the way in"
bearers = []
sites = 7
skippable = 5
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR"]
why = "The chaining and pending-command buffers are cleared by dedicated helpers, and five of the seven wipes are below an early exit of the dispatch that calls them — a refused APDU leaves before the clear. This is the seam the threat model's transport-buffer sentence is about on the CCID side."

[[secret]]
file = "crates/rsk-slip39/src/lib.rs"
secret = "the seed being split into SLIP-39 shares, the digest share and the round-function intermediates"
bearers = []
sites = 14
skippable = 6
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "platform"
residual = ["RES-ERROR-EARLY-EXIT", "RES-SRAM-CLEAR"]
why = "The Feistel rounds and the share arithmetic wipe their intermediates on their own tails; six of the fourteen are below the fallible entry's early returns, where the seed is live across a parameter check. A share is the seed under a threshold, so every intermediate here is seed-grade."

[[secret]]
file = "crates/rsk-usb/src/ccid.rs"
secret = "the CCID bulk transfer buffers, which carry a PIN or an imported key in and a session key out"
bearers = []
sites = 4
skippable = 0
wraps = 0
success = "explicit"
error = "explicit"
cancel = "n/a"
reboot = "platform"
residual = ["RES-SRAM-CLEAR"]
why = "The transport wipes its buffers on the transfer's own tail, which is the same statement whether the applet answered or errored. It reads no cancel frame of its own — the secure ceremony's cancel is decided in the worker, which is the row that answers for it."

[[secret]]
file = "crates/rsk-usb/src/ctaphid.rs"
secret = "the CTAPHID message reassembly buffer, which carries a clientPIN payload or an imported blob"
bearers = []
sites = 3
skippable = 0
wraps = 0
success = "explicit"
error = "explicit"
cancel = "explicit"
reboot = "platform"
residual = ["RES-SRAM-CLEAR"]
why = "`scrub` wipes the message buffer and the transport keeps working after it, which is what the host test beside it asserts. A CTAPHID_CANCEL aborts the touch wait and the command answers KEEPALIVE_CANCEL through the same return the other outcomes use."

[[secret]]
file = "firmware/src/core1.rs"
secret = "core1's mailbox — the RSA primes found by the second core and the keygen DRBG seed — and the two static sieves"
bearers = []
sites = 8
skippable = 5
wraps = 0
success = "explicit"
error = "partial"
cancel = "n/a"
reboot = "wiper"
wiper = "core1::scrub"
residual = ["RES-ERROR-EARLY-EXIT", "RES-CORE1-SIEVE"]
why = "The mailbox holds factors of a live modulus in plain SRAM between commands, which is why `core1::scrub` is on the reboot path at all. The sieves are single-core-exclusive and cannot be cleared from the other core, so a wedged core1 keeps its own."

[[secret]]
file = "firmware/src/handler.rs"
secret = "the TRNG seed and reseed material feeding the board's DRBG"
bearers = []
sites = 2
skippable = 0
wraps = 0
success = "explicit"
error = "explicit"
cancel = "n/a"
reboot = "wiper"
wiper = "rng.borrow_mut().scrub"
residual = []
why = "Both seed buffers are wiped immediately after the DRBG has absorbed them, on infallible paths with no early exit. The DRBG state itself is the crypto crate's row, and the reboot path reaches this one through `rng.borrow_mut().scrub`."

[[secret]]
file = "firmware/src/otp_kbd.rs"
secret = "the typing queue that holds a rendered OTP ticket, static password or HMAC response"
bearers = []
sites = 1
skippable = 0
wraps = 0
success = "explicit"
error = "explicit"
cancel = "n/a"
reboot = "wiper"
wiper = "otp_kbd::scrub"
residual = []
why = "A ticket queued for typing outlives the command that produced it by design, so its wipe is the reboot's rather than the dispatch's: `otp_kbd::scrub` clears the queue and the frame buffers together before the drop to BOOTSEL."

[[secret]]
file = "firmware/src/worker.rs"
secret = "the static request and response exchange buffers, and the PIN the secure ceremony collects at the pad"
bearers = []
sites = 6
skippable = 3
wraps = 0
success = "explicit"
error = "partial"
cancel = "partial"
reboot = "wiper"
wiper = "ctap.scrub_secrets"
residual = ["RES-ERROR-EARLY-EXIT"]
why = "The exchange buffers are static and shared, so a survivor there is readable by the next command rather than merely resident — every roundtrip wipes both ends. The reboot path is this file's own, and it runs `ctap.scrub_secrets`, `ccid.scrub`, `rng.borrow_mut().scrub`, `otp_kbd::scrub` and `core1::scrub` before dropping."
