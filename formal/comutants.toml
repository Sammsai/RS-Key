# SPDX-License-Identifier: AGPL-3.0-only
# Copyright (C) 2026 RS-Key contributors
#
# Co-refutation: every model mutant, re-made as the code defect it models.
# The model's 28 Bug* switches each rebuild a real RS-Key defect and MUST go
# RED in TLC — that is measured weekly. This file asks the other half of the
# question: does the SAME defect, injected into the production Rust, go red
# at the code level too? A mutant the model catches and the code level does
# not is a measured abstraction gap — the exact place where "TLA+ green AND
# tests green" stops meaning "the same system was checked twice".
#
# scripts/comutate.py owns the mechanics: `--lint` (a check.sh row) holds this
# file against the Mut_*.cfg roster in both directions, resolves every patch
# anchor against the current tree, and floors the pending count; `run` applies
# a patch in a throwaway worktree, runs the slice, and compares the verdict to
# `expect`. Anchors are exact source snippets, not diff line numbers — code
# drift makes the lint fail loudly instead of the patch applying somewhere else.
#
# Statuses:
#   patch        find/replace below; `expect` records the measured verdict
#                (killed | gap). A killed that becomes gap is a regression.
#   unreachable  the defect became unreachable by construction after a shipped
#                fix; the model measured it (evidence names where). No patch.
#   pending      batch not yet derived. The count is FLOORED by --lint: it can
#                only go down, so "later" stays a plan rather than a hole.
#
# The invariant each mutant targets is NOT recorded here — the lint derives it
# from the mutant's own Solo_*.cfg, so this file cannot disagree with the
# matrix about who catches what.

pending_floor = 0
# 28 until the token-less makeCredential bridge, and 31 now. The count is an
# EQUALITY and not a floor, so it moves only with the `Mut_*` roster itself — the
# three additions are BugUvNotRqdIgnoresRk and BugTokenlessIgnoresAlwaysUv
# (batch 6) and BugForceChangeIgnored (batch 7). The arithmetic is stated because
# an earlier version of this comment named two additions beside a count of 31.
phase2_count = 31

# ---- batch 1: the token/PIN family and the two write orders -----------------

[comutant.BugTokenSurvivesPinChange]
status = "patch"
file = "crates/rsk-fido/src/clientpin.rs"
find = """
    clear_force_change(ctx.fs)?;
    ctx.state.reset_pin_uv_auth_token(ctx.rng);
"""
replace = """
    clear_force_change(ctx.fs)?;
"""
slice = ["cargo", "test", "-p", "rsk-fido"]
expect = "killed"
note = "changePIN keeps the session token: §6.5.5.6 step 15 skipped."

[comutant.BugStopUsingKeepsPerms]
status = "patch"
file = "crates/rsk-fido/src/state.rs"
find = """
        self.paut.in_use = false;
        self.paut.permissions = 0;
"""
replace = """
        self.paut.in_use = false;
"""
slice = ["cargo", "test", "-p", "rsk-fido"]
expect = "killed"
note = "stopUsingPinUvAuthToken keeps the permission bits."

[comutant.BugWrongPinKeepsToken]
status = "patch"
file = "crates/rsk-fido/src/clientpin.rs"
find = """
        // platform can always mint another.
        ctx.state.reset_pin_uv_auth_token(ctx.rng);
"""
replace = """
        // platform can always mint another.
"""
slice = ["cargo", "test", "-p", "rsk-fido"]
expect = "killed"
note = "the pre-E38 tree: a PIN mismatch leaves the outstanding token alive."

[comutant.BugSetPinOverExisting]
status = "patch"
file = "crates/rsk-fido/src/clientpin.rs"
find = """
    if ctx.fs.try_has_data(EF_PIN).map_err(|_| CtapError::Other)? {
        return Err(CtapError::PinAuthInvalid);
    }
"""
replace = ""
slice = ["cargo", "test", "-p", "rsk-fido"]
expect = "killed"
note = "setPIN overwrites a live PIN instead of refusing (§6.5.5.5)."

[comutant.BugCredBeforeRp]
status = "patch"
file = "crates/rsk-fido/src/credential.rs"
find = """
    if new_record {
        bump_rp(fs, seed, rp_id_hash, rp_id)?;
    }
"""
replace = """
    let _defer_bump = new_record;
"""
find2 = """
    if let Err(e) = fs.put(EF_CRED + slot, &rec[..total]) {
        if new_record {
            let _ = crate::credmgmt::decrement_rp(fs, rp_id_hash);
        }
        return Err(e);
    }
"""
replace2 = """
    fs.put(EF_CRED + slot, &rec[..total])?;
    if _defer_bump {
        bump_rp(fs, seed, rp_id_hash, rp_id)?;
    }
"""
slice = ["cargo", "test", "-p", "rsk-fido"]
expect = "killed"
note = "registration writes EF_CRED before EF_RP: a torn write strands a live credential no enumeration surface can reach."

[comutant.BugDeleteRpBeforeCred]
status = "patch"
file = "crates/rsk-fido/src/credmgmt.rs"
find = """
    ctx.fs
        .delete(EF_CRED + slot)
        .map_err(|_| CtapError::NotAllowed)?;
"""
replace = """
    decrement_rp(ctx.fs, rp_id_hash)?;
    ctx.fs
        .delete(EF_CRED + slot)
        .map_err(|_| CtapError::NotAllowed)?;
"""
find2 = """
    largeblobext::discard(ctx.fs, slot);
    decrement_rp(ctx.fs, rp_id_hash)?;
    Ok(0)
"""
replace2 = """
    largeblobext::discard(ctx.fs, slot);
    Ok(0)
"""
slice = ["cargo", "test", "-p", "rsk-fido"]
expect = "killed"
note = """deleteCredential drops the RP entry first: a torn delete strands the
credential. Was batch 1's one measured GAP — the registration twin's harness
tears writes, and no harness tore a delete — closed by
a_torn_delete_never_leaves_a_credential_without_its_rp (a mutating-op-budget
tear over a sole-credential RP), re-measured killed."""

[comutant.BugUnscopedCancel]
status = "patch"
file = "crates/rsk-device/src/presence.rs"
find = """
        if self.wait_scope.load(Ordering::Acquire) == SCOPE_FIDO {
            self.cancel_requested.store(true, Ordering::Release);
        }
"""
replace = """
        self.cancel_requested.store(true, Ordering::Release);
"""
slice = ["cargo", "test", "-p", "rsk-device"]
expect = "killed"
note = "a CTAPHID cancel lands whatever transport owns the touch."

[comutant.BugUnscopedOtpCancel]
status = "patch"
file = "crates/rsk-device/src/presence.rs"
find = """
        if self.wait_scope.load(Ordering::Acquire) == SCOPE_OTP {
            self.cancel_requested.store(true, Ordering::Relaxed);
"""
replace = """
        {
            self.cancel_requested.store(true, Ordering::Relaxed);
"""
slice = ["cargo", "test", "-p", "rsk-device"]
expect = "killed"
note = "the second writer of the same cancel flag, unscoped."

# ---- unreachable by construction — measured, not assumed --------------------

[comutant.BugBackupSealedNotAGate]
status = "patch"
file = "crates/rsk-fido/src/reset.rs"
find = """
        EF_PIN | EF_DEVICE_PIN | EF_ALWAYS_UV | EF_MINPINLEN | EF_BACKUP_SEALED
"""
replace = """
        EF_PIN | EF_DEVICE_PIN | EF_ALWAYS_UV | EF_MINPINLEN
"""
slice = ["cargo", "test", "-p", "rsk-fido"]
expect = "killed"
note = "THE VERDICT THIS ROW CARRIED WAS MEASURED IN THE MODEL AND WRITTEN ABOUT THE CODE. `unreachable` said the defect is not falsifiable on its own, and that is true of the MODEL: with the seed leading the wipe unconditionally (0x08BF) the window this re-opens is over a seed already gone, which is why `Mut_BugBackupSealedNotAGate.cfg` has to arm `BugSeedDoesNotLead` beside it. Nothing had asked the code the same question, and a model measurement is evidence about the model. WHAT THE CODE ANSWERS, AND WHAT THAT ANSWER IS WORTH -- both halves, because the first draft of this note got the second one wrong. Two tests fall deterministically, and BOTH TRANSCRIBE THE MATCH ARM this patch edits: `the_gate_set_defers_every_record_whose_absence_is_permissive` (crates/rsk-fido/src/reset_tests.rs:638) iterates the same five-member list, and `reset_projection_finishes_only_after_every_ordered_phase` (crates/rsk-fido/src/reset_assurance_tests.rs:76) asserts the Gate phase may delete it. Control measured, not argued: demoting `EF_MINPINLEN` instead -- which crates/rsk-fido/src/reset.rs:237-243 says out loud is in the phase and in NO clause -- kills the slice identically, 3 of 3 runs. So on their own these two answer `did somebody edit the list`, not `is the defect caught`. THE BEHAVIOURAL TEST THAT EXISTED WAS A COIN FLIP, and the mechanism is measured: `RamStorage` is a `HashMap` and `for_each_key` iterates it (crates/rsk-fs/src/storage.rs:78 and :105), so whether the sweep reaches 0xCC02 before it aborts on the refused 0xCC00 is a fresh permutation per process. Over 400 fixtures the marker survived 233 times and was lost 167. `a_seed_the_medium_kept_stops_the_wipe_before_the_gates` is therefore ~42% and reads deterministic-green on the shipped tree, which is where it looks trustworthy. SO A DETERMINISTIC ONE WAS ADDED IN THE SAME DIFF: `a_backup_seal_swept_with_the_secrets_is_a_reopened_export_window` (crates/rsk-fido/src/reset_assurance_tests.rs:130) drives the projection, whose `delete` takes its phase from the production `reset_phase`, and asserts `reset_keeps_the_backup_seal` over a seed still reachable. Measured in all three directions: shipped GREEN, this mutant RED 5 of 5, the `EF_MINPINLEN` control GREEN 3 of 3. That is the test this row`s `killed` should be read against; the two transcribers are the reason it is not read against them."

[comutant.BugSetPinKeepsPpuat]
status = "unreachable"
evidence = "MEASURED AT THE CODE LEVEL, after its sibling BugBackupSealedNotAGate was found carrying a model measurement written about the code. The model half stands: under eab4b5c the grant is swept in phase 1 and EF_PIN in phase 2, so `~pin.set /\\ gate.ppuat` is unreachable and the mutant explored 40M+ states without a counterexample (E171). The CODE half is different and is why this stays without a patch: there is no site whose removal reproduces `setPIN keeps the grant` ALONE. `set_pin`s own `clear_ppuat` (crates/rsk-fido/src/clientpin.rs:220) is REDUNDANT — deleting it leaves all 668 rsk-fido tests green, three runs — because `write_pin_verifier` (crates/rsk-fido/src/clientpin.rs:845-847) clears the grant before the EF_PIN write, which is where the ordering guarantee actually lives. Deleting THAT one is red at once (`every_ef_pin_verifier_write_revokes_the_persistent_token` and `local_pin_change_revokes_the_persistent_token`), but it is the site changePIN and the panel share, so it is BugChangePinKeepsPpuats twin as much as this ones. A shared chokepoint has no per-caller mutant, and saying so is the measurement."

# ---- batch 2: the rest, floor now 0 ----------------------------------------

[comutant.BugChangePinKeepsPpuat]
status = "patch"
file = "crates/rsk-fido/src/clientpin.rs"
find = """
    clear_ppuat(ctx.fs).map_err(|_| CtapError::Other)?;
    let res = store_new_pin(ctx, &padded);
    padded.zeroize();
    res?;
    // The new PIN met minPINLength (store_new_pin), so the change satisfies a
"""
replace = """
    let res = store_new_pin(ctx, &padded);
    padded.zeroize();
    res?;
    // The new PIN met minPINLength (store_new_pin), so the change satisfies a
"""
find2 = """
    if fid == EF_PIN {
        clear_ppuat(fs).map_err(|_| CtapError::Other)?;
    }
"""
replace2 = ""
slice = ["cargo", "test", "-p", "rsk-fido"]
expect = "killed"
note = """changePIN keeps the persistent pcmr grant. TWO layers must go: the
authoritative revoke lives in write_pin_verifier (the run-37 fix moved it into
the storage core), and change_pin's own clear_ppuat is a redundant second line.
The first patch removed only the outer layer and measured exactly that
redundancy — every test stayed green because the inner revoke held. That
finding is worth the note: the property is enforced twice, and a single-layer
mutant cannot re-create the model's defect."""

[comutant.BugPinWriteBeforeRevoke]
status = "patch"
file = "crates/rsk-fido/src/clientpin.rs"
find = """
    clear_ppuat(ctx.fs).map_err(|_| CtapError::Other)?;
    let res = store_new_pin(ctx, &padded);
    padded.zeroize();
    res?;
    // The new PIN met minPINLength (store_new_pin), so the change satisfies a
"""
replace = """
    let res = store_new_pin(ctx, &padded);
    padded.zeroize();
    res?;
    // The new PIN met minPINLength (store_new_pin), so the change satisfies a
"""
find2 = """
    // Before the new verifier lands, never after: a power cut the other way round
    // leaves the old holder's grant live against a PIN they no longer know.
    if fid == EF_PIN {
        clear_ppuat(fs).map_err(|_| CtapError::Other)?;
    }
"""
replace2 = ""
find3 = """
    fs.put(fid, &pin_data).map_err(|_| CtapError::Other)
}
"""
replace3 = """
    fs.put(fid, &pin_data).map_err(|_| CtapError::Other)?;
    if fid == EF_PIN {
        clear_ppuat(fs).map_err(|_| CtapError::Other)?;
    }
    Ok(())
}
"""
slice = ["cargo", "test", "-p", "rsk-fido"]
expect = "killed"
note = """the revoke moved AFTER the verifier write at the authoritative site
(write_pin_verifier), with change_pin's redundant outer layer removed so the
reorder is real: a cut between the two writes leaves the old holder's grant
live under a PIN they no longer know. The completed end state is identical
either way — only the torn-changePIN harness distinguishes the orders."""

[comutant.BugCmWalkIgnoresChannel]
status = "patch"
file = "crates/rsk-fido/src/state.rs"
find = "self.channel == channel && self.rp_counter <= self.rp_total"
replace = "self.rp_counter <= self.rp_total"
slice = ["cargo", "test", "-p", "rsk-fido"]
# THE FIRST PROOF HALF IN THIS FILE. Every other slice here is a `cargo test`, so
# no Kani harness in the tree was reddened by any recorded mutant and the one
# proof `SEC-FIDO-001` has was falsified by nothing. This patch drops the channel
# conjunct out of `may_walk_rps`, so B1's EQUALITY must fail on the non-owning
# probe specifically — a different check falling means a different defect, which
# is what `proof_names` compares.
# `--verbose` because Kani suppresses a child's output without it (its own
# `--quiet` says so), and the runner's first answer here was `goto-cc exited with
# status 1` with goto-cc's reason thrown away.
proof = [
  "cargo", "kani", "-p", "rsk-fido", "--features", "kani-soft",
  "--harness", "no_authorization_bypass_walk_owner", "--verbose",
]
proof_names = "NoAuthorizationBypass/B1"
expect = "killed"
note = "the RP walk ignores which channel opened it — any channel continues it."

[comutant.BugNoConsumeAfterUp]
status = "patch"
file = "crates/rsk-fido/src/state.rs"
find = """
        if self.paut.in_use {
            self.paut.permissions &= PERM_LBW;
            self.paut.user_present = false;
"""
replace = """
        if self.paut.in_use {
            self.paut.user_present = false;
"""
slice = ["cargo", "test", "-p", "rsk-fido"]
expect = "killed"
note = "mc/ga survive one user presence (GHSA-wqjm-653g-hgw3): the narrowing to largeBlobWrite is gone."

[comutant.BugConsumeKeepsMcGa]
status = "patch"
file = "crates/rsk-fido/src/state.rs"
find = "self.paut.permissions &= PERM_LBW;"
replace = "self.paut.permissions &= PERM_LBW | PERM_MC | PERM_GA;"
slice = ["cargo", "test", "-p", "rsk-fido"]
expect = "killed"
note = "the §6.5.5.7 triad kept instead of narrowed — mc/ga persist past the presence they were spent on."

[comutant.BugResetGatesFirst]
status = "patch"
file = "crates/rsk-fido/src/reset.rs"
find = """
    orphaned |= sweep(ctx, |fid| is_fido_fid(fid) && !is_fido_gate_fid(fid))?;
    orphaned |= sweep(ctx, is_fido_gate_fid)?;
"""
replace = """
    orphaned |= sweep(ctx, is_fido_gate_fid)?;
    orphaned |= sweep(ctx, |fid| is_fido_fid(fid) && !is_fido_gate_fid(fid))?;
"""
slice = ["cargo", "test", "-p", "rsk-fido"]
expect = "killed"
note = "gates swept before secrets: a torn reset can strand a passkey with its PIN gate already gone."

[comutant.BugSeedDoesNotLead]
status = "patch"
file = "crates/rsk-fido/src/reset.rs"
find = """
    let mut refused = false;
    for fid in FIDO_SEED_FIDS {
        let gone = ctx.fs.force_delete_halves(fid);
        refused |= gone.value.is_err();
        orphaned |= gone.record.is_err();
    }
"""
replace = """
    let refused = false;
"""
slice = ["cargo", "test", "-p", "rsk-fido"]
expect = "killed"
note = "the pre-0x08BF wipe: the seed is swept in ring order instead of leading, so a cut can leave a decryptable credential behind a live seed."

[comutant.BugStateResetAfterWipe]
status = "patch"
file = "crates/rsk-fido/src/reset.rs"
find = """
    ctx.state.reset();
    // And the seed leads the flash, in its own write ahead of the batch: ring order
"""
replace = """
    // And the seed leads the flash, in its own write ahead of the batch: ring order
"""
find2 = """
    ensure_seed(&ctx.dev, ctx.fs, ctx.rng).map_err(|_| CtapError::Other)?;
"""
replace2 = """
    ensure_seed(&ctx.dev, ctx.fs, ctx.rng).map_err(|_| CtapError::Other)?;
    ctx.state.reset();
"""
slice = ["cargo", "test", "-p", "rsk-fido"]
expect = "killed"
note = """E76 regression: the live session reset moved behind the flash work.
Was a measured gap — the torn-reset harness tore the flash but never asked when
the SESSION died — closed by
a_torn_reset_never_leaves_the_session_running_on_a_wiped_seed, which plants the
RAM seed copy and asserts it is gone at EVERY tear budget, including 0."""

[comutant.BugWarmResetReopensWindow]
status = "patch"
file = "crates/rsk-fido/src/reset.rs"
find = "!ctx.state.warm_boot && ctx.now_ms <= RESET_WINDOW_MS"
replace = "ctx.now_ms <= RESET_WINDOW_MS"
slice = ["cargo", "test", "-p", "rsk-fido"]
expect = "killed"
note = "a warm reset re-opens the reset window a host-requestable sys_reset can restart at will."

[comutant.BugPpuatIsAGate]
status = "patch"
file = "crates/rsk-fido/src/reset.rs"
find = """
    matches!(
        fid,
        EF_PIN | EF_DEVICE_PIN | EF_ALWAYS_UV | EF_MINPINLEN | EF_BACKUP_SEALED
    )
}
"""
replace = """
    matches!(
        fid,
        EF_PIN | EF_DEVICE_PIN | EF_ALWAYS_UV | EF_MINPINLEN | EF_BACKUP_SEALED
    ) || fid == EF_PAUTHTOKEN.get()
}
"""
slice = ["cargo", "test", "-p", "rsk-fido"]
expect = "killed"
note = "eab4b5c reverted: EF_PAUTHTOKEN back in the deferred gate phase (it is a KeyFid, so `.get()`, not a match arm), where a torn wipe strands a grant with no PIN behind it."

# ---- rsk-device: presence arbitration + the PinLock carry -------------------

[comutant.BugNoDropStaleCancelAtEntry]
status = "patch"
file = "crates/rsk-device/src/presence.rs"
find = """
        arb.set_cancel_requested(false);
        arb.set_up_pending(true);
"""
replace = """
        arb.set_up_pending(true);
"""
slice = ["cargo", "test", "-p", "rsk-device"]
expect = "killed"
note = "the wait-entry cancel drop removed: a cancel latched by a dispatch that never entered wait is consumed by the next one."

[comutant.BugTouchNotSpent]
status = "patch"
file = "crates/rsk-device/src/presence.rs"
find = """
                if !self.spent {
                    break Outcome::Confirmed;
                }
"""
replace = """
                break Outcome::Confirmed;
"""
slice = ["cargo", "test", "-p", "rsk-device"]
expect = "killed"
note = "the spent latch gone: a press the previous ceremony consumed satisfies the next one."

[comutant.BugPanelCancelable]
status = "patch"
file = "crates/rsk-device/src/presence.rs"
find = """
        if self.wait_scope.load(Ordering::Acquire) == SCOPE_FIDO {
            self.cancel_requested.store(true, Ordering::Release);
        }
"""
replace = """
        if self.wait_scope.load(Ordering::Acquire) != SCOPE_OTP {
            self.cancel_requested.store(true, Ordering::Release);
        }
"""
slice = ["cargo", "test", "-p", "rsk-device"]
expect = "killed"
note = "E45: request_cancel lands during a panel (SCOPE_NONE) ceremony, not just a FIDO wait. The model splits the scope test three ways; the code fuses it into one guard, so this and BugUnscopedCancel share a site."

[comutant.BugNoTouchRequired]
status = "patch"
file = "crates/rsk-device/src/presence.rs"
find = """
            if board.pressed() {
                // A press the previous ceremony already consumed is not consent
                // for this one; it stays spent until the finger actually lifts.
                if !self.spent {
                    break Outcome::Confirmed;
                }
            } else {
"""
replace = """
            if true {
                break Outcome::Confirmed;
            } else if board.pressed() {
            } else {
"""
slice = ["cargo", "test", "-p", "rsk-device"]
expect = "killed"
note = "the wait confirms without a press at all."

[comutant.BugHostPreemptsLocalWait]
status = "patch"
file = "crates/rsk-device/src/presence.rs"
find = """
    pub fn set_wait_scope(&self, scope: u8) {
        self.wait_scope.store(scope, Ordering::Release);
    }
"""
replace = """
    pub fn set_wait_scope(&self, scope: u8) {
        let scope = if scope == SCOPE_NONE { SCOPE_FIDO } else { scope };
        self.wait_scope.store(scope, Ordering::Release);
    }
"""
slice = ["cargo", "test", "-p", "rsk-device"]
expect = "killed"
note = "a host command claims the button over a live on-panel ceremony: SCOPE_NONE (the panel's) is overwritten with a host scope."

[comutant.BugSoftLockLostOnWarmReset]
status = "patch"
file = "crates/rsk-device/src/ctap.rs"
find = """
        st.restore_pin_lock(boot.lock);
        st.warm_boot = boot.warm;
"""
replace = """
        st.warm_boot = boot.warm;
"""
slice = ["cargo", "test", "-p", "rsk-device"]
expect = "killed"
note = """the clientPIN soft lock dropped across a warm reset a host can
request ungated, freeing the retry budget. Was a measured gap — nothing drove
boot_state() with a live lock — closed by a_warm_boot_carries_the_soft_lock_in."""

# ---- RSKeyStore: the flash layer (rsk-fs) -----------------------------------

[comutant.BugDeleteValueBeforeMeta]
status = "patch"
file = "crates/rsk-fs/src/fs.rs"
find = """
        let meta = self.meta_delete(fid);
        if self.present_bit(fid) {
            self.storage.remove(fid)?;
            self.mark_absent(fid);
            self.write_gen = self.write_gen.wrapping_add(1);
        }
        self.dynamic.retain(|&f| f != fid);
"""
replace = """
        if self.present_bit(fid) {
            self.storage.remove(fid)?;
            self.mark_absent(fid);
            self.write_gen = self.write_gen.wrapping_add(1);
        }
        let meta = self.meta_delete(fid);
        self.dynamic.retain(|&f| f != fid);
"""
slice = ["cargo", "test", "-p", "rsk-fs"]
expect = "killed"
note = """Fs::delete's two backend writes reversed: the value goes first, so a
cut between them leaves metadata naming a file that is gone — the state
delete_landed forbids. The completed end state is identical either way; only
the power-cut oracle's torn sweeps see the order."""

[comutant.BugDeleteMetaOnlyUnderPresent]
status = "patch"
file = "crates/rsk-fs/src/fs.rs"
find = """
        let meta = self.meta_delete(fid);
        if self.present_bit(fid) {
            self.storage.remove(fid)?;
"""
replace = """
        let meta = if self.present_bit(fid) { self.meta_delete(fid) } else { Ok(()) };
        if self.present_bit(fid) {
            self.storage.remove(fid)?;
"""
slice = ["cargo", "test", "-p", "rsk-fs"]
expect = "killed"
note = "the 0x077C databug verbatim: the metadata drop gated on the file's own present bit, so a meta-only file (meta_add with no put) keeps its record through its deletion and it reads back alive."

[comutant.BugDeleteHidesFaultedDrop]
status = "patch"
file = "crates/rsk-fs/src/fs.rs"
find = """
        let meta = self.meta_delete(fid);
        if self.present_bit(fid) {
            self.storage.remove(fid)?;
            self.mark_absent(fid);
            self.write_gen = self.write_gen.wrapping_add(1);
        }
        self.dynamic.retain(|&f| f != fid);
        meta
"""
replace = """
        let _ = self.meta_delete(fid);
        if self.present_bit(fid) {
            self.storage.remove(fid)?;
            self.mark_absent(fid);
            self.write_gen = self.write_gen.wrapping_add(1);
        }
        self.dynamic.retain(|&f| f != fid);
        Ok(())
"""
slice = ["cargo", "test", "-p", "rsk-fs"]
expect = "killed"
note = """the shipped tree before the delete contract was fixed: the failed metadata
drop is swallowed and the value goes anyway, so the caller is told Ok(()) about a
file whose value is gone and whose record still stands. The orphan is not what
this mutant adds -- both arms leave one over a faulted read -- it is that nobody
is told, which is why its target is NoSilentOrphan and not NoOrphanedMetadata."""

[comutant.BugCacheFaultAsAbsent]
status = "patch"
file = "crates/rsk-fs/src/fs.rs"
find = """
        if r.is_none() && self.storage.last_error() {
            return Err(Error::MemoryFatal);
        }
"""
replace = """
        if r.is_none() && self.storage.last_error() {
            self.record(fid, false);
            return Err(Error::MemoryFatal);
        }
"""
slice = ["cargo", "test", "-p", "rsk-fs"]
expect = "killed"
note = "audit run-36: the cache is written on a FAULT, so one transient backend error becomes a decided absence for the rest of the boot and every gate that reads has_data opens. `record_unless_faulted` is `settle` since 0x0995, which owns both halves — what may be cached and what may be returned — so the patch keeps the Err and poisons only the cache, or it would model two defects at once."

[comutant.BugMetaWriteTearsBlob]
status = "unreachable"
evidence = """the switch denies a BACKEND assumption, and no `Storage` in this
tree can tear a write. Enumerated: every fault backend in
`crates/rsk-fs/src/storage.rs` — `MetaStuck`, `ProbeStuck`, `RemoveStuck`,
`TruncatedWalk` — delegates `write` to `RamStorage::write`, which inserts and
answers `Ok(())` and cannot fail, and so does every local backend in
`fs_tests.rs`. The one write-failing medium in the tree,
`crates/rsk-openpgp/src/dying_storage.rs`, refuses ATOMICALLY — before writing —
so it drives `crates/rsk-fs/src/fs.rs:786`'s `?` and cannot leave a partial
blob. So there IS a
line of `rsk-fs` that decides what a failed metadata write costs, and there is no
way to make one TEAR: the assumption belongs to the medium, and
`assurance/platform.toml` PLAT-FLASH-001 is where it lives, pending, owed to a
supply cut on a board. What the model row buys is that the assumption became
DENIABLE — `StoreSolo_BugMetaWriteTearsBlob.cfg` goes RED on
`NoRecordLostToMetaWrite` — rather than prose no configuration can reach."""

[comutant.BugMetaDeleteTearsBlob]
status = "unreachable"
evidence = """the same backend assumption its sibling denies, at
`meta_delete`'s own rewrite (`crates/rsk-fs/src/fs.rs:827`) instead of
`meta_add`'s (`crates/rsk-fs/src/fs.rs:786`), and no `Storage` in this tree
can tear either.
Re-enumerated rather than inherited, because the roster grew: FIVE fault
backends in `crates/rsk-fs/src/storage.rs` now — `MetaStuck`, `ProbeStuck`,
`RemoveStuck`, `TruncatedWalk` and `Undead` — and every one delegates `write`
to `RamStorage::write`, which inserts `data.to_vec()` whole and answers
`Ok(())`; so do all seven local backends in `fs_tests.rs`. The one
write-failing medium, `crates/rsk-openpgp/src/dying_storage.rs`, spends its
budget BEFORE `self.inner.write`, so it drives the `?` and leaves the blob
untouched. A patch could make the rewrite lose records, but only by rewriting
`rebuild_meta` — which models a DIFFERENT defect (a wrong rebuild) from the one
the switch is about (a right rebuild the medium half-lands). What the model row
buys is that PLAT-FLASH-001 is now deniable at BOTH EF_META write sites rather
than one: `StoreSolo_BugMetaDeleteTearsBlob.cfg` goes RED on
`NoRecordLostToMetaWrite`, and it is a bystander's record that goes -- measured
by the arm staying GREEN at a single FID, where the subject's own drop is all
there is to lose."""

[comutant.BugMetaDeleteDropsOnFault]
status = "patch"
file = "crates/rsk-fs/src/fs.rs"
find = """
            None if self.storage.last_error() => return Err(Error::MemoryFatal),
            None => {
                self.mark_absent(EF_META);
"""
replace = """
            None if false => return Err(Error::MemoryFatal),
            None => {
                self.mark_absent(EF_META);
"""
slice = ["cargo", "test", "-p", "rsk-fs"]
expect = "killed"
note = "found by the reverse direction (D2): cargo-mutants reported this guard MISSED on a line the model itself cites, and the model had no MetaDelete fault branch to catch it either. A faulted EF_META read cached as absence loses every record on the NEXT meta_add, which trusts known_absent and rebuilds from empty."

[comutant.BugTruncatedScanDecidesAll]
status = "patch"
file = "crates/rsk-fs/src/fs.rs"
find = """
        if complete {
            decided.fill(0xFF);
        }
"""
replace = """
        let _ = complete;
        decided.fill(0xFF);
"""
slice = ["cargo", "test", "-p", "rsk-fs"]
expect = "killed"
note = "scan decides the whole FID space after a TRUNCATED walk: a committed key the read-fault hid is confirmed absent without ever probing the reliable backend."

[comutant.BugMetaAddDropsOnFault]
status = "patch"
file = "crates/rsk-fs/src/fs.rs"
find = """
            if r.is_none() && self.storage.last_error() {
                return Err(Error::MemoryFatal);
            }
            r.unwrap_or(0).min(scratch.len())
"""
replace = """
            r.unwrap_or(0).min(scratch.len())
"""
slice = ["cargo", "test", "-p", "rsk-fs"]
expect = "killed"
note = """the 0x077C databug's meta half: a FAILED EF_META read treated as an
empty blob, so the rewrite drops every other FID's record. Was a measured GAP —
meta_add_keeps_records_when_ef_meta_unknown drives the UNKNOWN-cache path, and
nothing drove meta_add over a read that FAULTS — closed by
a_faulted_ef_meta_read_never_rebuilds_the_blob_from_empty, re-measured killed."""

# ---- RSKeyAdminSurface: the mask, its carve-out, the rescue gate ------------

[comutant.BugMaskIsCosmetic]
status = "patch"
file = "crates/rsk-sdk/src/applet.rs"
find = """
    fn selectable(&self, i: usize) -> bool {
        i >= 32 || self.enabled & (1 << i) != 0
    }
"""
replace = """
    fn selectable(&self, i: usize) -> bool {
        let _ = i;
        true
    }
"""
slice = ["cargo", "test", "-p", "rsk-sdk", "-p", "rsk-device"]
expect = "killed"
note = "the pre-0x084A tree: USB_ENABLED reported in DeviceInfo while SELECT and dispatch never consulted it — `ykman config usb --disable` disabled nothing."

[comutant.BugAdminGateable]
status = "patch"
file = "crates/rsk-device/src/ccid.rs"
find = """
const APPLET_CAPS: [u16; 8] = [
    0,
    rsk_devconf::CAP_OPENPGP,
    0,
    rsk_devconf::CAP_OATH,
    rsk_devconf::CAP_OTP,
    rsk_devconf::CAP_PIV,
    0,
    rsk_devconf::CAP_FIDO2 | rsk_devconf::CAP_U2F,
];
"""
replace = """
const APPLET_CAPS: [u16; 8] = [
    rsk_devconf::CAP_PIV,
    rsk_devconf::CAP_OPENPGP,
    rsk_devconf::CAP_PIV,
    rsk_devconf::CAP_OATH,
    rsk_devconf::CAP_OTP,
    rsk_devconf::CAP_PIV,
    rsk_devconf::CAP_PIV,
    rsk_devconf::CAP_FIDO2 | rsk_devconf::CAP_U2F,
];
"""
slice = ["cargo", "test", "-p", "rsk-device"]
expect = "killed"
note = "the always-on carve-out removed: management/vendor/rescue gated behind CAP_PIV, so one `--disable PIV` closes the re-enable path and the disable is irreversible."

[comutant.BugPrivilegedOpUngated]
status = "patch"
file = "crates/rsk-rescue/src/lib.rs"
find = """
    fn require_presence(&self, confirm: Confirm<'_>) -> bool {
        self.presence.borrow_mut().request(confirm) == Presence::Confirmed
    }
"""
replace = """
    fn require_presence(&self, confirm: Confirm<'_>) -> bool {
        let _ = confirm;
        true
    }
"""
slice = ["cargo", "test", "-p", "rsk-rescue"]
expect = "killed"
note = "the operator gate removed from every privileged rescue command — keydev signing, cert/config writes, BOOTSEL reboot, fuse burns — so a USB host alone drives them."

[comutant.BugLockWriteResetsCaps]
status = "patch"
file = "crates/rsk-devconf/src/lib.rs"
find = """
    let mut merged = [0u8; DEV_CONF_MERGE_MAX];
    let m = merged_dev_conf(fs, &stripped[..n], &mut merged)?;
    if m > EF_DEV_CONF_MAX {
        return Err(DevConfError::TooLong);
    }
"""
replace = """
    let merged = stripped;
    let m = n;
    if m > EF_DEV_CONF_MAX {
        return Err(DevConfError::TooLong);
    }
"""
slice = ["cargo", "test", "-p", "rsk-devconf"]
expect = "killed"
note = """audit run-35 verbatim: the merge replaced with a verbatim store, so a
`config set-lock-code` write (strips to zero bytes) leaves an EMPTY record that
`read_enabled_caps` reads as SUPPORTED_CAPS — every disabled application
silently re-enabled. Killed outright by the fix's own regression pair,
a_partial_write_config_keeps_the_fields_it_does_not_mention and
trimming_an_over_cap_record_never_evicts_the_enabled_applications_policy — the
first admin batch to measure 4/4 with no gap."""

# ---- RSKeyTrustedDisplay: the confirm ceremony -------------------------------

[comutant.BugPadSubstitutesForCard]
status = "patch"
file = "crates/rsk-fido/src/clientpin.rs"
find = """
    pub fn needs_confirm(self, shows_confirm: bool) -> bool {
        !self.up_collected || shows_confirm
    }
"""
replace = """
    pub fn needs_confirm(self, shows_confirm: bool) -> bool {
        let _ = shows_confirm;
        !self.up_collected
    }
"""
slice = ["cargo", "test", "-p", "rsk-fido"]
expect = "killed"
note = "audit run-28 F1 verbatim: built-in UV deletes the RP card — the pad's collected presence completes the operation and the display build never shows who is being authenticated to."

[comutant.BugPreScreenTouchApproves]
status = "patch"
file = "crates/rsk-display/src/power.rs"
find = """
        match self.touch.read() {
            None => {
                self.touch_armed = true;
                None
            }
            Some(p) if self.touch_armed => Some(p),
            // Still the contact that predates this screen: ignore, stay disarmed.
            Some(_) => None,
        }
"""
replace = """
        match self.touch.read() {
            None => {
                self.touch_armed = true;
                None
            }
            Some(p) => Some(p),
        }
"""
slice = ["cargo", "test", "-p", "rsk-display"]
expect = "killed"
note = """audit run-33's class: the level-not-edges controller reads a finger
already down as a tap on whatever just painted. This patch removes the ambient
chokepoint (armed_touch); the ceremonies carry a SECOND edge of their own
(wait_release_ceremony at card entry), so the layers are two and this measures
the chokepoint's — the ceremony layer's own kill is
a_finger_still_down_when_the_prompt_appears_cannot_approve_it."""

[comutant.BugAnyTapApproves]
status = "patch"
file = "crates/rsk-ui/src/lib.rs"
find = """
    if ALLOW_RECT.contains(p) {
        Some(Button::Allow)
    } else if DENY_RECT.contains(p) {
        Some(Button::Deny)
    } else {
        None
    }
"""
replace = """
    let _ = p;
    Some(Button::Allow)
"""
slice = ["cargo", "test", "-p", "rsk-display", "-p", "rsk-ui"]
expect = "killed"
note = "the Allow/Deny separation collapsed: every tap anywhere on the confirm screen reads as the approval — a deny is an approve and a stray brush above the band signs."

# ---- RSKeyTransport: the CTAPHID frame reassembler --------------------------

[comutant.BugContIgnoresChannel]
status = "patch"
file = "crates/rsk-usb/src/ctaphid.rs"
find = """
            if cid != self.cid {
                return Outcome::Error(cid, ERR_CHANNEL_BUSY);
            }
            let seq = type_byte & !TYPE_INIT;
"""
replace = """
            let seq = type_byte & !TYPE_INIT;
"""
slice = ["cargo", "test", "-p", "rsk-usb"]
expect = "killed"
note = "a continuation on a channel other than the in-progress transaction's is appended instead of refused — one host application's bytes spliced into another's message."

[comutant.BugContIgnoresSeq]
status = "patch"
file = "crates/rsk-usb/src/ctaphid.rs"
find = """
            let seq = type_byte & !TYPE_INIT;
            if seq != self.seq {
                self.in_tx = false;
                return Outcome::Error(cid, ERR_INVALID_SEQ);
            }
"""
replace = """
            let seq = type_byte & !TYPE_INIT;
            let _ = seq;
"""
slice = ["cargo", "test", "-p", "rsk-usb"]
expect = "killed"
note = "an out-of-order continuation is appended instead of aborting the transaction — the reassembler completes a message the host never sent in that order."

[comutant.BugInitLenUnchecked]
status = "patch"
file = "crates/rsk-usb/src/ctaphid.rs"
find = """
            let bcnt = ((f[5] as usize) << 8) | f[6] as usize;
            if bcnt > CTAP_MAX_MESSAGE {
                return Outcome::Error(cid, ERR_INVALID_LEN);
            }
"""
replace = """
            let bcnt = (((f[5] as usize) << 8) | f[6] as usize).min(CTAP_MAX_MESSAGE);
"""
slice = ["cargo", "test", "-p", "rsk-usb"]
expect = "killed"
note = "the INIT length ceiling clamped-away instead of refused: a header declaring more than CTAP_MAX_MESSAGE is silently truncated rather than answered ERR_INVALID_LEN, so bcnt_too_large's error verdict is gone. (A bare removal would index past `msg`; the clamp keeps the patch compiling while still dropping the checked behaviour.)"

# ---- rsk-display: the on-panel PIN gate ------------------------------------

[comutant.BugLocalPinIgnoresBudget]
status = "patch"
file = "crates/rsk-display/src/gates.rs"
find = "                PinScope::Fido => rsk_fido::passkeys::pin_retries_left(&mut fs),"
replace = "                PinScope::Fido => Some(255),"
slice = ["cargo", "test", "-p", "rsk-display"]
expect = "killed"
note = "the on-pad PIN pad ignores the exhausted EF_PIN counter, offering unlimited attempts past the lockout."

[comutant.BugLocalPinKeepsToken]
status = "patch"
file = "crates/rsk-display/src/gates.rs"
find = "        let ends_host_token = scope == PinScope::Fido && retries.is_some_and(|left| left > 0);"
replace = "        let ends_host_token = false;"
slice = ["cargo", "test", "-p", "rsk-display"]
expect = "killed"
note = "E66: the panel's PIN pad no longer ends the host token — a fourth door that leaves a host session live."

# =============================================================================
# The applet batch. Four crates — rsk-piv, rsk-openpgp, rsk-oath, rsk-otp — hold
# 33 773 lines and are the subject of four of the nine modules, and until this
# batch they held ZERO co-refutation patches: the roster's first 43 entries put
# 31 of them in rsk-fido, rsk-device and rsk-fs. So "the applet models are green"
# was fidelity NOBODY HAD MEASURED. These 24 measure it.
#
# The seam/lattice switches often guard the same rule in two applets (`check_ref`
# and `check_pin` are the same floor twice over). An entry may patch SEVERAL
# files — the `[[comutant.X.site]]` array — and BugAdminOpensKeyOps does, over
# four gates in two. Where an entry still leaves a twin unpatched it names that
# twin and why, so the half that is NOT measured is stated rather than implied.
# The rule this file keeps: an entry that reads as covering its switch either
# covers it or says which part it does not.
# =============================================================================

# ---- RSKeyRetryLattice: the retry/recovery ladder ---------------------------
# The module docstring's warning was right and this is its answer: dropping the
# `left == 0` floor alone lets `left - 1` underflow a u8, and the slice then dies
# on the arithmetic rather than on a blocked reference authenticating — a kill for
# the wrong reason, which measures nothing. Rebinding `left` to `max(1)` removes
# the floor AND keeps every `left - 1` below it in range, so what fails is the
# property. Same both-layers shape as the store's revoke-before-write.

[comutant.BugUseWhenBlocked]
status = "patch"
file = "crates/rsk-piv/src/lib.rs"
find = """
    if left == 0 {
        return Sw::PIN_BLOCKED;
    }
"""
replace = """
    let left = left.max(1);
"""
slice = ["cargo", "test", "-p", "rsk-piv"]
expect = "killed"
note = "check_ref's blocked floor removed: an exhausted PIN/PUK compares and authenticates. OpenPGP's twin floor (crates/rsk-openpgp/src/pin.rs:234-238) is the same switch's other half and is not in this entry — one entry, one file."

[comutant.BugWrongDoesNotSpend]
status = "patch"
file = "crates/rsk-openpgp/src/pin.rs"
find = """
    pw[idx] -= 1;
    let remaining = pw[idx];
"""
replace = """
    let remaining = pw[idx];
"""
slice = ["cargo", "test", "-p", "rsk-openpgp"]
expect = "killed"
note = "spend_pin_retry stops charging the attempt: unlimited PW1/PW3/RC guesses at full speed. Deliberately the OpenPGP half of this switch, so the lattice's three entries cover both applets rather than PIV three times; PIV's twin is the `set_retries_left(fs, retry, left - 1)` at crates/rsk-piv/src/lib.rs:1312."

[comutant.BugRecoveryWithoutSecret]
status = "patch"
file = "crates/rsk-piv/src/lib.rs"
find = """
    match check_ref(dev, fs, EF_PUK, RETRY_PUK, puk) {
        Sw::OK => {}
        sw => return sw,
    }
"""
replace = """
    let _ = puk;
"""
slice = ["cargo", "test", "-p", "rsk-piv"]
expect = "killed"
note = "RESET RETRY COUNTER refills the PIN budget without proving the PUK — a budget raised out of nothing. OpenPGP's RC twin is crates/rsk-openpgp/src/pin.rs:901."

# ---- RSKeyAppletPolicies: slot policy, attributes, gates, counter ----------

[comutant.BugPivPolicyIgnored]
status = "patch"
file = "crates/rsk-piv/src/auth.rs"
find = """
        PINPOLICY_ALWAYS => sess.has_pin && sess.pin_fresh,
        _ => sess.has_pin,
"""
replace = """
        PINPOLICY_ALWAYS => true,
        _ => true,
"""
slice = ["cargo", "test", "-p", "rsk-piv"]
expect = "killed"
note = "pin_satisfied answers TRUE for every policy, so a private-key operation runs with no PIN status at all."

[comutant.BugPivAlwaysDoesNotSpend]
status = "patch"
file = "crates/rsk-piv/src/auth.rs"
find = """
        if self.pin_policy != PINPOLICY_NEVER && is_key(self.key_ref) {
            self.sess.pin_fresh = false;
        }
"""
replace = """
        let _ = (self.pin_policy, is_key(self.key_ref));
"""
slice = ["cargo", "test", "-p", "rsk-piv"]
expect = "killed"
note = "spend_pin stops clearing freshness: one VERIFY authorises every later operation at an ALWAYS slot, which is the policy's whole content."

[comutant.BugPgpAttributeKeepsKey]
status = "patch"
file = "crates/rsk-openpgp/src/putdata.rs"
find = "        if current != replacement {"
replace = "        if false && current != replacement {"
slice = ["cargo", "test", "-p", "rsk-openpgp"]
expect = "killed"
note = "a changed algorithm attribute keeps the stored key pair, so the slot's metadata and its key material disagree — the state AttributeChangeInvalidatesTheKey forbids."

[comutant.BugOathCodeIgnored]
status = "patch"
file = "crates/rsk-oath/src/lib.rs"
find = """
    fn cmd_calculate<S: Storage>(&mut self, apdu: &Apdu, fs: &mut Fs<S>, res: &mut ResBuf) -> Sw {
        if !self.validated {
            return Sw::SECURITY_STATUS_NOT_SATISFIED;
        }
"""
replace = """
    fn cmd_calculate<S: Storage>(&mut self, apdu: &Apdu, fs: &mut Fs<S>, res: &mut ResBuf) -> Sw {
"""
slice = ["cargo", "test", "-p", "rsk-oath"]
expect = "killed"
note = "CALCULATE runs without the access-code session: every credential's code is readable off an unvalidated applet."

[comutant.BugOathTouchIgnored]
status = "patch"
file = "crates/rsk-oath/src/lib.rs"
find = """
        if prop & PROP_TOUCH != 0
            && self
                .presence
                .borrow_mut()
                .request(Confirm::titled("Show OATH code?"))
                != Presence::Confirmed
        {
            return Sw::SECURITY_STATUS_NOT_SATISFIED;
        }
"""
replace = """
        let _ = prop;
"""
slice = ["cargo", "test", "-p", "rsk-oath"]
expect = "killed"
note = "a PROP_TOUCH credential computes with no press, and the HOTP counter burns on a request the holder never approved."

[comutant.BugOtpCodeIgnored]
status = "patch"
file = "crates/rsk-otp/src/lib.rs"
find = """
            // Existing config: the host must present its access code.
            if data.len() < CONFIG_SIZE + ACC_CODE_SIZE {
                return Sw::WRONG_LENGTH;
            }
            if !ct_eq(
                &data[CONFIG_SIZE..CONFIG_SIZE + ACC_CODE_SIZE],
                &stored[OFF_ACC_CODE..OFF_ACC_CODE + ACC_CODE_SIZE],
            ) {
                return Sw::SECURITY_STATUS_NOT_SATISFIED;
            }
"""
replace = """
            // Existing config: the host must present its access code.
"""
find2 = """
        if programmed.is_some() {
            if data.len() < CONFIG_SIZE + ACC_CODE_SIZE {
                return Sw::WRONG_LENGTH;
            }
            if !ct_eq(
                &data[CONFIG_SIZE..CONFIG_SIZE + ACC_CODE_SIZE],
                &stored[OFF_ACC_CODE..OFF_ACC_CODE + ACC_CODE_SIZE],
            ) {
                return Sw::SECURITY_STATUS_NOT_SATISFIED;
            }
"""
replace2 = """
        if programmed.is_some() {
"""
slice = ["cargo", "test", "-p", "rsk-otp"]
expect = "killed"
note = "a programmed slot is overwritten (find) or updated (find2) without its six-byte access code. Two anchors because the gate is written twice — the model has ONE OtpMutate switch, so patching one site would leave the other half of the switch unmeasured. The swap path states the same rule a THIRD time, at crates/rsk-otp/src/lib.rs:611-615, and is left unpatched: its gate is a swap's own move/delete rule rather than the configure/update pair OtpMutate models."

[comutant.BugOtpCounterRepeats]
status = "patch"
file = "crates/rsk-otp/src/counter.rs"
find = """
    let new_session = session.wrapping_add(1);
    // Guard the value about to be stored, not the one already stored: at the
    // ceiling `counter + 1` is 0x8000, which sets the reserved high bit and
    // which `boot_use_counter` would then refuse to advance ever again.
    if new_session == 0 && counter < USE_COUNTER_MAX {
        (counter + 1, new_session, true)
    } else {
        (counter, new_session, false)
    }
"""
replace = """
    (counter, session, false)
"""
slice = ["cargo", "test", "-p", "rsk-otp"]
expect = "killed"
note = "E36's shape: NEITHER half of the (use, session) position advances on a press, so two tokens carry the same pair and a validation server cannot tell a replay from a use. The anchor is the whole body, and that is the correction: freezing only the session — the previous patch — is not this defect at all, because the wrap test then reads true on every press and the USE half advances instead, so the position goes on moving. The model switch encodes the frozen PAIR, and the patch has to encode the same arm or the kill is a kill for another reason. Measured rather than argued: the previous patch IS killed too, and its failures read the wrong way round — `the_press_bump_stops_below_the_reserved_high_bit` answers `(6, 0, true)` where `(5, 1, false)` was owed (crates/rsk-otp/src/counter_tests.rs:9), so the USE half advanced AND demanded a flash write, and `a_press_types_nothing_when_it_cannot_persist_the_counter` falls at crates/rsk-otp/src/tests.rs:2176 on `the wrap press did not resume` with the counter run away to 256. Its single repeat is a re-CONFIGURE zeroing a counter that had already reached 3 — a position that had been MOVING. Under THIS patch three tests say `re-typed` instead, and `swap_carries_the_session_counter_with_its_record` goes from green to red."

[comutant.BugOtpPressTypesUnpersisted]
status = "patch"
file = "crates/rsk-otp/src/lib.rs"
find = """
            if !self.put_slot(fs, fid, &rec) {
                return None;
            }
"""
replace = """
"""
slice = ["cargo", "test", "-p", "rsk-otp"]
expect = "killed"
note = "a press types its ticket while the persisted half of the position never moves, and the RAM session rolls anyway — so the press after the wrap reads the old counter back and pairs it with session 0, this power cycle's FIRST position. An in-cycle repeat at the very next press, which is the replay a validation server has no way to tell from a use. ONE host test falls and its direction was read rather than counted: `a_press_types_nothing_when_it_cannot_persist_the_counter` (crates/rsk-otp/src/tests.rs:2124) reports `the press after a refused counter write re-typed Some(([112, 117, 98, 108, 105, 99], 1, 0)), already emitted this power cycle` at crates/rsk-otp/src/tests.rs:2161 — a position REPEATED, not a press mis-refused, which is the half of that test the model switch is about."

[comutant.BugOtpBootKeepsPosition]
status = "patch"
file = "crates/rsk-otp/src/lib.rs"
find = """
        if let Some(counter) = counter::boot_use_counter(stored) {
            rec[CONFIG_SIZE..CONFIG_SIZE + 2].copy_from_slice(&counter.to_be_bytes());
            // The write half of the same window, and it is the more reachable one:
            // nothing has to fault for it to open, and the old record goes on
            // reading perfectly. A retry is all this frame can do — boot has no one
            // to report to, and the press cannot tell a stale counter from a fresh
            // one — so the persistent case stays a residual rather than a guard.
            for _ in 0..BUMP_TRIES {
                if seal::seal_put(dev, fs, rng, KeyFid::new(fid), &rec) {
                    break;
                }
            }
        }
"""
replace = """
        let _ = (counter::boot_use_counter(stored), &rng);
"""
slice = ["cargo", "test", "-p", "rsk-otp"]
expect = "killed"
note = "a cold boot leaves the persisted half where the last power cycle left it while the RAM session restarts at 0, so every pair that cycle typed is typeable again: the counter that exists to make a position unique ACROSS reboots stops crossing the reboot. Three host tests fall and all three say that — `boot_bump_retries_a_refused_write_and_pins_what_it_cannot_close` (crates/rsk-otp/src/tests.rs:2184) reports `one refused boot write re-typed the last cycle's position` with both sides `([112, 117, 98, 108, 105, 99], 1, 0)` at crates/rsk-otp/src/tests.rs:2217, and `power_up_bump_retries_a_slot_the_medium_refused` reads the stored counter back at 0 where 1 was owed (crates/rsk-otp/src/tests.rs:1985). The anchor is the whole `if let` and the replacement keeps the `boot_use_counter` call, so the patch is the WRITE and nothing else: dropping the call too left the function dead, and a slice run under -D warnings would then score build-broke instead of a kill."

[comutant.BugOtpSwapKeepsSession]
status = "patch"
file = "crates/rsk-otp/src/lib.rs"
find = """
        self.session_counter.swap(
            (fid1 - EF_OTP_SLOT1) as usize,
            (fid2 - EF_OTP_SLOT1) as usize,
        );
"""
replace = """
"""
slice = ["cargo", "test", "-p", "rsk-otp"]
expect = "killed"
note = "SLOT_SWAP moves the record and its persisted use counter and leaves the RAM session counter behind, so the record is re-paired with a session counter used FEWER times: the replay position goes BACKWARDS and the swapped slot re-types a pair already emitted this power cycle. ONE host test falls, `swap_carries_the_session_counter_with_its_record` (crates/rsk-otp/src/tests.rs:1414), through the `press_once` helper at crates/rsk-otp/src/tests.rs:1405: `slot 2 re-typed the replay position ([97, 97, 97, 97, 97, 97], 1, 1), already emitted this power cycle` — a repeat, which is what OtpCounterNeverRepeats is about, and not a swap refused or a record mislaid."

# ---- RSKeyAppletSeams: what a status costs, and who may hold it ------------
# The old exclusion said these are "exercised at the wire, not by a cargo test on
# one function". Half true and it hid the useful half: the wire is where the
# STATUS LIFETIME shows, but every one of these switches has a named function
# that decides it, and that function has host tests. What the batch measures is
# exactly whether those tests fail when the decision is taken back out.

[comutant.BugSelectKeepsOtherApplet]
status = "patch"
file = "crates/rsk-sdk/src/applet.rs"
find = """
                    if let Some(c) = self.current
                        && c != i
                    {
                        applets[c].deselect(ctx);
                    }
"""
replace = ""
slice = ["cargo", "test", "-p", "rsk-sdk"]
expect = "killed"
note = "selecting a different AID no longer deselects the applet that was current, so its verified PIN survives into another applet's session."

[comutant.BugReselectResetsStatus]
status = "patch"
file = "crates/rsk-piv/src/lib.rs"
find = """
        if !reselect {
            self.sess.reset();
        }
"""
replace = """
        let _ = reselect;
        self.sess.reset();
"""
slice = ["cargo", "test", "-p", "rsk-piv"]
expect = "killed"
note = "637ed98 taken back out on the PIV half: every SELECT resets, including the re-SELECT SP 800-73-4 pt2 §3.1.1 says shall leave every indicator unchanged. OpenPGP's twin is crates/rsk-openpgp/src/lib.rs:355-358."

[comutant.BugCardResetKeepsStatus]
status = "unreachable"
evidence = """the applets' deselect is redundant with their own fresh SELECT, so
removing it from the ICC power transition changes nothing observable. Three
applets carry an access status and implement deselect — crates/rsk-piv/src/lib.rs:390-392
(sess.reset), crates/rsk-openpgp/src/lib.rs:337-339 (reset_session), crates/rsk-oath/src/lib.rs:1222-1229
(validated + otp_pin_verified) — and all three ALSO re-lock in select: piv and
openpgp on `if !reselect`, oath by recomputing `validated = !code_set` and clearing
otp_pin_verified unconditionally (crates/rsk-oath/src/lib.rs:1262-1263). After Dispatcher::reset_card
takes `self.current` (crates/rsk-sdk/src/applet.rs:227) every route back to an applet is a
`select(reselect = false)`, so no reachable command sees an inherited status.
The first patch here neutered the WHOLE reset_card call and scored a false kill:
the only test that fell was `a_card_reset_drops_the_selection`, which asserts the
selection and never verifies a PIN. The faithful mutant — an empty applet slice,
selection still dropped — is GREEN over 68 tests. What keeps this argument true is
the selection half, and that IS asserted, here and by rsk-sdk's
`reset_card_clears_selection_and_security_status`."""

[comutant.BugAdminOpensKeyOps]
status = "patch"
slice = ["cargo", "test", "-p", "rsk-openpgp"]
expect = "killed"
note = "e5da38b taken back out at every gate the switch covers: the admin PIN signs, deciphers, authenticates and runs the AES PSO. PgpKeyOpGuard is quantified over r in {pw1, pw2} and its comment names PSO:CDS (§7.2.10, PW1 no. 81), PSO:DECIPHER and INTERNAL AUTHENTICATE (§7.2.11 and §7.2.13, PW1 no. 82); the AES arm follows its siblings by the tree's own decision. A YubiKey 5.7.4 answers 6982 to PW3 alone on all of them. Four sites in two files — the first version of this entry patched PSO:CDS alone and named the other three in prose, which measured a quarter of the switch while reading as covering it. Each gate was then measured ALONE and each is killed on its own (2/1/1/1 failing tests), so no site rides on a sibling's coverage; the combined run shows two failures rather than five only because the same matrix tests span several gates."

[[comutant.BugAdminOpensKeyOps.site]]
file = "crates/rsk-openpgp/src/pso.rs"
find = """
            if !sess.has_pw1 {
                return Err(Sw::SECURITY_STATUS_NOT_SATISFIED);
            }
            (EF_ALGO_PRIV1, EF_PK_SIG)
"""
replace = """
            if !sess.has_pw1 && !sess.has_pw3 {
                return Err(Sw::SECURITY_STATUS_NOT_SATISFIED);
            }
            (EF_ALGO_PRIV1, EF_PK_SIG)
"""

[[comutant.BugAdminOpensKeyOps.site]]
file = "crates/rsk-openpgp/src/pso.rs"
find = """
            if !sess.has_pw2 {
                return Err(Sw::SECURITY_STATUS_NOT_SATISFIED);
            }
            (sess.algo_dec, sess.pk_dec)
"""
replace = """
            if !sess.has_pw2 && !sess.has_pw3 {
                return Err(Sw::SECURITY_STATUS_NOT_SATISFIED);
            }
            (sess.algo_dec, sess.pk_dec)
"""

[[comutant.BugAdminOpensKeyOps.site]]
file = "crates/rsk-openpgp/src/pso.rs"
find = """
        if !sess.has_pw2 {
            return Err(Sw::SECURITY_STATUS_NOT_SATISFIED);
        }
        check_uif(fs, EF_UIF_DEC, presence)?;
"""
replace = """
        if !sess.has_pw2 && !sess.has_pw3 {
            return Err(Sw::SECURITY_STATUS_NOT_SATISFIED);
        }
        check_uif(fs, EF_UIF_DEC, presence)?;
"""

[[comutant.BugAdminOpensKeyOps.site]]
file = "crates/rsk-openpgp/src/internalaut.rs"
find = """
    if !sess.has_pw2 {
        return Err(Sw::SECURITY_STATUS_NOT_SATISFIED);
    }
"""
replace = """
    if !sess.has_pw2 && !sess.has_pw3 {
        return Err(Sw::SECURITY_STATUS_NOT_SATISFIED);
    }
"""

[comutant.BugFailedChangeKeepsStatus]
status = "patch"
file = "crates/rsk-oath/src/lib.rs"
find = """
        self.validated = false;
        self.otp_pin_verified = false;
        // Same anti-bruteforce gate as VERIFY: refuse at the counter floor. After a
"""
replace = """
        // Same anti-bruteforce gate as VERIFY: refuse at the counter floor. After a
"""
slice = ["cargo", "test", "-p", "rsk-oath"]
expect = "killed"
note = "aa47867 taken back out: a failed OTP-PIN CHANGE leaves the standing authentication up, so the budget can be burned through a door that never closes."

[comutant.BugPinFreshNotSpent]
status = "patch"
file = "crates/rsk-piv/src/auth.rs"
find = "        PINPOLICY_ALWAYS => sess.has_pin && sess.pin_fresh,"
replace = "        PINPOLICY_ALWAYS => sess.has_pin,"
find2 = """
        if self.pin_policy != PINPOLICY_NEVER && is_key(self.key_ref) {
            self.sess.pin_fresh = false;
        }
"""
replace2 = """
        let _ = (self.pin_policy, is_key(self.key_ref));
"""
slice = ["cargo", "test", "-p", "rsk-piv"]
expect = "killed"
note = "both layers, as the model's switch has them: the ALWAYS arm stops reading freshness AND the operation stops spending it. Its policy sibling BugPivAlwaysDoesNotSpend takes only the spend, so the two are not the same patch twice."

[comutant.BugPinFreshOutlivesPin]
status = "unreachable"
evidence = "the clamp is unobservable in today's Rust. `pin_fresh` has exactly one reader — crates/rsk-piv/src/auth.rs:63, `PINPOLICY_ALWAYS => sess.has_pin && sess.pin_fresh` — and it is CONJOINED with the status it refines, so a freshness that outlives `has_pin` authorises nothing: the guard is already false on the other half. `Session::set_pin` (crates/rsk-piv/src/lib.rs:183-186) is the only writer that sets it TRUE, and `GenAuth::spend_pin` (crates/rsk-piv/src/auth.rs:115-119) only ever sets it false. The first patch here scored a false kill by taking `self.pin_fresh = verified` out of `set_pin`, which is the INVERSE defect — it makes freshness unreachable rather than immortal, fail-closed rather than fail-open, and all 11 failures were 9000-expected-got-6982. The faithful mutant (`if verified { self.pin_fresh = true; }` — grant kept, clamp dropped) measures GREEN over 130 tests. The model keeps `pfresh` as a second requirement-side copy precisely to expose a split the Rust does not have."

[comutant.BugSigPinNotSpent]
status = "patch"
file = "crates/rsk-openpgp/src/keys.rs"
find = """
    if one_shot {
        sess.has_pw1 = false;
    }
"""
replace = """
    if one_shot {
        let _ = &sess;
    }
"""
slice = ["cargo", "test", "-p", "rsk-openpgp"]
expect = "killed"
note = "inc_sig_count stops clearing PW1 under the one-shot PW status, so one VERIFY signs for ever — the one-signature rule with nothing enforcing it."

[comutant.BugUserStatusOpensAdmin]
status = "patch"
file = "crates/rsk-openpgp/src/putdata.rs"
find = """
    if fid == EF_PRIV_DO_1 || fid == EF_PRIV_DO_3 {
        sess.has_pw2
    } else {
        sess.has_pw3
    }
"""
replace = """
    if fid == EF_PRIV_DO_1 || fid == EF_PRIV_DO_3 {
        sess.has_pw2
    } else {
        sess.has_pw3 || sess.has_pw1 || sess.has_pw2
    }
"""
slice = ["cargo", "test", "-p", "rsk-openpgp"]
expect = "killed"
note = "the converse of BugAdminOpensKeyOps: a USER status opens PUT DATA's administrative surface. PIV states the same rule at seven separate 9B management-key gates (crates/rsk-piv/src/lib.rs:288, 594, 676, 804, 1053, 1113 and crates/rsk-piv/src/keygen.rs:600), none of them patched here."

[comutant.BugRefusedValidateGrants]
status = "patch"
file = "crates/rsk-oath/src/lib.rs"
find = """
        if !ct_eq(resp, &mac[..size]) {
            return Sw::WRONG_DATA;
        }
"""
replace = """
        if !ct_eq(resp, &mac[..size]) {
            self.validated = true;
            return Sw::WRONG_DATA;
        }
"""
slice = ["cargo", "test", "-p", "rsk-oath"]
expect = "killed"
note = "a refused VALIDATE grants the unlock while still answering 6A80 — the status word says no and the applet opens. The refusal rule exempts VALIDATE entirely, so nothing but a direct test can tell the two apart."

[comutant.BugPwStatusIgnoresAdmin]
status = "patch"
file = "crates/rsk-openpgp/src/putdata.rs"
find = """
    if fid == EF_PRIV_DO_1 || fid == EF_PRIV_DO_3 {
        sess.has_pw2
    } else {
        sess.has_pw3
    }
"""
replace = """
    if fid == EF_PRIV_DO_1 || fid == EF_PRIV_DO_3 {
        sess.has_pw2
    } else if fid == EF_PW_STATUS {
        sess.has_pw3 || sess.has_pw1 || sess.has_pw2
    } else {
        sess.has_pw3
    }
"""
find2 = """
pub fn put_pw_status<S: Storage>(fs: &mut Fs<S>, sess: &Session, data: &[u8]) -> Sw {
    if !sess.has_pw3 {
"""
replace2 = """
pub fn put_pw_status<S: Storage>(fs: &mut Fs<S>, sess: &Session, data: &[u8]) -> Sw {
    if !sess.has_pw3 && !sess.has_pw1 && !sess.has_pw2 {
"""
slice = ["cargo", "test", "-p", "rsk-openpgp"]
expect = "killed"
note = "PUT DATA C4 taken off PW3: a user status clears the one-shot flag and then signs for ever on a single PW1 VERIFY — BugSigPinNotSpent's requirement removed from underneath rather than through the door it watches. BOTH layers, because C4 is double-gated: the model's own comment names `write_authorized` (called at crates/rsk-openpgp/src/lib.rs:260-262) as the guard, and widening only put_pw_status's inner restatement left the wire command still answering 6982 — a kill that measured a defence in depth rather than the modelled defect. Its sibling BugUserStatusOpensAdmin widens the same function's whole else-arm; this one widens the C4 case alone, which is the door PgpSetPwStatus watches."

[comutant.BugPivChangeResetsStatus]
status = "patch"
file = "crates/rsk-piv/src/lib.rs"
find = """
        change_reference(dev, fs, which, old, new)
    }
"""
replace = """
        let sw = change_reference(dev, fs, which, old, new);
        self.sess.set_pin(false);
        sw
    }
"""
slice = ["cargo", "test", "-p", "rsk-piv"]
expect = "killed"
note = "an EXEMPT refusal taken back out: CHANGE REFERENCE DATA clears the standing status. SP 800-73-4 pt2 3.2.2/3.2.3 say it does not and a YubiKey 5.7.4 was measured keeping it, so this is a deliberate divergence the tree defends — the mutant asks whether anything would notice it going."

[comutant.BugRefusedValidateDropsUnlock]
status = "patch"
file = "crates/rsk-oath/src/lib.rs"
find = """
        if !ct_eq(resp, &mac[..size]) {
            return Sw::WRONG_DATA;
        }
"""
replace = """
        if !ct_eq(resp, &mac[..size]) {
            self.validated = false;
            return Sw::WRONG_DATA;
        }
"""
slice = ["cargo", "test", "-p", "rsk-oath"]
expect = "killed"
note = "the second exempt refusal: a refused VALIDATE drops the standing unlock. A MAC challenge-response has no retry counter for a refusal to protect, so dropping it costs availability and buys nothing."

[comutant.BugRemoveCodeUnvalidated]
status = "patch"
file = "crates/rsk-oath/src/lib.rs"
find = """
    fn cmd_set_code<S: Storage>(&mut self, apdu: &Apdu, fs: &mut Fs<S>) -> Sw {
        if !self.validated {
            return Sw::SECURITY_STATUS_NOT_SATISFIED;
        }
        let data = &apdu.data[..apdu.nc];
"""
replace = """
    fn cmd_set_code<S: Storage>(&mut self, apdu: &Apdu, fs: &mut Fs<S>) -> Sw {
        let data = &apdu.data[..apdu.nc];
        let removing = matches!(parse_exact(data, [TAG_KEY]), Some([key]) if key.is_empty());
        if !self.validated && !removing {
            return Sw::SECURITY_STATUS_NOT_SATISFIED;
        }
"""
slice = ["cargo", "test", "-p", "rsk-oath"]
expect = "killed"
note = "SEC-SEAM-006's own defect: the `73 00` removal reaches past the validated gate, so anyone who can select the applet unlocks it for good. The model could not see this for two revisions — the exemption fired exactly on the state the removal creates — and closing it needed a recorder at the STEP."

# ---- batch 6: the token-less makeCredential carve-out ----------------------
#
# Both patches land in the SAME function, and that is the point of them: item 9's
# rescan put `crates/rsk-fido/src/makecredential.rs::enforce_pin` in
# `assurance/token_refinement.toml` twice — as the `UseMc` volatile writer and as
# its outcome producer — and the model edge those two mutants falsify is the
# `None` arm of exactly that function. The anchors below are what makes the tie
# checkable rather than asserted: `--lint` resolves each against the tree on
# every `check.sh` run, so the model's claim about who owns this gate fails
# loudly the day the code moves out from under it.

[comutant.BugUvNotRqdIgnoresRk]
status = "patch"
file = "crates/rsk-fido/src/makecredential.rs"
find = """
            if pin_set && req.rk {
                return Err(CtapError::PuatRequired);
            }
"""
replace = """
"""
slice = ["cargo", "test", "-p", "rsk-fido"]
expect = "killed"
note = "makeCredUvNotRqd widened to every credential: §6.1.2 step 10 carves the token out for a NON-discoverable credential only, and without `req.rk` a resident one is created with a PIN set and no token. `crates/rsk-fido/src/makecredential.rs:589-591` — the direction matters and BOTH levels were read for it rather than for their colour: TLC's counterexample has RegisterStart firing at `pin.set` with no token, and the host test reports `Ok(770)` against `Err(PuatRequired)` — a credential minted, not a refusal wearing a different code. It said `Err(Other)` until the test's `out` was sized for a served response; the encoder ran out of room AFTER the gate had already let it through, which is the shape of a kill that reads like the inverse defect."

[comutant.BugTokenlessIgnoresAlwaysUv]
status = "patch"
file = "crates/rsk-fido/src/makecredential.rs"
find = """
            if always_uv {
                return Err(CtapError::PuatRequired);
            }
"""
replace = """
"""
slice = ["cargo", "test", "-p", "rsk-fido"]
expect = "killed"
note = "the other half, §6.1.2 steps 6.2/6.4: alwaysUv with no way to verify refuses whatever `rk` says. Dropped, a token-less request falls through to `Ok(UvOutcome::NONE)` and alwaysUv is served on the touch alone. `crates/rsk-fido/src/makecredential.rs:583-585`. Two switches and not one because a single widened guard cannot say which half was load-bearing — the same split TraceSecurity draws with MutateUvNotRqd / MutateAlwaysUvArm. Two host tests fall, both reporting `Ok(802)` against `Err(PuatRequired)`, so this one is served and not merely mis-refused either."

# ---- batch 7: the gate the module did not carry ----------------------------

[comutant.BugForceChangeIgnored]
status = "patch"
file = "crates/rsk-fido/src/clientpin.rs"
find = """
    if force_change_pending(ctx) {
        return Err(if req.subcommand == CP_GET_PIN_TOKEN {
            CtapError::PinInvalid
        } else {
            CtapError::PinPolicyViolation
        });
    }
"""
replace = """
"""
slice = ["cargo", "test", "-p", "rsk-fido"]
expect = "killed"
note = "EF_MINPINLEN[1], and the only mutant here whose MODEL half had to be built before the code half could be compared: `RSKeySecurityState` carried three conjuncts where `clientpin.rs` has four, so until `gate.forceChange` existed there was nothing for this patch to be the twin of (PLAT-MODEL-010). The direction was read at both levels rather than taken from the colour. TLC: setPIN, a token with `acfg`, ConfigOp sets the flag, and GetPinToken fires AGAIN at depth 8 — a token ISSUED over a standing gate, not a refusal wearing another code. The host tests fall the same way and were read rather than counted: FOUR fall, and both named ones report `Ok(52)` — a 52-byte token response SERVED — against `Err(PinInvalid)` (`forced_pin_change_blocks_tokens_until_change_pin`, crates/rsk-fido/src/clientpin_tests.rs:1251) and `Err(PinPolicyViolation)` (`forced_pin_change_0x09_is_policy_violation`, crates/rsk-fido/src/clientpin_tests.rs:1315). The other two are the faulted-probe twins of the same door. The UV door at crates/rsk-fido/src/clientpin.rs:493-495 is a SECOND site this patch leaves standing, so the kill is attributed to the PIN doors alone."

# ---- batch 8: the boot family, and the exclusion that covered it -------------
#
# `BootMut_*` matched no `PREFIXES` entry, so `roster()` did not see it and the
# lint was green in BOTH directions over three switches nobody had to register.
# The stated reason was that two of the three sites live in `firmware/`, which
# `cargo test` cannot reach. One did: the scratch-word carry's model conjunct is
# `Boot`'s `lock' = recorded`, and that assignment is `restore_pin_lock` in
# `rsk-fido`. The marker-after-lap order genuinely was in `firmware/src/main.rs`
# — where a patch scores `build-broke` and never a kill — so it was lifted into
# `rsk_fs::run_at_rest_lap` first, with the host test the patch must break.
# All three or none: the prefix admits the whole family at once.

[comutant.BugRekeyKeepsTheMarker]
status = "patch"
file = "crates/rsk-fs/src/lib.rs"
find = """
pub fn request_rescrub<S: Storage>(fs: &mut Fs<S>) -> Result<()> {
    let _ = fs.delete(EF_HARDENED);
    // Not `delete`'s own answer: it reports the METADATA drop (EF_HARDENED, a one-byte
    // flag, keeps none) and answers `Ok` where the present bit is clear over a live
    // marker — what a read-fault-truncated `Fs::scan` leaves. Ask the lap's own gate.
    let answer = match fs.try_has_data(EF_HARDENED) {
        Ok(true) => Err(Error::MemoryFatal),
        Ok(false) => Ok(()),
        Err(e) => Err(e),
    };
    // Both arms, and before the caller can drop it: an unreadable probe is a re-arm
    // that cannot be shown to have landed, which is the same medium fault the wipe
    // sites discard.
    if answer.is_err() {
        fs.note_rescrub_refused();
    }
    answer
}
"""
replace = """
pub fn request_rescrub<S: Storage>(fs: &mut Fs<S>) -> Result<()> {
    let _ = fs;
    Ok(())
}
"""
slice = ["cargo", "test", "-p", "rsk-fs"]
expect = "killed"
note = "audit run-35, shipped at four of five re-key sites: a lazy re-key — or a lazy DELETE, which appends a tombstone rather than erasing — leaves EF_HARDENED standing over the weak copy it just superseded, so no future boot ever scrubs it. The DEFINITION is patched, not the call sites, because it dominates all of them — patching sites would leave the oath and openpgp halves unmeasured and over-anchor the entry to an enumeration that grows. The anchor is the WHOLE body because the re-arm outgrew its `delete`: the read-back has to go with it, since the defect is a re-arm that keeps the marker AND still answers `Ok` — leave `try_has_data` standing and the patch models the REFUSAL instead, which kills through `expect(\"a healthy medium re-arms and says so\")` and says the wrong half. The `Fs::rescrub_refused` latch goes with it for the same reason: it is set from this body's `Err` arm, so a patch that kept it would leave a stuck medium still reported and only half the defect modelled. Direction read at both levels: TLC's counterexample is `LazyRekey` at `marker = TRUE` reaching `weak = 1`, a marker standing over a leftover, and the host failure is `requesting_a_rescrub_clears_the_hardened_marker` (crates/rsk-fs/src/fs_tests.rs:385) reporting `a rescrub request must clear the marker, or the lap never runs again` — the marker SURVIVED, not a spurious clear."

[comutant.BugMarkerBeforeScrub]
status = "patch"
file = "crates/rsk-fs/src/lib.rs"
find = """
    if fs.compact().is_ok() {
        let _ = fs.put(EF_HARDENED, &[1u8]);
    }
"""
replace = """
    let _ = fs.compact();
    let _ = fs.put(EF_HARDENED, &[1u8]);
"""
slice = ["cargo", "test", "-p", "rsk-fs"]
expect = "killed"
note = "the `compact().is_ok()` short-circuit dropped: a torn lap claims completion and the weak copies it left ride under a set marker forever. This is the switch the module's exclusion was really about, and it had no code twin until the order left `firmware/src/main.rs` for `rsk_fs::run_at_rest_lap` — a firmware-compiling patch is classified `build-broke`, which is not a kill. Direction: the host failure is `the_at_rest_lap_writes_its_marker_only_after_a_completed_scrub` (crates/rsk-fs/src/fs_tests.rs:505) reporting `a torn lap claimed completion, so no later boot ever scrubs what it left` — read off the MEDIUM past `Fs`'s present cache, and the marker is PRESENT over a failed lap rather than absent after a good one."

[comutant.BugPartialLockCarry]
status = "patch"
file = "crates/rsk-fido/src/state.rs"
find = """
        self.needs_power_cycle = lock.engaged;
        self.new_pin_mismatches = lock.mismatches;
"""
replace = """
        self.needs_power_cycle = lock.engaged;
        self.new_pin_mismatches = if lock.engaged { lock.mismatches } else { 0 };
"""
slice = ["cargo", "test", "-p", "rsk-fido"]
expect = "killed"
note = "the half carry `firmware/src/pin_lock.rs:18-21` names as the laundering: the engaged flag rides the warm reset and the mismatch batch is dropped, so a host stops at two wrong PINs, reboots, and restarts the batch. The security module's BugSoftLockLostOnWarmReset drops BOTH halves; this drops one, which that mutant cannot express. CONDITIONAL on `engaged` deliberately, and re-measured for it: the model's switch is `lock' = IF BugPartialLockCarry /\\ recorded = \"batch\" THEN \"clear\" ELSE recorded`, so it diverges only at `\"batch\"`. An unconditional `= 0` also fires at `\"engaged\"`, where the model says the restore is CORRECT, and kills on crates/rsk-fido/src/state_tests.rs:119 with `left: PinLock { engaged: true, mismatches: 0 }` against `right: ... mismatches: 3 }` — an assertion whose model image is unchanged behaviour. Conditional, the kill lands where the counterexample does: `pin_lock_round_trips_and_boot_leaves_it_alone` fails at crates/rsk-fido/src/state_tests.rs:134 with `left: 0 right: 2` — a live batch of two ERASED by the restore, which is the laundering, not a batch a restore invented. The slice stays `-p rsk-fido`, and that is a measurement and not an argument: widening it to `-p rsk-fido -p rsk-device` CANNOT move the verdict. `run_one` runs a slice as ONE command and reads `gap` off `returncode == 0`, so a crate added to a slice that already fails is unobservable — driven as a 2x2, slice narrow/wide against the rsk-device case present/absent, every arm answered rc 101 and `killed`. What widening does change is which failure the row RECORDS. No slice in this file carries `--no-fail-fast`, cargo schedules `rsk_device` first, and with the case present the wide row runs that one binary and stops, recording `test result: FAILED. 78 passed; 1 failed` — the rsk-fido kill this entry is argued from is then not measured by this row at all. Narrow, the recorded detail is that kill: `test result: FAILED. 668 passed; 1 failed`. The boot WIRING that calls the patched definition — crates/rsk-device/src/ctap.rs:127-128, in `AppletHandler::new` — is a second site, and what holds it is the ordinary rsk-device host row rather than this slice. `a_warm_boot_carries_a_sub_limit_batch_in` is the case that holds it, and the defect only that case catches is a CONDITIONAL wiring rather than this patch: guard the call with `if boot.lock.engaged` and it falls on `the sub-limit batch was erased`, `left: 0 right: 2`, rc 101 and `78 passed; 1 failed`; delete the case and the same defect leaves `cargo test -p rsk-device --target aarch64-apple-darwin` at rc 0. Its twin `a_warm_boot_carries_the_soft_lock_in` (crates/rsk-device/src/ctap_tests.rs:101-123) drives only `engaged: true, mismatches: 3` — the arm such a guard leaves alone — and stays green over it. The `78 passed; 0 failed` this entry used to record for that command under the patch is HEAD~1's shape, before the case existed: at HEAD the same command is rc 101 and `78 passed; 1 failed`. Control, measured rather than argued: the mirror `if lock.engaged { 0 } else { lock.mismatches }` is a real defect at the same statement, and under it the new case stays GREEN while the `mismatches: 3` twin falls at crates/rsk-device/src/ctap_tests.rs:122 — the specificity check and the proof the control compiled in, in one run. Direction read, not taken from the colour."


# ---- batch 9: the OATH default-open family ---------------------------------
# `validated = !code_set` is one rule stated in five places in the model and in
# two observable places in the code -- `new()`'s defaults and `select()`'s
# recompute -- so three of the five twins below land on a line a sibling also
# names, in the other direction. Each entry says which half of the model clause
# it is the twin of; where no host test falls, the row records a GAP rather than
# an argument.

[comutant.BugFreshCardOpensOtpPin]
status = "patch"
file = "crates/rsk-oath/src/lib.rs"
find = """
            validated: true,
            otp_pin_verified: false,
"""
replace = """
            validated: true,
            otp_pin_verified: true,
"""
slice = ["cargo", "test", "-p", "rsk-oath"]
expect = "killed"
note = "`Session::new()`'s second default flipped: a fresh applet answers OTP-PIN-verified. The model's twin is `Init`, whose function constructor hands out exactly ONE reference, and this hands out the sibling. Direction read, not taken from the colour: 133 passed, 1 failed, and the assertion is `crates/rsk-oath/src/otp_pin_tests.rs:66` — `assert!(!safe_open(...), \"closed before any PIN\")` — the password safe SERVED before any PIN was presented, which is a status handed out rather than one lost."

[comutant.BugDeselectKeepsOathUnlock]
status = "patch"
file = "crates/rsk-oath/src/lib.rs"
find = """
        self.validated = !code_set;
        self.otp_pin_verified = false;
"""
replace = """
        self.validated = true;
        self.otp_pin_verified = false;
"""
slice = ["cargo", "test", "-p", "rsk-oath"]
expect = "killed"
note = "`select`'s recompute pinned open: a PROVISIONED applet comes back unlocked. This is the observable half of the model's `ClearedFor`, which is applied where OATH is or becomes the current applet; `deselect`'s own clear is the other half and has its own entry under BugResetKeepsOathUnlock, where nothing is selected at all. 122 passed, 12 failed. The witness is `crates/rsk-oath/src/set_code_tests.rs:45` — \"the challenge and the gate disagree about whether a code is set\": SELECT still offers the challenge that says a code is installed and LIST is served anyway. `crates/rsk-oath/src/code_tests.rs:154` says the same as a control, \"a fresh SELECT on a code-locked applet is unvalidated\". Both fell in the SERVED direction; none said a command was refused."

[comutant.BugResetKeepsOathUnlock]
status = "patch"
file = "crates/rsk-oath/src/lib.rs"
find = """
        self.validated = false;
        self.otp_pin_verified = false;
        self.chain = Chain::None;
"""
replace = """
        self.chain = Chain::None;
"""
slice = ["cargo", "test", "-p", "rsk-oath"]
expect = "killed"
note = "`deselect`'s clear removed, which is the path the model writes as `AllCleared`: the applet loses the selection outright (`Dispatcher::reset_card`, and a power cycle rebuilding from `new()`) rather than losing and regaining it. It was expected to be `unreachable` for BugCardResetKeepsStatus's reason — every route back is a `select` that recomputes — and the measurement REFUTED that: `set_code_tests::a_deselect_drops_the_validate_unlock` calls `Applet::deselect` directly and asserts the next LIST, so the redundancy argument does not cover it. 133 passed, 1 failed, at `crates/rsk-oath/src/set_code_tests.rs:322` — \"the unlock must not outlive the selection that earned it\", which is `NoStatusOutsideItsSelection` written in Rust, and it fell because LIST was SERVED after the deselect."

[comutant.BugWipeWithoutItsReboot]
status = "patch"
file = "crates/rsk-display/src/pin.rs"
find = """
            self.hooks.request_reboot(false);
            self.end_modal();
            return true;
"""
replace = """
            self.end_modal();
            return true;
"""
slice = ["cargo", "test", "-p", "rsk-display"]
expect = "gap"
note = "GAP, and the row exists to say so. The trusted-display factory wipe queues the reboot that rebuilds every applet from `new()` — the model folds the two into the single `FactoryWipe` step — and with `request_reboot` dropped all 127 `rsk-display` tests stay GREEN: nothing asserts that a completed wipe asks for the reboot, so a wipe that left every in-RAM status standing over freshly-defaulted verifiers would ship. The CCID half is `firmware/src/worker.rs:397-398`, which no host slice reaches at all — a firmware-compiling patch is classified `build-broke`, not a kill — so the display path is the only half a slice can measure, and it is the half that is open."

[comutant.BugCodelessOathIsAStatus]
status = "patch"
file = "crates/rsk-oath/src/lib.rs"
find = """
        self.validated = !code_set;
        self.otp_pin_verified = false;
"""
replace = """
        self.validated = false;
        self.otp_pin_verified = false;
"""
slice = ["cargo", "test", "-p", "rsk-oath"]
expect = "killed"
note = "The exemption removed from the IMPLEMENTATION, where the model switch removes it from the requirement: `select` locks a code-less applet instead of computing `!code_set`. Its model twin is the only one in this batch that edits an invariant rather than a step, so the code twin is the same fact's other side — if `NoStatusOutsideItsSelection` may not exempt a code-less OATH, a code-less OATH must not be open, and the tests say what that costs. 118 passed, 16 failed. Direction read: `crates/rsk-oath/src/tests.rs:1328` is `lock_with_code`'s own `assert_eq!(SET CODE, Sw::OK)` failing — the helper cannot install a code because the applet it must install it on is now locked — and `crates/rsk-oath/src/increasing_tests.rs:36` is \"a refusal must read as 6A80\" over a CALCULATE answered 6982. Every one fell in the REFUSED direction, which is the defect this models; none said something was served."
