\* SPDX-License-Identifier: AGPL-3.0-only
\* Copyright (C) 2026 RS-Key contributors
\*
\* THE SCOPE EACH MODULE'S OWN MUTANTS NEED, so that a configuration cannot go
\* green by being too SMALL TO EXPRESS the defect it claims to catch.
\*
\* `floors.txt` asks whether a run shrank. This asks the question one level out,
\* and it is not hypothetical: THREE of the fifty-three mutants measured here are
\* GREEN one element below the shipped scope and RED at it.
\*
\*   BugCmWalkIgnoresChannel  needs a second CTAPHID channel -- the credential
\*                            walk it hijacks has to belong to someone else
\*   BugContIgnoresChannel    needs a second channel for the same reason one
\*                            layer down, at the reassembler
\*   BugMetaAddDropsOnFault   needs a second FID: one to `meta_add`, one whose
\*                            record must survive it
\*
\* Until this file nothing stopped those domains being narrowed to a singleton,
\* and all three rows would have stayed GREEN over a live defect.
\*
\* A FOURTH joined that class after the sweep above and was driven against the
\* minimum rather than assumed to fit: `BugMetaDeleteTearsBlob`, `meta_delete`'s
\* torn rewrite. Its violation is a BYSTANDER's record going, so at one FID it
\* explores its whole space without a counterexample and falls at two -- which is
\* also what separates it from its inverse, an arm that would redden on the
\* subject's own record being dropped as asked.
\*
\* TWO columns are written by hand: the measured minimum, and THE INVARIANT IT
\* WAS MEASURED AGAINST. The second is not decoration -- a minimum only binds
\* configurations that check that invariant. The fairness row sits in the safety
\* tier and runs one channel deliberately, checking `OpAdvancesIsOneActivity`;
\* holding it to a number measured on `NoAuthorizationBypass` would be a red for
\* the wrong reason. Everything else -- which constants exist, which module owns
\* a configuration, what each one assigns -- is derived by scripts/scope_gate.py,
\* because a hand-kept copy of a derivable fact is the rot this tree has paid for
\* twice already.
\*
\* A minimum EQUAL to the shipped value does not mean bigger is unnecessary. It
\* means nothing in the roster probes above it. Reading it the other way is the
\* same mistake as reading a floor as a proof.
\*
\* LOWERING A MINIMUM TAKES TWO EDITS, and this is where the second one is named.
\* The gate reads the number out of this file, so a number lowered here is a gate
\* that agrees with it -- measured, `RSKeyStore Fids` 2 -> 1 is exit 0 on the row.
\* Every minimum of 2 or more is therefore pinned in `scope_gate.MEASURED_MINIMA`
\* beside the configuration it is the minimum FOR, and the pair must agree. Move
\* one only with what re-measured it.
\*
\* Columns: <module>  <scope constant>  <measured minimum, or - >  <invariant, or - >

\* --- RSKeySecurityState ----------------------------------------------------
\* Measured over the 28 mutants and 2 historical configurations the roster then
\* held, one constant reduced at a time with the rest left shipped. It holds 30
\* now: the two token-less makeCredential mutants were driven against these
\* minima afterwards rather than assumed to fit, and move neither of them.
\* The witness: BugCmWalkIgnoresChannel explores the whole space at one channel
\* without a counterexample, and falls at two.
RSKeySecurityState     Channels               2   NoAuthorizationBypass
\* Every one of the thirty-two fires with a SINGLE relying party -- including the
\* two registration-order mutants, and BugUvNotRqdIgnoresRk, whose whole defect is
\* a registration that writes one. The module comment asks for ">= 2 to exercise
\* rpId binding"; no mutant in the roster backs that, and the honest record says
\* so rather than repeating the claim.
RSKeySecurityState     RPs                    1   NoUnmanageableCredential
\* All thirty-two fire at MaxClock = 0 as well, but that scope is degenerate: the
\* clock has one value, `Tick` is never enabled, and COVERAGE=1 names it
\* (DEAD ACTION, measured GREEN over 29 512 104 distinct). 1 is the smallest
\* scope at which the constant means anything, so 1 is what is recorded.
RSKeySecurityState     MaxClock               1   NoAuthorizationBypass
\* NOT measured, and the reason is worth keeping: neither can be reduced on its
\* own, because the soft lock is unreachable unless MaxRetries exceeds
\* MismatchLimit. The real constraint is a relation between two constants, which
\* a per-constant minimum cannot express -- so `-` rather than a number that
\* would read as measured. The fairness configuration runs the pair at 2 : 1,
\* which is the same relation one size down.
RSKeySecurityState     MaxRetries             -   -
RSKeySecurityState     MismatchLimit          -   -
\* Already at its floor: the reset window is modelled at its narrowest.
RSKeySecurityState     ResetWindow            0   NoAuthorizationBypass

\* --- the modules that carry the other two witnesses -------------------------
RSKeyStore             Fids                   2   NoRecordLostToMetaWrite
RSKeyTransport         Channels               2   NoCrossChannelSplice
\* The module's own precondition is Cap >= 2, and all three mutants fire there.
RSKeyTransport         Cap                    2   NoBufferOverrun

\* --- RSKeyAppletPolicies ----------------------------------------------------
\* The Yubico OTP position is a PAIR over a SET of slots, and each half has its
\* own witness. Every one of the four OTP mutants still falls with the use
\* counter reduced to one, which is why that row does not move.
RSKeyAppletPolicies    CounterMax             1   OtpCounterNeverRepeats
\* The session has to be able to WRAP, because the wrap is the only place the
\* persisted half moves and the only thing a cold boot rewinds. Measured, one
\* below the shipped value: both BugOtpBootKeepsPosition and
\* BugOtpSwapKeepsSession explore their whole space without a counterexample
\* when the session cannot roll, and fall as soon as it can.
RSKeyAppletPolicies    SessionMax             1   OtpCounterNeverRepeats
\* The witness for the second slot: BugOtpSwapKeepsSession is the defect of
\* re-pairing a moved record with the session counter of the slot it left, and
\* with one slot there is nowhere to move it to -- measured, it explores the
\* whole space with no counterexample there and falls at two.
RSKeyAppletPolicies    Slots                  2   OtpCounterNeverRepeats

\* --- where a single element is enough ---------------------------------------
\* Every mutant of these three fires over one element; the shipped values are the
\* domain's real vocabulary, not a measurement.
RSKeyAdminSurface      Caps                   1   DisabledAppletNeverDispatches
RSKeyRetryLattice      Max                    1   NoAuthWhenBlocked
RSKeyBootHardening     MaxWeak                1   MarkerNeverLies
\* Not a size. The open RP2350 question (M7-Q1) is a standing assumption now,
\* two-armed and registered: `assurance/assumptions.toml` carries it and
\* scripts/assumption_gate.py holds it against the tree. It stays listed here so
\* a reader of the scope record is not left thinking it is one.
RSKeyBootHardening     PowerOnClearsScratch2  -   -
\* Not a size either, and not a hardware question: whether the record write and
\* the re-arm are two STEPS. FALSE in every configuration the tiers are about --
\* one action, no window -- and TRUE in the two order rows, which is where the
\* order the tree ships becomes falsifiable at all. assurance/assumptions.toml
\* carries it; listed here so a reader of the scope record is not left thinking
\* a minimum was measured for it.
RSKeyBootHardening     RekeyOrderModelled     -   -
RSKeySecurityState     AlwaysUvShipped        -   -
RSKeyTokenRefinement   AlwaysUvShipped        -   -
TraceSecurity          AlwaysUvShipped        -   -
\* Not a size either: a two-armed standing assumption about the permission
\* domain, registered in assurance/assumptions.toml. The number that WOULD be a
\* scope -- how many of the sixteen subsets the model builds -- is what the two
\* arms differ by, and it is 5 against 16. Listed so a reader of the scope
\* record is not left thinking a minimum was measured for it.
RSKeySecurityState     WidePerms              -   -
RSKeySecurityState     ForceChangeModelled    -   -
RSKeyTokenRefinement   WidePerms              -   -
RSKeyTokenRefinement   ForceChangeModelled    -   -
TraceSecurity          WidePerms              -   -
TraceSecurity          ForceChangeModelled    -   -

\* --- tiers whose properties these minima were not measured against ----------
\* The refinement tier runs the smallest scope that still has a token; the trace
\* tier replays a recorded session at the firmware's real constants. Both are
\* deliberate, and neither is checked against a roster minimum.
RSKeyTokenRefinement   RPs                    -   -
RSKeyTokenRefinement   Channels               -   -
RSKeyTokenRefinement   MaxClock               -   -
RSKeyTokenRefinement   MaxRetries             -   -
RSKeyTokenRefinement   MismatchLimit          -   -
RSKeyTokenRefinement   ResetWindow            -   -
TraceSecurity          RPs                    -   -
TraceSecurity          Channels               -   -
TraceSecurity          MaxClock               -   -
TraceSecurity          MaxRetries             -   -
TraceSecurity          MismatchLimit          -   -
TraceSecurity          ResetWindow            -   -
